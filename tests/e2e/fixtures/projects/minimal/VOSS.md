# Minimal e2e fixture project

This tree is copied by `tests/e2e/conftest.py:tmp_project` into a tmp_path
for every e2e test. Keep contents tiny.

## Conventions

- Code style: imperative, lowercase.

## Architecture

Single `hello.voss` source. No external deps.
