<!-- generated by `voss sync` — do not edit. Customize in VOSS.md prose, not here. -->
# {{ project_name }} — Voss agent cheatsheet

## Layout

- Project root: `{{ project_root }}`{% if is_worktree %} (git worktree checkout — `.voss/` state is local to this worktree){% endif %}.
{% if type %}
- Project type: {{ type }}.
{% endif %}
- Voss state lives in `{{ voss_dir }}`. Never edit files there by hand.
- Generated docs live in `{{ docs_dir }}`. Machine-owned: every `{{ command_prefix }} sync` rewrites them.
- Managed instructions live inside the VOSS.md workflow fence. Edit prose outside the fence only; never touch the fence markers or hash.

## Active capabilities

{% if capabilities %}
{% for cap in capabilities %}
- {{ cap }}
{% endfor %}
{% else %}
- None active. Enable via the `project:` block in `.voss/config.yml`, then re-run `{{ command_prefix }} sync`.
{% endif %}

## Conventions

{% if install_command %}
- Install dependencies with `{{ install_command }}`.
{% endif %}
{% if check_command %}
- Run `{{ check_command }}` before committing. Never commit with it failing.
{% endif %}
{% if tools %}
- Project tools: {% for tool in tools %}`{{ tool }}`{% if not loop.last %}, {% endif %}{% endfor %}.
{% endif %}
- Run `{{ command_prefix }} sync` after changing `.voss/config.yml`. Never edit generated docs directly.
