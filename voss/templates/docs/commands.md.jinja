<!-- generated by `voss sync` — do not edit. -->
# voss command reference — {{ project_name }}

## Invocation

{% if is_worktree %}
This checkout is a git worktree. Run all commands from the worktree root: `cd {{ project_root }}`. `.voss/` state here is local to this worktree; git history is shared with the main checkout.
{% else %}
Run all commands from the repository root: `cd {{ project_root }}`.
{% endif %}

## Core

- `{{ command_prefix }} sync` — regenerate managed docs, the VOSS.md fence, and synced prompts. Idempotent; safe to re-run.
- `{{ command_prefix }} chat` — interactive agent REPL.
- `{{ command_prefix }} do "<task>"` — one-shot agent task.
{% if "memory" in capabilities %}

## Memory

- `{{ command_prefix }} memory` — inspect and adopt project memory.
{% endif %}
{% if "review" in capabilities %}

## Review

- `{{ command_prefix }} board` — review board state.
- `{{ command_prefix }} review` — drive the review workflow (see `review.md`).
{% endif %}
{% if "eval" in capabilities %}

## Eval

- `{{ command_prefix }} eval` — run project evals.
{% endif %}
