<!-- generated by `voss sync` — do not edit. -->
# Review workflow — {{ project_name }}

{% if review.enabled %}
Review is enabled for this project.
{% if review.reviewers %}
Reviewers: {% for reviewer in review.reviewers %}`{{ reviewer }}`{% if not loop.last %}, {% endif %}{% endfor %}.
{% endif %}

- Track work as board cards: `{{ command_prefix }} board`.
- Request review before marking a card Done. Never self-approve.
- Run `{{ command_prefix }} review` to drive the reviewer loop.
{% else %}
Review is not enabled. Set `project.review.enabled: true` in `.voss/config.yml` and re-run `{{ command_prefix }} sync`.
{% endif %}
