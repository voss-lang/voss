"""Voss TUI subpackage (M9).

The full Textual app lands in M9-02. M9-01 ships the capability shim
(`capability.py`) used by the CLI to decide whether to bootstrap the TUI
or fall back to PlainRenderer.
"""
