"""V25 server-native swarm runtime package.

Holds the append-only JSONL event log (`events.py`) and the git-tracked
role-prompt templates (`prompts/`). The SwarmStore itself lives one level up
in `voss/harness/swarm_store.py` (imported by `server/app.py`).
"""
