---
gsd_state_version: 1.0
milestone: v0.1.1
milestone_name: patch)*
status: executing
last_updated: "2026-06-07T15:58:41.988Z"
last_activity: 2026-06-07
progress:
  total_phases: 38
  completed_phases: 0
  total_plans: 0
  completed_plans: 0
  percent: 0
---

# State: Voss

## Project Reference

See: `.planning/PROJECT.md` (updated 2026-05-10)

**Core value:** A developer can give Voss a repo task and get bounded, inspectable, resumable AI coding work, while the most important agent logic is expressible as compiler-checkable `.voss` workflows instead of prompt soup.
**Current focus:** M0 — Scope Lock planning rebaseline from `.vscode/voss_v_0_1_scope_lock.md`.

## Current Position

**Phase:** A4 — voss-app Layout Presets
**Status:** Ready to execute
**Goal:** Add L1 visual layout presets (`fanout`, `pipeline`, `swarm`, `watchers`), `Cmd+G` cycling, non-destructive preset switching, and named layout save/load.
**Critical build order:** A4 depends on the completed A3 grid substrate. A3-06 must integrate GridRoot into App and register Rust grid sync before A4 implementation starts.
**Next move:** Finish/verify A3-06, then run `/gsd:execute-phase A4`.
**Rust status:** `crates/` frozen-spike untouched. NEW `crates/voss-app-core` (A1, path-dep'd by src-tauri, populated A2+) + `apps/voss-app/src-tauri` are the live voss-app track members — distinct from the frozen v0.1-ship crates.
**Last activity:** 2026-06-07

## Phase Status

| Phase | Name | Status |
|---|---|---|
| M0 | Scope Lock | Ready to plan |
| M1 | Harness Happy Path | Ready to execute (7 plans) |
| M2 | Project Cognition | Ready to execute (7 plans) |
| M3 | Language Validation | Ready to execute (6 plans, 4 waves) |
| M4 | Voss-authored Harness Loop | Complete (5 plans, 5 waves) |
| M5 | Eval and Distribution Prep | 6/6 plans summarized (M5-01..M5-06). |
| M6 | npm Wrapper | Plans ready to execute (5 plans, 4 waves, verified iteration 2/3). |
| M7 | SDK Polish | Plans ready to execute (6 plans, 6 waves, verified iter 2/3 PASS clean). |
| M11 | Voss-aware Tools (CAPS-01b) | Plans ready to execute (5 plans, 5 waves, inline verification because local GSD agents are not installed). |
| M13 | Multi-agent in Chat (CAPS-01d) | ⊘ ABSORBED into V8 — V8 shipped the V4-backed persisted unification (2026-06-06). M13's in-memory `M13Allocator` superseded by the V4 `SessionTreeManager`; artifacts retained as reference. |
| T8 | Input Bar Ergonomics | Complete (5/5 plans summarized, focused T8 suite green). |
| F2 | Hybrid Semantic Search | Plans ready to execute (3 plans, 3 waves; BM25 lexical fallback, Chroma RRF hybrid, dependency closeout; inline planning because local GSD agents/check router are unavailable). |
| A1 | voss-app Tauri Shell | Complete (4/4 plans executed + human-verified; SHL-01..06). |
| A2 | voss-app PTY Pane | Complete (5/5 plans; PTY-01..08; PTY pane live in shell, proven via Claude Code in-pane). |
| A3 | voss-app Grid Engine | Complete (6/6 plans; GRD-01..08 closed; D-01 9-pane Canvas perf bar human-approved on dev machine; Warp locked-tiling realized via balanceRatios). |
| A4 | voss-app Layout Presets | Plans ready to execute (A3 substrate now in place; 6 plans, 5 waves). |
| A6 | voss-app Session Persist | Context gathered (12 decisions D-01..D-12). Ready for planning. |
| A7 | voss-app Cmd Palette + Keymap | Context gathered (16 decisions D-01..D-16). Ready for planning. |
| A8 | voss-app Workspaces, UX Polish, & Theming | Context gathered (14 decisions D-01..D-14). Ready for planning. |
| A9 | voss-app Settings + Theme | Plans ready to execute (4 plans, 3 waves; D-01..D-16 covered; plan-checker 12/12 PASS). |
| A10 | voss-app Status Bar | Plans ready to execute (4 plans, 3 waves; BAR-01..08 covered; plan-checker iter 2/3 PASS clean). |
| M14 | Long-running Tasks + Watch (CAPS-01e) | M14-01..02 complete; M14-03/M14-04 Wave 3 tool+CLI surfaces next. |
| M15 | Skill / Plugin Marketplace (CAPS-01f) | Complete (6/6 plans, 5 waves; SKILL-01..06 all GREEN; 2026-05-20). |
| O6 | Audit Product + Calibration + Liveness Hardening | ⊘ SUPERSEDED by V9 (2026-06-05). 6 ready plans re-point to V9; O1–O6 archived-as-superseded (see ORCHESTRATION-PLAN.md banner). |
| V0–V12 | Agent Engineering Organization Layer | Roadmap-listed (2026-06-05); supersedes O-track + absorbs M13. SPEC pending per phase. Design: docs/ORCHESTRATION_LAYERS.md. Build order V1→V3→V4(keystone)→V5→V6→V7→V9, then V2/V10/V11/V12. |
| F3 | Budget & Token Visualization | Complete (3/3 plans; OSC budget pipeline, BudgetBar/BudgetPopover HUD, targeted Rust/Python/TS/app gates green; live visual checkpoint closed by operator-directed closeout on 2026-05-22). |
| F4 | Visual Context Heatmap | Plans ready to execute (4 plans, 3 waves; D-01..D-27 covered; research deviation D-19 stdin→file-based pin channel). |
| F5 | Commit with Critique Hook | Plans ready to execute (2 plans, 2 waves; D-01..D-16 covered; zero new deps). |
| V5 | Board State Machine (supersedes O3) | ✅ COMPLETE — VBOARD-03/07/10 implemented (Card fields, self-Done `no-reviewer` guard, `voss board` CLI); shipped surface VBOARD-01/02/04/05/06/08/09 regressed green; full board suite 121 passed; frozen schemas untouched; O3 superseded. |
| V7 | Engineering Manager Loop (supersedes O5) | ✅ COMPLETE — 3/3 plans, 3 waves. `voss team run "<goal>"` composes V3 team + V4 session tree + V5 board + V6 Reviewer-A/B two-source slots + O5 em_loop → terminal on stub; `RunFinal` persisted to `.voss/sessions/<root_id>/run-final.json` (10 fields + `sign_off`); human approve/reject sign-off (record-only). Cage re-verified (no set_ceiling/set_p/extend_budget; undeclared-role dispatch denied; kill/rescope lineage). Zero frozen-schema drift; no new deps; O5 superseded. |
| V8 | Multi-agent Chat + Live Steering (absorbs M13) | ✅ COMPLETE — 3/3 plans, 3 waves. Shipped the V4-backed persisted unification: VMAG-10 (every chat spawn a persisted session-tree node), VMAG-UNIFY (single V4 `SessionTreeManager`; `M13Allocator` removed), VMAG-07 (per-node-manager recursion, viable-floor bound, no depth constant), VMAG-ROOT (chat-root 60k+30k-reserve envelope, finalized on exit). MAG-01..09 + steer + orphan-teardown regressed green; frozen schemas unchanged; no new deps. ADE child panels → V11. |
| V12 | Safety & Factory Fallbacks | Plans ready to execute (4 plans, 4 waves; inline plan-check because local GSD agent/checker verbs are unavailable). Policy + audit first: `.voss/safety.yml`, runtime safety overlay, factory fallback audit, EM/direct parity. |

## Recent Activity

- 2026-06-07 — **Phase V13.2 (Rust Local / Native Client SDK) SPEC + CONTEXT done.** `/gsd:spec-phase V13.2` → 7 requirements locked, ambiguity 0.144 (gate ≤0.20). New `voss-sdk` reusable lib crate for the locked REST/SSE protocol: generated contract/event types (serde tagged enum from the snapshot), typed REST client, typed SSE event client (`impl Stream`), handshake+bearer auth helpers, `voss serve` supervisor, protocol-exposed `sessions/saved`/`cost`/`doctor` readers. Mirrors V13.1 boundaries (crates.io publish OUT, deep audit/session-tree readers gated V4/V9, real-`voss serve` integration tests via `VOSS_SERVE_FAKE_TURN`, no orch reimpl, no PROTOCOL edits). `crates/voss-tui` left byte-stable (zero-diff AC); its dedupe onto the SDK deferred. SPEC reconciliation: "leave voss-tui untouched" + "AppEvent maps from it" → SDK owns the full 21-member union (no `Other` catch-all) + a parity-tested lossy projection mirroring voss-tui's `AppEvent` shape; no SDK→voss-tui dep. `/gsd:discuss-phase V13.2` → 3 HOW decisions locked: **D-01** codegen = Oxide `typify` (types only: event-union JSON-schema → tagged enum + OpenAPI components → REST structs) + hand-written client modeled on `voss-tui/src/net.rs` (progenitor/hand-roll rejected); **D-03** reuse voss-tui stack (reqwest rustls + eventsource-stream + tokio + futures-util workspace pins); **D-05** crate at `crates/voss-sdk` (workspace member, not `sdk/rust/`); **D-07 dependency correction** — real exec dep is **V13.1** (which OWNS `contracts/{openapi.json,events.schema.json}` + exporter + drift gate per V13.1 D-10), **not V13** (docs-only); snapshot not on disk yet (V13.1 unexecuted). Generated Rust types checked-in + cargo regen-drift gate (D-06/D-08). Resume: `/gsd:plan-phase V13.2` (blocks on V13.1 shipping `contracts/` at execution; planning may proceed ahead).
- 2026-06-06 — **Phase V8 (Multi-agent Chat + Live Steering, absorbs M13) COMPLETE — 3/3 plans, 3 waves.** Unified the M13 in-memory fan-out onto the V4 persisted substrate. V8-01: RED scaffold `test_multiagent_session_tree.py` (6 classes, 12 tests, no xfail) driving the planned `node_manager=`/persisted-node surface; router fixed to key on the stripped task text (tmp_path embeds the test name → "grandchild" leaked into routing). V8-02 (3 prod files): `session_tree.py` += `SessionTreeManager.release_child` (only change); `multiagent.py` removed `M13Allocator`, `attach_multiagent_tools(allocator=)`→`node_manager: SessionTreeManager`, `ChildHandle.node` (only new dataclass field), inline even-split → `allocate_child(scope="chat", role=agent)` (BudgetAllocationError = authoritative no-oversell), per-node `child_manager` recursion (reserve=`VIABLE_FLOOR`, no depth constant), `finalize_node` on done/error/interrupt + `release_child`; `cli.py` creates `_chat_root`+`_chat_tree` once per `_run_repl`, injects `node_manager=_chat_tree`, finalizes root on exit. V8-03: migrated `TestEvenSplitRebalance`/`TestNoOversell`/`TestDepth2` off `M13Allocator` to the V4 path (xfail dropped; `TestConcurrentInFlight` xfail also dropped — now hard-green); `M13Allocator`=0 in tests; `test_subagent_recursion.py` + `TestBackCompatRecursionPinIntact` byte-stable green. **3 reconciled plan contradictions:** (1) even-split `//(active+2)` not greedy `//n` (operator-approved — V4 limits immutable, can't rebalance like M13, so headroom-reserve lets siblings coexist); (2) kept a V4-backed `node_manager=None` fallback (plan said remove all fallback, but V8-03 requires `TestOrphanTeardown` green unchanged with a no-`node_manager` call — fallback is a V4 root, not `M13Allocator`, so `test_no_m13allocator_attribute` stays green); (3) router fix in the V8-01 test file (beyond declared files — my own scaffold bug). **Verification:** full `tests/harness/` failure set IDENTICAL with V8 test-edits stashed (22 failures, all pre-existing: openai-401 env, stale EXIT_REASONS [red since O3/O5], t1 perf, tui hash-baseline on unchanged recorder.py, repl_slash, conventions/dog07 env — none in V8-touched modules); V8 surface suites (session_tree/multiagent/fanout/recursion/steer/subagent-pin) all green; frozen records (session.py/recorder.py/voss_runtime) zero drift; only new dataclass field = `ChildHandle.node`. Pre-existing `tests/e2e/test_multiagent_chat_e2e` AUTH_STEERED unchanged (same line 399, not regressed). No ADE panel (→V11). Resume: V9 (Audit Product, depends V7✓) or V10/V11/V12.
- 2026-06-06 — **Phase V7 (Engineering Manager Loop, supersedes O5) COMPLETE — 3/3 plans, 3 waves.** Delta on the shipped O5 pieces: pure composition, no reimplementation. V7-01 (`fd5fb2f` baseline) the 10 RED scaffolds. V7-02 (`8fe7caa` feat) `voss/harness/cli.py` only — `@team_group.command("run")` (`voss team run "<goal>" --cwd --max-iterations`) + `_default_team_config()` (DEFAULT_ROSTER + `TeamCeiling(budget_tokens=500_000, latency=3600)`, direct construction, no AST `TeamDecl`) + `_persist_run_final()` (`.voss/sessions/<rf.root_id>/run-final.json`, `dataclasses.asdict` 10 fields + superset `sign_off`, `chmod 0o600`, root_id-derived path → T-V7-05 traversal-safe). Inner `asyncio.run(_run())` builds the stack in order: `create_root(500_000)` → `SessionTreeManager(reserve=0)` → **both** real V6 slots `reviewer_a`(A/fast)+`reviewer_b`(B/strong) `DeterministicReviewerStub` (satisfies the two-source Done gate) → `Board.from_team_config(per_card_budget=100_000)` → **pre-spawn `board.spawn_card(risk_tier="med")`** (so `total_cards>=1`, Pitfall 1) → `EMBoardHandle` → `DeterministicEMStub([CreateTicketOp(worker_role="backend"), NoopOp])` → `em_loop`. **Deviation:** `finalize_run()` hardcodes `idea=""` (handle.py:352), so threaded the goal via `dataclasses.replace(rf, idea=goal)` (same frozen-replace em_loop uses for `em_iterations`; cli.py stays the only production file). `click.prompt(Choice[approve,reject])` then re-persist with the decision; reject record-only. **Guard scoped (operator decision):** `team run` imports the always-pass `DeterministicReviewerStub` into shipped cli.py, tripping `tests/harness/board/test_stub.py::TestProductionImportGuard` — allowlisted `voss/harness/cli.py` alongside `stub.py` (SPEC frames this as the stub smoke-run; guard still blocks stub imports in every other production file). **Pre-existing flake found+fixed in-branch:** 3 sync tests in `test_em_handle_dispatch.py` hand-rolled `asyncio.get_event_loop().run_until_complete(...)` → `RuntimeError` in py3.13 after a prior `asyncio.run` closes the loop (reproduced on untouched `board/ + em/` combo); swapped to `asyncio.run(...)`. V7-03 (this) verify: cage (`test_em_handle_cage`)+lineage (`test_em_lineage`)+dispatch green; full em suite 88 green; `test_team_backcompat_regression -k schema` green; `git diff 839aa6c..HEAD` on session.py/recorder.py/voss_runtime/tickets.py/pyproject.toml = **empty** (zero frozen-schema drift, no new deps); V7 touched exactly cli.py + 2 test files + summaries. All 7 V7 SPEC acceptance criteria met across V7-01/02/03. Resume: V8 (Multi-agent Chat, executable now) or V9 (Audit Product, depends V7 ✓).
- 2026-06-06 — **Phase V5 (Board State Machine, supersedes O3) COMPLETE — 4/4 plans, 3 waves, executed via subagents.** V5-01 (`b2b3102`/`e292c57`) 3 RED scaffolds (14 tests: test_card_fields_v5/test_self_done_guard/test_board_cli) driving the real planned surface — 12 RED, 2 invariant-GREEN. V5-02 (`f7c378f`/`da5dd2f`, machine.py only) 4 additive frozen-`Card` string fields (idea/role/acceptance_criteria/verification_requirement, `""` defaults) + module-level `card_status`/`card_budget` helpers (NOT @property — slots interaction) + self-Done `no-reviewer` guard in `Board.move` gated on a new `self._reviewer_injected` flag (`reviewer is not None or reviewer_a is not None or reviewer_b is not None`) — NOT naive `self._reviewer is None` (which defaults to reviewer_b), so the V6 A/B-only two-source boards still reach Done while a `reviewer=None` board fails closed with exactly one refused delta. V5-03 (`9ed7496`) new `voss/harness/board/cli_view.py` `render_board(cwd, root_id=None)` (read-only, no live Board/manager, local `_COLUMNS`, column rule copied verbatim from audit/load.py, mtime-latest root, path-traversal guard rejecting `/`+`..` before FS touch) + standalone `board_cmd` (`voss board [root_id] --cwd`) in `AGENT_COMMANDS`. V5-04 closeout (inline, not subagent): switched the stale `test_exit_reasons_is_sorted_superset_of_pre_o3` to an `issubset` check (future-proof against additive EXIT_REASONS members; was already green from the V4-closeout fix); confirmed frozen schemas (RunRecord/SessionRecord/BudgetScope/SessionTreeNode) untouched; ROADMAP O3 row already banners ⊘ SUPERSEDED by V5, V5 row+block marked COMPLETE. **Full board suite 121 passed, 0 failed.** redaction + team_check green (no AGENT_COMMANDS regression). Resume: V7-02 (team run impl, 10 V7-01 RED scaffolds await) or V8.

- 2026-06-06 — **Phase V8 (Multi-agent Chat + Live Steering, absorbs M13) PLANNED — 3 plans, 3 waves, plan-checker PASS (0 blockers, 2 doc-state warnings fixed).** `/gsd:plan-phase V8`: CONTEXT direct from SPEC (V-track default); gsd-phase-researcher HIGH; VALIDATION (Nyquist Dim-8); gsd-pattern-mapper (6/6); gsd-planner (opus); gsd-plan-checker (12 dims). **Sequencing handled (V7-lesson — verified disk before trusting SPEC):** at planning start V4 was mid-execution (V8 hard-depends V4+M13); operator chose "plan against post-V4 surface." Researcher adapted to live disk (V4-01/02 landed during research, M13 fully shipped — 10 xfail XPASSED); by close V4-03 also landed → **V4 fully complete, V8 dependency satisfied, executable now.** Core change pinned: replace in-memory `M13Allocator(reserve=…)` with `SessionTreeManager(child_node, reserve=VIABLE_FLOOR, cwd)` (per-node manager = depth>1 recursion, no depth constant); `ChildHandle.node` field (only dataclass change); additive `release_child` method on manager; 3 finalize sites (gather done/error, teardown interrupt, chat-root finally done — all `EXIT_REASONS` members, none new); chat root `create_root(60k)+reserve 30k`. **Pitfall-7 coupling:** `attach_multiagent_tools(allocator=)`→`node_manager:` rename + cli.py:2019 call-site update kept in the SAME plan/wave (V8-02 T2+T3). Frozen records untouched; `test_subagent_recursion.py` zero-change; known pre-existing `test_multiagent_chat_e2e` AUTH_STEERED failure flagged must-not-regress. DAG W0 V8-01 RED scaffold (`test_multiagent_session_tree.py`, 6 classes, no xfail mask) → W1 V8-02 (multiagent.py+session_tree.py+cli.py; STRIDE threat_model) → W2 V8-03 (migrate EvenSplit/NoOversell/Depth2 off M13Allocator, MAG-01..09 regress, M13-absorbed bookkeeping). `.venv/bin/python` throughout. Resume: `/gsd:execute-phase V8`.
- 2026-06-06 — **Phase V4 (Session-Tree Substrate + Budget Fan-out, supersedes O1 — KEYSTONE) COMPLETE — 3/3 plans, 3 waves (sequential chain), executed via subagents.** V4-01 (`0010768`) additive nullable `scope`/`role` on `SessionTreeNode` + `allocate_child` kwargs + `_hydrate_node` back-compat + `"error"` added to `EXIT_REASONS` frozenset; `_NODE_JSON_KEYS` intentionally extended (SessionTreeNode schema-lock, distinct from frozen records). V4-02 (`913dee0`, **the keystone**) `run_subagent` pre-emptive spend guard (refuses to start a call when `spent>=limit`, finalizes budget before any spend) + `mutate_envelope` spend wiring from real token totals (guard live, not dead code) + `except asyncio.TimeoutError`/`except Exception`/`finally` net so error/timeout/budget each emit exactly one finalized node; signature unchanged; documented gap T-V4-14 (tool-dispatch `node=None` unguarded → V5/V7). V4-03 (`d67584a`/`e2be258`) read-only `export_tree(root_id, cwd)` + `SessionTreeNotFoundError` (glob aggregation, carries scope/role, round-trips via `_hydrate_node`) + `voss session tree <root_id>` CLI (Option A non-breaking — `sessions_cmd` intact) in `AGENT_COMMANDS`. Frozen records untouched; `test_session_redaction.py` green UNMODIFIED throughout. **One V4-caused regression found+fixed in closeout:** board-copy `test_session_tree_additive::...superset_of_pre_o3` (asserted `pre_o3|{timeout,killed}`) broke when V4-01 added `"error"` — V4-01's verify scope (test_session_tree+redaction only) missed it; superset assertion updated to include `"error"`. NOTE for V5-04: that planned "stale-test fix" is now already applied. V4-touched surface 67 green. Remaining full-harness reds are pre-existing/unrelated: V7-01 RED scaffolds (10), cross-suite asyncio flakes that pass in isolation (em dispatch + session resume), two stale EXIT_REASONS tests omitting timeout+killed (red since O3/O5), memory/recall openai-401 + tui runtime-drift. Resume: `/gsd:execute-phase V5` or V7-02.

- 2026-06-06 — **Phase V5 (Board State Machine, supersedes O3) PLANNING COMPLETED — resumed partial plan to 4 plans/3 waves, plan-checker PASS (all 12 dims).** `/gsd:plan-phase V5` resume: prior run had stalled at V5-01 only (529). Spawned gsd-planner (opus) to ADD V5-02/03/04 (V5-01 untouched/committed), then gsd-plan-checker (sonnet) → VERIFICATION PASSED. DAG `V5-01 → {V5-02, V5-03} → V5-04`. V5-02 (`machine.py`: 4 additive Card fields + `card_status`/`card_budget` + self-Done `no-reviewer` guard, VBOARD-03/07), V5-03 (`cli_view.py` new + `cli.py` `board_cmd`, mtime-latest + traversal-safe, VBOARD-10), V5-04 (shipped-surface regress VBOARD-01/02/04/05/06/08/09 + stale-test `test_exit_reasons_is_sorted_superset_of_pre_o3` fix + frozen-schema zero-drift + O3-superseded bookkeeping; SPEC reqs 4/5). Checker flagged + confirmed handled: `Board(reviewer=None)` defaults `_reviewer` to `reviewer_b` (machine.py:260) so guard must track an injected-reviewer flag, not naive `is None`. Plans committed `a7fdb83`. Resume: `/gsd:execute-phase V5` (Wave 2 V5-02+V5-03 parallel).
- 2026-06-06 — **Phase V12 (Safety & Factory Fallbacks) PLANNED — 4 plans, 4 waves, inline plan-check PASS.** `/gsd:plan-phase V12`: operator chose to continue without CONTEXT.md and research first. Local GSD `query`/agent/checker verbs were unavailable, so research/planning/checking ran inline per precedent. Scope locked from V12-SPEC: policy + audit first; no broad pipeline DSL, real deploy/payment/migration integrations, rollback, or ADE UI. Artifacts: V12-RESEARCH.md (PermissionGate/tool invocation/RunRecorder/EM seams + Validation Architecture), V12-VALIDATION.md, V12-PATTERNS.md, and four executable plans. Plan shape: W1 V12-01 safety schema/loader/classifier (`.voss/safety.yml`, strict validation, unknown runbook refs fail closed) -> W2 V12-02 PermissionGate and `_invoke_step_with_gate` safety overlay (`auto_yes` cannot bypass irreversible confirmation; runbook/fixed-pipeline route-or-deny; ordinary operations unchanged) -> W3 V12-03 additive factory fallback audit evidence (`RunRecorder`/`RunRecord`, redaction, old-record hydration) -> W4 V12-04 EM role-gate parity and weak-model scaffold context (`gate_for_role` preserves safety policy; direct and EM-derived gates share decisions; EM cage unchanged). Coverage gate: VSAFE-01..07 all assigned to plans; plan structure and git diff check passed. Resume: `/gsd:execute-phase V12`.
- 2026-06-06 — **Phase V7 (Engineering Manager Loop, supersedes O5) PLANNED — 3 plans, 3 waves, plan-checker iter2 PASS.** `/gsd:plan-phase V7`: CONTEXT synthesized direct from SPEC (V-track default, discuss skipped); gsd-phase-researcher HIGH; VALIDATION (Nyquist Dim-8); gsd-pattern-mapper; gsd-planner (opus). **Caught a stale-research defect:** the researcher ran against a pre-merge tree and reported "V6 unexecuted / single `reviewer=` slot / board/ 13 RED / Pitfall-2 TypeError" — verified WRONG on disk (V6 COMPLETE: `Board.from_team_config` has `reviewer_a`/`reviewer_b` machine.py:281-283, `domain_inferred` verdict.py:34, `voss review` cli.py:2487, board/+em/ both 100% green; V6 merged via `d35c7ae` mid-research). Corrected CONTEXT/RESEARCH/VALIDATION/SPEC/PATTERNS to disk-truth + **replanned to inject the real V6 A/B reviewers** (`reviewer_a`+`reviewer_b` DeterministicReviewerStub, satisfying the two-source Done gate). Plan shape: W1 V7-01 RED scaffold `tests/harness/test_team_run_cli.py` (10 tests, real surface, no fictional API / no xfail mask per memory `gsd-scaffold-fictional-api`) → W2 V7-02 `@team_group.command("run")` + `_default_team_config()` (DEFAULT_ROSTER + `TeamCeiling(budget_tokens=500_000)`) + `_persist_run_final()` (`.voss/sessions/<rf.root_id>/run-final.json`, `dataclasses.asdict` 10 fields + `sign_off`, chmod 0o600, root_id-derived path; STRIDE threat_model) + pre-spawn `board.spawn_card(risk_tier="med")` + `asyncio.run(em_loop(...))` sync-body + `click.prompt(Choice[approve,reject])` record-only → W3 V7-03 verify EM cage (em/ gate; undeclared-role→EMCageViolation; no set_ceiling/set_p/extend_budget) + frozen-schema zero-drift + O5 superseded. `.venv/bin/python` throughout. Resume: `/gsd:execute-phase V7`.
- 2026-06-06 — **Phase V5 (Board State Machine, supersedes O3) PARTIAL PLAN — SPEC→PATTERNS landed, planner stalled 1/N (529 overload).** `/gsd:plan-phase 5`→ disambiguated (bare "5" resolved to legacy executed `05-cli-packaging-linguist`; user picked V5). CONTEXT direct from SPEC; gsd-phase-researcher HIGH (guard site machine.py:358/379; found a pre-existing stale test `test_exit_reasons_is_sorted_superset_of_pre_o3` to fix); VALIDATION; gsd-pattern-mapper (7/7 analogs). gsd-planner died on a transient 529 after writing V5-01 only. Resume: `/gsd:plan-phase V5 --chunked` (chunked resumes from the committed plan).
- 2026-06-06 — **Phase V6 (Reviewer A/B Split, supersedes O4) COMPLETE — 5/5 plans, 5 waves, VREV-03..10 + VREV-05 GREEN.** W0 V6-01 fixed the pre-existing exit-reasons red baseline (+`killed`) and laid 13 RED scaffolds (two-source gate, domain_inferred, sidecar, review CLI) + 6→7-field verdict edit. W1 V6-02 added `ReviewerVerdict.domain_inferred` (7th defaulted field, last; zero-transitive-harness-import contract intact); Reviewer-B populates+clamps to `{code,ai,docs,unknown}` (garbage/parse-fail→unknown), Reviewer-A defaults. W2 V6-03 wired the two-source Done gate: `GateContext` A/B reviewer+verdict slots, `a_verification_passes`+`b_passes` predicates (cheap→expensive; conf_meets_p stays intermediate-only), `Board.reviewer_a/reviewer_b` + legacy `reviewer` alias (with `self._reviewer`→A fallback so the intermediate conf gate still functions in pure two-stub mode), B-`block`→Blocked terminal seam via `_force_terminal`, and `review_persistence._write_review_sidecar` (0o600 `.review.json`: a_verification/b_verdict/final_outcome, guarded so pure A-fail writes nothing); also widened `verdict_snapshot` capture to `ctx.verdict or verdict_b or verdict_a`. W3 V6-04 shipped read-only `voss review [run_id]` (latest-by-mtime default, mirrors sessions_cmd, no live Board/manager/provider, corrupt-sidecar guard) registered in AGENT_COMMANDS. W4 V6-05 close-out: full board suite **106 passed / 0 failed**; O4 behaviors verified intact (A-excludes-EM-AC `test_a_uses_original_idea`, B-narrative-blind `test_b_message_isolation`, B-Residual-2-block `test_b_residual_2_block`); frozen records (RunRecord/SessionRecord/BudgetScope) field-unchanged (no V6 commit touched session.py/budget.py + V3-03 dataclasses.fields freeze green); ROADMAP O4 superseded-by-V6 banner (artifacts retained as production surface, plan list kept for lineage) + V6 plans `[x]`; `voss review` output legibility **human-approved**. Repo auto-commit watcher batched plan edits into its own commits throughout. Resume: V7 (Engineering Manager Loop, supersedes O5).

- 2026-06-06 — **Phase V3 (`.voss team{}` Spec + Specialist Roster, supersedes O2) plans V3-01..03 executed.** V3-01 tier-based `RoleDefaults` roster + config-backed `get_model_tiers()` (`[model_tiers]` over built-in defaults; only home of concrete model names) + closed-set tier resolution (`strong`/`cheap`/`fast`) in `_parse_model_value` with raw passthrough. V3-02 `voss team check [path]` CLI wrapping the single `compile_team` validator (0/1/non-zero exits, --json). V3-03 V1-capability seam marker on `filter_toolset_for_role` (comment-only) + back-compat/TEAM-04/05/06 regression suite + schema-freeze guard. ~90 team-surface tests green; team.py carries no hardcoded model names.

- 2026-06-05 — **Added V0–V12 Agent Engineering Organization Layer track to ROADMAP.md.** New V-prefix track from `docs/ORCHESTRATION_LAYERS.md` PRD (P0–P12 → V0–V12). **V supersedes the O-track** (V3↔O2, V4↔O1, V5↔O3, V6↔O4, V7↔O5, V9↔O6 — O1–O6 archived-as-superseded with banner in ORCHESTRATION-PLAN.md; O6's 6 ready plans re-point to V9) and **absorbs M13** into V8. Requirement IDs namespaced `V*` (VRFM/VCAP/VPRIN/VTEAM/VTREE/VBOARD/VREV/VEM/VMAG/VAUD/VLANG/VADE/VSAFE) to avoid LANG/MAG/ADE clashes with M3/M13/A12. No SPECs yet — next step per phase is `/gsd:spec-phase V{n}` to lock the namespaced requirements (keystone = V4 session-tree + pre-emptive budget fan-out). Current active focus unchanged (A4). Docs touched: ROADMAP.md, docs/ORCHESTRATION_LAYERS.md (status banner), ORCHESTRATION-PLAN.md (superseded banner), STATE.md.

- 2026-05-22 — **Phase F5 (Commit with Critique Hook) planned — 2 plans, 2 waves.** W1 F5-01: consensus.py (Pydantic models, YAML constraints loading, single-shot provider.complete critique, structured output formatting) + consensus_cmd CLI registration + 15+ unit tests covering D-01..D-04, D-08..D-16. W2 F5-02: hooks_group (install/uninstall) + hook lifecycle tests covering D-05..D-07. Zero new deps (pyyaml, pydantic, click all already in pyproject.toml). All 16 CONTEXT decisions mapped. Resume: `/gsd:execute-phase F5`.

- 2026-05-22 — **Phase F5 (Commit with Critique Hook) context gathered.** `/gsd-discuss-phase F5`: all 4 gray areas discussed. F5-CONTEXT.md captures D-01..D-16. **Constraint format:** D-01 natural language rules in `.voss/constraints.yml`; D-02 system prompt injection (constraints + diff in one context); D-03 constraints.yml only (no conventions merge); D-04 skip silently if no file. **Hook lifecycle:** D-05 `voss hooks install/uninstall` CLI; D-06 thin shell shim (`exec voss consensus --staged`); D-07 refuse if hook exists (--force override); D-08 `voss consensus` is a real standalone CLI command. **Output & gate:** D-09 configurable `mode: block|warn` in constraints.yml; D-10 structured violations list (constraint + file:line + explanation); D-11 violations only (summary shows totals); D-12 one-liner on clean pass. **Agent model:** D-13 single-shot prompt (one LLM call, ~2-5s); D-14 same provider as configured; D-15 no budget cap; D-16 fail open on LLM errors. Resume: `/gsd:plan-phase F5`.

- 2026-05-22 — **Phase F3 (Budget & Token Visualization) COMPLETE — 3/3 plans.** F3-01 shipped Python `_emit_budget_osc`, agent iteration emission, Rust `BudgetData`/`BudgetUpdate`, and PTY OSC stripping. F3-02 shipped `budget_update` transport handling, `BudgetBar`, `BudgetPopover`, `Popover`, and component tests. F3-03 wired local pane budget state, agent-pane-only header HUD, click popover, and 150ms bar transition. Closeout gates: `cargo test -p voss-app-core` 132 passed; `python3 -m pytest voss/harness/test_budget_osc.py -q` 4 passed; `cd apps/voss-app && npm run test -- --reporter=dot` 525 passed; `npx tsc --noEmit` clean; `npm run build` passed with the existing large-chunk warning; `cargo build -p voss-app-core` passed. Runtime visual checkpoint was closed by operator-directed closeout; no independent live LLM-pane screenshot was captured in this session.

- 2026-05-22 — **Phase F4 (Visual Context Heatmap) context gathered.** `/gsd-discuss-phase F4`: all 4 gray areas discussed. F4-CONTEXT.md captures D-01..D-27. **Panel hosting:** D-01 collapsible right-side overlay 240px (⌘I toggle); D-02 per-pane scope (switches on focus); D-03 outside grid overlay (no reflow); D-04 header "Context — ●3 ~/project"; D-05 fixed 240px; D-06 persist open/closed in settings.json; D-07 150ms CSS slide; D-08 empty state for shell panes; D-09 status bar toggle button; D-10 no search; D-11 below overlays z-layer; D-12 compact summary row at top. **Data granularity:** D-13 per-file rows; D-14 3-state full/compressed/dropped (green/yellow/gray); D-15 sort by token count desc; D-16 filename + tokens + mini bar (hover for full path); D-17 system prompt + conversation as special rows. **Pinning:** D-18 ship with pinning; D-19 PTY stdin injection (`voss-pin=` OSC); D-20 next-iteration effect; D-21 immune to compression; D-22 pin only existing context files. **Emission:** D-23 new `voss-context=` OSC type; D-24 per-iteration after budget; D-25 `{path, tokens, state, pinned}` per file; D-26 full snapshot each emission; D-27 pin ack via next context emission. Resume: `/gsd:plan-phase F4`.

- 2026-05-21 — **Phase F3 (Budget & Token Visualization) context gathered.** `/gsd-discuss-phase F3`: all 4 gray areas discussed. F3-CONTEXT.md captures D-01..D-14. **Data pipeline:** D-01 OSC escape sequences in PTY stream (zero new IPC, reuses Channel<PtyEvent> with BudgetUpdate variant); D-02 OSC 1337 with `voss-budget=` prefix (extensible namespace); D-03 cumulative totals payload (idempotent, no frontend accumulation); D-04 emit after each LLM response at `end_iteration()`. **HUD placement:** D-05 per-pane header bar (22px PaneHeader, agent panes only); D-06 right-aligned cost + mini progress bar before `⋯` menu; D-07 cost-only when no budget limit; D-08 3-tier color thresholds (accent/warning/error at 70%/90%). **Metrics:** D-09 cost + bar always-visible, detail in click popover; D-10 reuses A10 Popover component; D-11 no ADE-side persistence (self-heals from cumulative OSC). **Cadence:** D-12 per-pane local Solid signal (no global store); D-13 150ms CSS transition on bar width; D-14 no debounce needed. Resume: `/gsd:plan-phase F3`.
- 2026-05-21 — **Phase F2 (Hybrid Semantic Search) planned — ready to execute.** Created F2-RESEARCH.md, F2-PATTERNS.md, F2-VALIDATION.md, and 3 execution plans / 3 waves: **W1 F2-01** symbol-aware BM25 tokenizer, corpus builder, and BM25-only recall replacing `_keyword_scan()`; **W2 F2-02** hybrid BM25 + Chroma recall via Reciprocal Rank Fusion with Chroma-failure degradation; **W3 F2-03** `rank-bm25` base dependency placement, lock validation, and targeted recall regression closeout. Local GSD agents and `check.decision-coverage-plan` router were unavailable, so planning gates were validated inline against FSRCH-01..04 and D-01..D-08.
- 2026-05-20 — **Phase A10 (voss-app Status Bar) context gathered.** `/gsd-discuss-phase A10`: all 3 gray areas discussed. A10-CONTEXT.md captures D-01..D-11. **Popovers:** D-01 click-outside + Esc dismissal (one popover at a time); D-02 left popover = recent projects list (reuses A5 recents); D-03 center popover = pane detail card (read-only: cwd/shell/PID/index/command); D-04 one generic `<Popover>` component reused by all clusters. **Notifications:** D-05 unified event stream (every event → both toast + notification store); D-06 opening bell popover clears badge; D-07 global `notifications.json` (~/.config/voss-app/, last 50, written on quit); D-08 L1.8.2 event sources as-is (5 sources, no additions). **Git:** D-09 branch name only (no dirty/ahead-behind in L1); D-10 Rust file watcher on `.git/HEAD` (near-instant, 500ms requirement); D-11 hide branch entirely when no git repo. Resume: `/gsd:plan-phase A10`.
- 2026-05-20 — **Phase A9 (voss-app Settings + Theme) context gathered.** `/gsd-discuss-phase A9`: all 4 gray areas discussed. A9-CONTEXT.md captures D-01..D-16. **Panel hosting:** D-01 full-screen overlay (same z-layer as palette, Esc dismisses); D-02 ⌘, shortcut (+ palette + A10 cog); D-03 fixed sidebar nav (~160px, 7 categories); D-04 search filters to matches. **Merge & override:** D-05 inline badge per row for workspace overrides; D-06 most settings workspace-overridable (Appearance/Terminal/Layout yes, Keybindings/Updates/Telemetry global-only); D-07 shallow merge by key; D-08 runtime Rust validation (serde typed structs). **Hot-reload:** D-09 visual settings (theme/font/opacity) immediate all panes; D-10 shell = new panes only; D-11 scrollback = new panes only; D-12 no ask-before flow needed. **Edit-as-JSON & telemetry:** D-13 OS default editor for Edit as JSON; D-14 telemetry = consent toggles only (no send infra); D-15 inline descriptions for consent; D-16 updates section = placeholder. Resume: `/gsd:plan-phase A9`.
- 2026-05-20 — **Phase A8 (voss-app Workspaces, UX Polish, & Theming) context gathered.** `/gsd-discuss-phase A8`: all 4 gray areas discussed. A8-CONTEXT.md captures D-01..D-14. **Workspaces:** D-01 all workspaces stay mounted (CSS display toggle, PTYs keep running — Warp model); D-02 state structure = planner discretion; D-03 Warp-style fixed color dot palette (~8 colors, auto from name hash); D-04 workspaces.json index + per-workspace session files. **Themes:** D-05 12 bundled curated themes (no VSCode import engine); D-06 static JSON in repo with CSS var mappings + ANSI colors, reuses applyThemeOverrides(); D-07 custom themes via .voss/themes/<name>.json same schema. **Platform:** D-08 native vibrancy (macOS NSVisualEffectView, Windows acrylic/mica, Linux CSS fallback); D-09 full platform polish all three; D-10 OS-native window chrome defaults. **A11y/Animation:** D-11 CSS transitions only (150ms split, 200ms reflow, prefers-reduced-motion kill switch); D-12 high-contrast = token override layer on active theme (WCAG AAA); D-13 font dropdown + live preview + JetBrains Mono fallback; D-14 profiles = full settings snapshot. Resume: `/gsd:plan-phase A8`.
- 2026-05-20 — **Phase A7 (voss-app Cmd Palette + Keymap) context gathered.** `/gsd-discuss-phase A7`: all 4 gray areas discussed. A7-CONTEXT.md captures D-01..D-16. **Registry:** D-01 central typed CommandRegistry (Map<string, Command> with id/label/category/keybinding/handler); D-02 registry dispatch replaces A3 keymap.ts (single dispatch path, all A3/A4 chords migrated); D-03 single AppContext object built at App.tsx mount; D-04 auto-generate Tauri native menus from registry. **Palette:** D-05 ⌘P shows saved layouts + recent projects (extensible for L4 files); D-06 one CommandPalette component with quick/full modes; D-07 simple substring + recency boost fuzzy; D-08 centered overlay, Esc/click-outside dismiss; D-09 right-aligned chord hints in rows. **tmux prefix:** D-10 timed 1.5s ⌘B prefix window with [⌘B…] pane header indicator; D-11 named profiles (vscode default, tmux inherits + adds prefix); D-12 profile in ~/.config/voss-app/settings.json. **Keymap override:** D-13 additive merge + unbind via null (VSCode semantics); D-14 hot-reload via Rust file watcher; D-15 per-entry validation, partial apply, per-error toast; D-16 A7 ships minimal toast component (first consumer). Resume: `/gsd:plan-phase A7`.
- 2026-05-20 — **Phase M15 (Skill / Plugin Marketplace, CAPS-01f) COMPLETE — 6/6 plans, 5 waves, SKILL-01..06 all GREEN.** W0 M15-01 RED suite + signed fixture bundle + cryptography dep (human gate). W1 M15-02 trust.py Ed25519 verify + portalocker pinned-key store [SKILL-03] ∥ M15-03 scope.py declared-scopes→existing PermissionGate [SKILL-04]. W2 M15-04 fetch.py (HTTPS-only git/shorthand/local-first) + PluginManifest schema extend + install/remove/update staging→verify→copy [SKILL-01, SKILL-05]. W3 M15-05 VossSkillAdapter (subprocess + scoped gate, no in-process exec) + load_voss_skills + public compile_voss_file + voss skill add/list/remove/update/trust CLI + RunRecorder audit events [SKILL-02]. W4 M15-06 e2e trust→add→list→run→update→remove cycle + committed-sig guard + gate-only confinement documented in README/skill-list/doctor + scope-fence audit clean (no central registry/OS sandbox/M9 TUI/GPG/parallel system) [SKILL-06]. 17 tests GREEN (15 harness + 2 e2e), 0 regression on extensions/recorder. Phase closeout: VALIDATION sign-off, ROADMAP plans [x], STATE updated.
- 2026-05-20 — **Phase A6 (voss-app Session Persist) context gathered.** `/gsd-discuss-phase A6`: all 4 gray areas discussed. A6-CONTEXT.md captures D-01..D-12. **Scrollback:** D-01 on-quit extraction only (no periodic snapshots); D-02 plain text (strip ANSI); D-03 always `buffer.normal` (never alternate screen). **Save trigger:** D-04 two-tier — structural-change-debounced tree auto-save (~2s, reuses `markStructuralChange`) + blocking quit save for scrollback; D-05 Tauri `close-requested` blocks close, extracts scrollback, writes `session.json` with portalocker flock, then allows quit; D-06 single `session.json` file (scrollback null during auto-save, populated on quit). **Restore banner:** D-07 new `RestoreBanner` component (sibling to ExitBanner); D-08 copy "Session restored — N lines"; D-09 auto-dismiss on first keystroke. **Session vs layout:** D-10 session.json wins over default.json (layout is fallback for first-open only); D-11 corrupt session → fall through to default.json → fresh pane (A4 D-09 fail-safe pattern); D-12 `projectLessAccepted` persisted in `global-session.json` (resolves A5 D-04 deferral — setup window only on true first launch). Resume: `/gsd:plan-phase A6`.
- 2026-05-20 — **Phase O6 (Audit Product + Calibration + Liveness Hardening) planned — ready after O1-O5 substrate preflight.** Created O6-RESEARCH.md, O6-VALIDATION.md, O6-PATTERNS.md, and 6 execution plans / 5 waves: **W0 O6-01** blocking O1-O5 audit-interface preflight + deterministic audit fixtures; **W1 O6-02** read-only `AuditSnapshot` model/loader with session-schema isolation; **W2 O6-03** Markdown+JSON human review export; **W2 O6-04** reviewer calibration + liveness metrics; **W3 O6-05** sign-off forcing function with digest-bound acknowledgement buckets; **W4 O6-06** Leak-6 mitigate-or-accepted-gap closure + end-to-end acceptance. Because `O6-SPEC.md` is absent and local GSD agents are not installed, OAUD-01..08 were derived inline from ROADMAP/O6-CONTEXT/ORCHESTRATION-PLAN and should be reconciled if a formal SPEC is later authored.
- 2026-05-19 — **A3-06 (app integration + mirror parity + perf sign-off) complete — Tasks 1+2 autonomous, Task 3 human-verified `approved` → Phase A3 CLOSED (6/6, GRD-01..08).** App.tsx mounts <GridRoot/> below the A1 titlebar (PaneComponent region swapped, Titlebar untouched); voss-app-core grid.rs +Default for GridState + plain overwrite/snapshot helpers + #[tauri::command] get_grid; apps/voss-app/src-tauri/src/lib.rs adds thin app-level sync_grid/get_grid #[tauri::command] wrappers delegating to grid::overwrite/snapshot + .manage(Mutex<GridState>) + handler registration (same cross-crate generate_handler! pattern A2-05 used for PTY); mirror-parity.test.ts (scripted split-H/V/fork/focus/keyboard-resize/close + get_grid round-trip parity-clean, no .voss/fs path in any IPC payload, GRD-08); grid-integration.spec.ts + grid-perf.spec.ts (Linux-CI-deferred test.skip per memory voss-app-tauri-e2e-macos-blocked; assertion intent retained verbatim, all required scenario tokens present). Gates: vitest src/grid **59/59**, cargo test grid 2/2, cargo build voss-app-core + voss-app (src-tauri) clean, tsc 0, INTEGRATION_OK + SPECS_PRESENT. **MID-GATE WARP CORRECTION (in-flight in this plan, memory voss-app-grid-warp-parity):** user reported live on first dev-machine test (a) ⌘D bound to fork not split, ⌘⇧D unbound; (b) "only 4 panes max" because the focused pane geometrically halved on each split and the 5th hit the 20×5 floor. Fixed: tree.ts +leafCount +balanceRatios (ratio = leaves(left)/leaves(node) ⇒ every leaf equal-area); operations.ts split/fork/close + equalizeAll call balanceRatios; resize.ts equalizeAllRatios → balance; geometry.ts simulateSplitViolates balances the post-swap clone so the floor pre-flight reflects the auto-equalized geometry (5th+ splits fit until the real 20-col floor); keymap.ts ⌘D = split right (H), ⌘⇧D = split below (V) per Warp parity, ⌘\/⌘⇧\ kept as aliases, forkFocused unbound; matching test updates (balanced ratios + ⌘D split semantics + a new operations "5 splits → 6 equal columns" test + tree balanceRatios test). **HUMAN SIGN-OFF (Task 3):** `approved` — 9-pane Canvas-per-pane perf bar HOLDS on the dev machine (idle ~60fps + one-pane yes-flood isolation); WebGL stays UN-adopted; no .voss/ created. **Carry-forward (A3-06-followup / A8 — A2-touching, NOT regressions):** (i) duplicate header (A2 PaneComponent renders its own internal header beneath the A3 Variant B one — reconcile via hide/surface; A2-touching); (ii) real A2 fg gate wiring so ⌘W cross-pane confirm banner triggers on real foreground processes (closeUI defaults isFg false; ⋯-menu Close fully wired+tested with injectable detector); (iii) live xterm cell metrics (DEFAULT_CW=8/CH=20 placeholder → A2 source); (iv) Linux-CI e2e un-skip on the unchanged contract. Resume: `/gsd:execute-phase A4` (Layout Presets) — A3 substrate now in place.
- 2026-05-19 — **Phase A4 (voss-app Layout Presets) planned — ready after A3-06.** Created A4-PATTERNS.md plus 6 plans / 5 waves: **W0 A4-00** blocking A3-06 substrate preflight (GridRoot mounted, sync_grid/get_grid registered, A3-06 summary present); **W1 A4-01** pure preset transform model/tests; **W2 A4-02** controlled titlebar switcher + `custom` state + `Cmd+G` injection; **W2 A4-03** Rust versioned layout persistence commands; **W3 A4-04** frontend save/load wrappers + non-destructive loaded-layout remap; **W4 A4-05** LAY-01..08 acceptance, e2e smoke, full verification, and manual Variant B visual sign-off. Graphify query was attempted but no `graphify-out/graph.json` exists, so A4-PATTERNS.md is source-inspected. Resume: execute A3-06 first, then `/gsd:execute-phase A4`.
- 2026-05-19 — **Phase M15 (Skill / Plugin Marketplace, CAPS-01f) planned — ready to execute.** 6 plans / 5 waves, safety-critical ordering honored (RESEARCH+CONTEXT hard-prereq): **W0 M15-01** RED skill suite (12 tests across tests/harness/skill/ + tests/e2e/test_skill_lifecycle.py mapping the M15-VALIDATION Requirement→Test rows) + `cryptography>=43.0.3` promoted transitive→direct dep (RESEARCH 'already a dep' was transitive-only — corrected) + real Ed25519-signed `examples/skills/voss-git-summary/` bundle + author-side signer + **blocking human gate** (committed fixture-key + dep decision, T-M15-SC); **W1 M15-02** `voss/harness/trust.py` Ed25519 detached-sig verify + 0600 portalocker pinned-key trust store [SKILL-03] ∥ **M15-03** `voss/harness/skill/scope.py` declared-scopes→EXISTING PermissionGate min-mode binding, no 2nd engine [SKILL-04] (file-disjoint, parallel); **W2 M15-04** fetch.py (HTTPS-only git/shorthand/local, local-first) + PluginManifest schema extend (backward-compat) + install/remove/update staging→verify→copy [SKILL-01, SKILL-05]; **W3 M15-05** VossSkillAdapter (subprocess via existing `voss run` path, no new interpreter, scoped gate) + load_voss_skills + voss skill add/list/remove/update/trust CLI + public compile_voss_file (RESEARCH OQ1) + RunRecorder audit [SKILL-02] — FIRST third-party exec, correctly after W1/W2; **W4 M15-06** e2e fixture trust→add→list→run→update→remove CI cycle + documented gate-only-confinement limitation (README+`voss skill list`+`voss doctor`, RESEARCH Pitfall 5) + phase scope-fence audit [SKILL-06]. Multi-source coverage audit: GOAL/REQ(6/6)/RESEARCH/CONTEXT all COVERED, zero unplanned items, deferred ideas correctly absent. Every task has `<read_first>`+`<acceptance_criteria>`+concrete `<action>` (no fenced code in actions); every plan has a substantive `<threat_model>` (signature forgery/scope-escape/supply-chain/trust-store-tamper/path-traversal/documented-subprocess-limit). `gsd-sdk` frontmatter.validate/commit verbs absent this build — req coverage + frontmatter + structure validated manually per T6/M13/M14 precedent; committed 4902302, STATE+ROADMAP updated manually. Resume: `/gsd:execute-phase M15`.
- 2026-05-19 — **Phase A4 (voss-app Layout Presets) context gathered.** `/gsd-discuss-phase A4`: all 4 gray areas discussed. A4-CONTEXT.md captures D-01..D-09 + Claude/planner discretion. **Mapping:** D-01 preset slots fill by A3 stable pane-index order (reuse `recomputeIndices()`), pane #1 → primary slot (fanout source / watchers main / swarm top-left / pipeline leftmost); D-02 focus follows the same PTY pane post-remap (content-follows-focus). **Capacity (LAY-05):** D-03 grow the preset's flexible region; D-04 over-cap (swarm >4×4 / watchers max) spills by binary-splitting the last region cell (never destroy — A3 D-04), under-count lays only existing panes, no filler spawn. **⌘G:** D-05 fixed cycle fanout→pipeline→swarm→watchers→wrap, custom-state snaps to fanout, switcher shows 'custom' until pick; D-06 geometry recomputed fresh per entry (stateless), tweaks transient, "Save layout" = the persistence path. **Save/Load:** D-07 saved `.voss/layouts/<name>.json` = tree+ratios+preset+per-pane cwd+shell (NOT scrollback/procs → A6); D-08 Load remaps open panes onto saved geometry (saved cwd/shell only for net-new slots, never kill running); D-09 default.json auto-applies on project open, integer schema version, best-effort migrate, fail→ignore+log, `.voss/` lazy on first write. No SPEC for A4 (LAY-01..08 sourced from ROADMAP + CONCEPT §10 Q4 + FEATURES §L1.3.5/6). No deferred ideas (scope clean). `gsd-sdk` commit/state verbs absent this build — committed (99892e9) + STATE updated manually per T6/A3/O1 precedent. Resume: `/gsd:plan-phase A4`.
- 2026-05-19 — **A3-05 (per-pane Variant B chrome) complete — autonomous, Wave 4.** PaneHeader.tsx (22px ● index cwd shell process ⋯, focus bg-bg-2/unfocus bg-bg-1, aria-label Pane menu/Shell running, process ''→hidden, 10px index tier) + DotMenu.tsx (128px bg-bg-3, EXACTLY 5: Fork pane/Split right/Split below/sep/Close pane, ⌘D/⌘\/⌘⇧\/⌘W kbd, dismiss on select/Esc/outside-click, → forkFocused/splitFocused/gated-close) + CloseConfirmBanner.tsx (22px char-exact `"<proc>" is running. Close anyway?` / Keep open / Close anyway, Enter→closeFocused Esc→keep, non-modal, parent auto-dismiss) + **requestCloseGated single close entry T-A3-13** (idle→closeFocused now / fg→showBanner; isForegroundRunning INJECTED via CloseUI.isFg, A3 never reimplements A2 D-07). SplitNode leaf seam filled (PaneHeader+conditional DotMenu/CloseConfirmBanner, per-leaf menuOpen/banner signals, close commit inside setStore(produce)); GridRoot onCloseRequest default swapped closeFocused→requestCloseGated gate (+closeUI prop/threading, dropped now-orphan closeFocused import). vitest PaneChrome 7/7, full src/grid **56/56** (A3-01..04 zero regression), tsc 0, HEADER_OK+MENU_OK+SEAM_OK (corrected GRD-07 gate), no src/pane/ file modified. GRD-02(close-confirm half)+GRD-06+GRD-07(header half). **DEVIATION (A2 reality, same class as A3-04):** A2 PaneComponent renders its OWN internal header + keeps dot/proc/fg private (surfaces nothing) → A3 PaneHeader is the canonical Variant B header in the leaf seam; cwd/shell/index from leaf (real), dotState/process default until A2 surfaces them. CONSEQUENCE: live app shows A3 header + A2 internal header (duplicate) — reconcile = A2-touching, OUT OF A3-05 scope → A3-06/A8. **DEVIATION:** requestCloseGated isForegroundRunning narrowed Promise|boolean→sync boolean (awaited detector ⇒ stale produce draft; realistic A2 source is cached fg signal). **SCOPE:** ⋯-menu Close pane fully wired+tested to local banner; ⌘W routes through requestCloseGated isFg-default-false ⇒ closes immediately (UNCHANGED from A3-04, zero regression); ⌘W cross-pane banner deferred A3-06 (needs real A2 fg). **PLAN-DEFECT (carried A3-04):** HEADER verify `ring`∈`string` false-positive — true GRD-07 verified via corrected gate (PaneHeader carries no boundary stroke; inset shadow on SplitNode wrapper). **Test-lib note:** plan said @solidjs/testing-library; used established solid-js/web + @testing-library/dom (A3-04 precedent, zero new deps, T-A3-SC). **A3-06 carry-forward:** mount <GridRoot/> in App.tsx (A1-owned, NOT edited) + sync_grid app-level (A3-01 carry, same as A2-05 PTY) + real xterm cell metrics (DEFAULT_CW/CH TODO) + 9-pane perf blocking human-verify + reconcile duplicate header & wire real A2 fg gate (closeUI={{isFg,fgName}} → A2 get_fg_process/fg_process). Resume: A3-06 (FINAL — app integration + sync seam + perf gate).
- 2026-05-19 — **A3-04 (recursive renderer + keymap + focus paint) complete — autonomous, Wave 3.** keymap.ts (dispatchKey: ⌘\ /⌘⇧\ split H/V via code Backslash+shift, ⌘D fork, ⌘W→injected onCloseRequest [A3-05 gate seam], ⌘= equalize→operations.equalizeAll ONLY [no double-bind], ⌘1-9 focus, ⌘[/] cycle, ⌘⌥arrow focusByDirection, ⌘⌥⇧arrow resizeByKeyboard; matched=preventDefault+true, unmatched/non-⌘=false no-preventDefault → PTY pass-through T-A3-09) + SplitNode.tsx (recursive Switch H=flex row/V=flex col, leaf wraps A2 PaneComponent, inset-shadow focus on wrapper, click→focusByClick) + DragHandle.tsx (6px transparent pointer-capture overlay → resizeByDrag per move, markDragSettled once on pointer-up, hover→border-bright) + GridRoot.tsx (createGridStore, window keydown→setStore(produce(dispatchKey)), minGridSize GRD-05 shrink clamp tile@max(window,floor) inside overflow-hidden, onCloseRequest default closeFocused). Solid reactivity seam: pure mutators run inside setStore(produce(s=>fn(s))) — render-layer concern per A3-02 SUMMARY. vitest keymap 5/5 + GridRoot 6/6, full src/grid **49/49** (no A3-01/02/03 regression), tsc 0, KEYMAP_OK + corrected GRD-07 gate. GRD-01+03+04+07. **DEFECT FIX (surgical, cross-plan):** geometry.ts simulateSplitViolates structuredClone(root)→pure cloneTree — render-layer root is a Solid produce-draft Proxy, structuredClone throws DATA_CLONE_ERR (A3-02 pure path never hit it); no API change, 12/12 A3-02 still green. **DEVIATION (not defect):** A2 PaneProps has no focused/size prop (locked black box) → focus paint+sizing on SplitNode wrapper, A2 ResizeObserver drives xterm fit. **PLAN-DEFECT logged:** Task2 verify `! grep ...'ring'...SplitNode.tsx` false-positives on substring in `string` (every typed tsx) — true GRD-07 intent verified via corrected gate; recommend `\bring-|outline:`. **A3-05/06 carry-forward:** A3-05 fills the marked SplitNode leaf mount comment (PaneHeader/⋯/CloseConfirmBanner) + swaps onCloseRequest to A2-D-07 foreground-gated; A3-06 mounts <GridRoot/> in App.tsx (A1-owned, NOT edited here) + wires sync_grid app-level (same cross-crate generate_handler! pattern as A2-05 PTY — invoke('sync_grid') rejects at runtime until then, mocked green) + real xterm cell metrics (DEFAULT_CW=8/CH=20 TODO) + 9-pane perf human-verify. Resume: A3-05 (Variant B header + ⋯ menu + close-confirm banner).
- 2026-05-19 — **A3-03 (focus + resize) complete — autonomous, Wave 2 (parallel-safe with A3-02, zero file overlap).** focus.ts (focusByIndex .index match / focusByClick direct+unknown-id-noop / cycleFocus inorder wrap both ends / focusByDirection i3 edge-midpoint D-03: local rectsOf, strict-side + perpendicular-overlap adjacency, min axis-gap then min clamped-midpoint perp-dist, no focus history) + resize.ts (resizeByDrag path-addressed split + snap-toward-0.5 floor clamp, mutates only target ratio; resizeByKeyboard ±5% nearest-axis-bounding split, per-step sync, clamp = no-op no-overshoot; equalizeAllRatios; **splitPath exported — splits have NO id, addressed by "L"/"R" path string; A3-01 wire shape unchanged → GRD-08 round-trip intact**). Neither imports geometry.ts (A3-02-owned same wave; private rectsOf each, verify `! grep` gate). vitest focus+resize 15/15, full src/grid 38/38 (tree 11+ops 12+focus 7+resize 8, A3-01/02 no regression), tsc 0, FOCUS_RESIZE_OK. GRD-03+GRD-04+GRD-05(resize half). **A3-04 carry-forward:** DragHandle MUST use splitPath/pass same path to resizeByDrag; pointer-up calls markDragSettled once (resizeByDrag never syncs); bind ⌘= to exactly ONE of operations.equalizeAll|resize.equalizeAllRatios (prefer operations); focusByDirection sig is (store,dir,winW,winH). Backlog 999.2 (⌘+/- focused-pane resize) folds into A3-04 keymap. Resume: A3-04 (recursive renderer + keymap + focus paint).
- 2026-05-19 — **A3-02 (tree mutations + 20×5 floor) complete — autonomous, Wave 2.** geometry.ts (computePaneRects/paneColsRows/wouldViolateFloor/simulateSplitViolates — pure, GRD-05 20×5 hard floor, 1px split border, 22px header-in-pane) + operations.ts (splitFocused H/V 50-50 / forkFocused cwd+shell+fresh-id / closeFocused D-04 sibling-expand+focus + last-pane respawn / equalizeAll; optional geom param runs floor pre-flight, violation = silent no-op no-sync). vitest operations 12/12 (4 geometry + 8 ops), tsc 0, greps+purity. GRD-02+GRD-05. Pure GridStore mutators (render layer wraps in setStore/produce — A3-04/06). Resume: A3-03 (focus.ts numeric/directional/i3/cycle).
- 2026-05-19 — **A3-01 (grid-model contracts) complete — autonomous, Wave 1.** tree.ts (binary-split SplitNode/PaneLeaf/TreeNode/GridStore + createGridStore Solid store + recomputeIndices geometric inorder no-gaps + equalizeRatios + makeSplit ratio-0.5 + findLeaf/collectLeaves), sync.ts (syncGridToRust + markStructuralChange immediate + markDragMove/markDragSettled drag-coalescer), grid.rs (Rust mirror + #[tauri::command] sync_grid, Mutex in-memory, zero disk I/O, serde rename_all=camelCase round-tripping tree.ts keys; Orientation unrenamed → literal "H"/"V"), lib.rs +pub mod grid. Gates green: vitest 11/11 (8 tree + 3 sync), cargo test grid 2/2, cargo build 0, tsc 0, no-disk gate, all contract greps. GRD-01 + GRD-08(structural). **Carry-forward (not a defect — contracts-only plan):** sync_grid needs app-level wrapper + .manage(Mutex<GridState>) in apps/voss-app/src-tauri/src/lib.rs (same cross-crate generate_handler! constraint as A2 PTY) — the A3-06 integration wave MUST wire it like A2-05 wired PTY (logged in patterns-established). Resume: execute A3-02 (operations.ts split/fork/close/equalize + 20×5 floor guard). Backlog 999.2 (⌘+/- focused-pane resize) folds into A3-04 resize keymap.
- 2026-05-19 — **A2-05 complete → Phase A2 (voss-app PTY Pane) COMPLETE (5/5).** D-02 flood-perf finalized: real Linux-CI assertion (P95_MAX_MS=33/ECHO_MAX_MS=200/process.exit(1), no mask) + macOS DEFERRED branch (mechanism unit-proven A2-03 pty-ipc 3/3); flood-perf.spec.ts Playwright driver skip-deferred; env-guarded window.__vossPerf rAF hook (T-A2-12). **2 in-flight integration plan-defects fixed (both required):** (1) no A2 plan mounted PaneComponent → mounted `<PaneComponent index={1}/>` in App.tsx; (2) no A2 plan wired voss-app-core into apps/voss-app/src-tauri/src/lib.rs → spawn_pty rejected (black terminal); fixed via app-level #[tauri::command] wrappers delegating to voss-app-core public pty API + managed Arc<PtyRegistry> (cross-crate generate_handler! can't resolve another crate's command macros via pub use; keeps bare-invoke contract, avoids plugin-permission path). PTY-08 user-approved via **Claude Code running interactively inside the pane** (maximal alt-screen TUI: status bar/signals/OSC title `claude.exe`) — stronger than vim/htop matrix (recorded as recommended later spot-check, non-blocking). No regression: vitest 5/5, cargo 4/4, tsc 0, app builds clean. Feature captured (not built, out of scope): navbar "Agents" button + Claude/Codex/Gemini/OpenCode launch prefixes → memory voss-agents-launcher-feature, candidate future A-track phase post-A3. Phase A2 ready for /gsd:verify-work. Resume: A3 (grid engine — Warp-style locked tiling per memory voss-app-grid-warp-parity).
- 2026-05-19 — **A2-04 (pane interaction layer) complete — autonomous + 1 user decision gate.** Built PasteGuard.tsx (multi-line paste banner, copy-exact "Discard"), ExitBanner.tsx ([exited N] + 3-tier dot + Restart), FindBar.tsx (⌘F overlay, Find… placeholder); wired 6 interaction layers into PaneComponent (capture-phase paste→guard, customKeyEventHandler ⌘⇧V-bypass/⌘C-smart-copy-or-SIGINT-0x03/⌘F/⌘⇧K-clear, OSC8+file-path linkHandler w/ {http,https,mailto,file} allowlist, OSC0/2-title + 2s-guarded pgid fallback, exit→banner→scrollback-preserving restart), added PtyTransport.fgProcess(). **E2E decision gate:** Tauri WebDriver impossible on macOS (no WKWebView WebDriver; tauri-driver=Linux/Win only) → A2-VALIDATION's 7 Playwright specs unsatisfiable here; user chose "code + unit/tsc green, e2e deferred" → specs converted to test.skip-with-reason, deferred to Linux CI (memory voss-app-tauri-e2e-macos-blocked). Gate green: vitest 5/5 (A2-01 PasteGuard reds resolved), cargo 4/4, tsc 0, all contract greps. Strict-narrowing fix (copyMode `as CopyMode`). Documented backend gap: open_url/open_path Rust handlers unimplemented (out of plan scope, A2-05/A10). Resume: execute A2-05 (flood-perf + ExitBanner offset + mount pane into App.tsx + manual visual gate).

- 2026-05-19 — **M13 W0→W1→W2A executed (M13-01/02/03), stopped after M13-03 per user scope.** M13-01 (W0): 5 RED scaffold test files + `scripted_multiagent_provider` conftest fixture + additive keymap-baseline rows + back-compat tripwire; additive-only gates held, 2 green guards pass. M13-02 (W1): NEW `voss/harness/multiagent.py` — `M13Allocator` (asyncio.Lock check-and-allocate, exactly-once `release` via `_credited_finished`, viable-floor bound, NO depth constant) + `ChildHandle` + `ChildRegistry` + `new_handle_id`; OQ-A1 resolved in-module: `DEFAULT_PARENT_RESERVE=30_000` / `DEFAULT_VIABLE_FLOOR=2_000` (agent.py:419 `token_budget=60_000` cited); subagents.py byte-stable, no O1 import. M13-03 (W2A): non-blocking `subagent_spawn/steer/status/gather` + `PanelBridgeRenderer` + `attach_multiagent_tools` (returns `_teardown_orphans`) + additive `ChildHandle` extension (queue/panel_id/sub_allocator); agent.py additive keyword-only `steer_inbox` kwarg + D-04 synthetic-message steer-drain at **agent.py:862-868** (non-terminating branch, between L830 `all_iter_records.append` and L870 budget check; OQ-A2 — history_block built once L513-519 so synthetic message is the only correct route). **Cross-plan defect found + resolved:** M13-01's scaffold *bodies* ad-libbed a fictional API (`ChildRegistry.register/release/steer`, `MultiAgentOrchestrator.run_child`) the M13-01 PLAN never specified — irreconcilable with M13-02's pinned `ChildRegistry.add/get/active/all` + M13-03's tool surface; `xfail(strict=False)` would have masked it as permanent-xfail (MAG-01/MAG-05 false-green). User chose "fix the scaffold" → `TestConcurrentInFlight`+`TestCorrectionChangesBehavior`+`test_steer_to_finished_child_is_noop` bodies rewritten to drive the real architecture with MAG-01 (≥2 in-flight + overlapping windows) / MAG-05 (correction-vs-control divergence) signal bars preserved verbatim (strengthened with explicit value asserts). Also reconciled stale-plan-vs-real-API: `M13Allocator(*,reserve=)` only (VIABLE_FLOOR class attr), sync `release()` + async `rebalance()`, `ChildHandle.id`. Independently verified: 9 XPASS (concurrency/even-split/no-oversell/correction/depth-budget all xfail→XPASS), TestOrphanTeardown hard-PASS (T-M13-02 executable gate), test_subagent_recursion + test_agent_loop green unmodified, byte-stable set {subagents,cli,renderer,app,keymap,conftest,test_keymap_baseline,test_subagent_recursion,test_agent_loop} all clean → M13-03∥M13-04 disjointness intact. 1 remaining xfail = depth-2 nested-panel (needs M13-05 recursion, out of scope). Plan files were already at HEAD (background auto-commit). Remaining: M13-04 (W2B TUI bridge+reveal) ∥-ready, M13-05 (W3 recursion), M13-06 (W4 cli wiring + headline e2e). Resume: execute M13-04+M13-05 then M13-06.
- 2026-05-18 — **Phase M13 (Multi-agent in Chat, CAPS-01d) SPEC + CONTEXT + RESEARCH + VALIDATION + PATTERNS + plans landed — ready to execute.** `/gsd-spec-phase M13`: 4 Socratic rounds, ambiguity 0.17, 8 reqs MAG-01..MAG-08 locked. Phase reframed from ROADMAP's literal "expose runtime spawn/gather" to **harden the harness `subagent_run` path** (user-chosen Q1) — `voss_runtime` `VossAgent.spawn`/`gather` explicitly scope-fenced OUT; concurrent children + gather; autonomous-parent mid-run course-correction (child-input channel); recursive even-split reserve (depth>1); quiet panels + Ctrl+O reveal (Ctrl+C stays interrupt); recursive **no-oversell** = must-not-happen guard. CONTEXT.md written **direct from SPEC** (per `gsd-spec-then-context-direct` default — user asked "craft context from spec, skip discuss"; 2 architecture forks confirmed via AskUserQuestion: reveal-key→Ctrl+O, steer-cadence→between-iterations) — 11 HOW decisions D-01..D-11. RESEARCH (HIGH, gsd-phase-researcher): riskiest unknown resolved — `_run_turn_exec` already iterative so NO parent-loop surgery; steer-drain pinned `agent.py:830` non-terminating branch; allocator = O1 `asyncio.Lock` pattern lifted M13-local (NOT O1 import); Validation Architecture (8 MAG seams) + Security Domain (6 STRIDE). VALIDATION.md (Nyquist Dim-8) derived. PATTERNS.md (13 files, 13/13 in-repo analogs, 0 greenfield; steer anchor corrected 832→830). gsd-planner **chunked** (opus single-shot timeout precedent T6/M11): outline→6 per-plan runs / 5 waves (W0 M13-01 red scaffolds+scripted-provider fixture; W1 M13-02 allocator+ChildRegistry; W2 M13-03 fan-out tools+steer-drain ∥ M13-04 TUI bridge+reveal, file-disjoint; W3 M13-05 recursion; W4 M13-06 cli wiring+headline e2e). 3 RESEARCH Open Qs each pinned in-task (A1 reserve/floor→M13-02; A2 steer-injection synthetic-message→M13-03; A3 "main"-context keymap dispatch→M13-04). gsd-plan-checker iter 1 ISSUES (1 doc-state blocker: RESEARCH `## Open Questions` not `(RESOLVED)` — fixed directly per O1 precedent, not a plan defect; 4 warnings: keymap 16→18 count, test_subagent_recursion overclaim M13-02/05, orphan-teardown T-M13-02 untested → added M13-03 `TestOrphanTeardown` executable gate + M13-06 cli teardown-hook wiring, cli.py line drift) all fixed; **iter 2/3 VERIFICATION PASSED clean** (full 12-dim battery, no regression, M13-03∥M13-04 disjointness preserved). gsd-sdk plan-phase/state verbs absent this build — req coverage 8/8 + ROADMAP req-rows locked manually, STATE updated manually per T6/A3/O1 precedent. Commits: 17d92d7 SPEC, 408f191 CONTEXT, bcd5989 RESEARCH+VALIDATION, PATTERNS, 922c9e7 M13-02+ROADMAP-block, plans M13-01/03/04/05/06+ROADMAP-rows, plan-check-iter1 fix. Resume: `/gsd:execute-phase M13`.
- 2026-05-18 — **Phase M14 (Long-running Tasks + Watch, CAPS-01e) SPEC + CONTEXT + RESEARCH + plans landed — ready to execute.** `/gsd-spec-phase M14`: 2 Socratic rounds, ambiguity 0.16, 5 reqs WATCH-01..05 locked (headless-only — M9 TUI strip + M10 code_refresh hookback BOTH scope-fenced; reuses shipped T5 job engine, layers `watchdog`). CONTEXT.md written **direct from SPEC** (new default workflow, saved to auto-memory `gsd-spec-then-context-direct`: skip full discuss-phase unless user explicitly asks) — 4 HOW decisions: D-01 200ms per-path Timer debounce, D-02 shared `_read_log_cursor` factored from `monitor_job` (not duplicated), D-03 hybrid daemon (`start_new_session` detached worker + `--_is-worker` re-entry guard, NOT registered for reap), D-04 sibling `_WATCHERS` registry. RESEARCH (HIGH, gsd-phase-researcher): watchdog 6.0.0 pin `>=4.0,<7`, `loop.call_soon_threadsafe` thread→asyncio bridge, exact T5 reuse seams w/ line numbers, Validation Architecture (10 tests) + Security Domain (6 STRIDE threats). VALIDATION.md (Nyquist Dim-8) derived. PATTERNS.md (9 files, 7 analogs, 2 greenfield flagged). gsd-planner (opus): 4 plans / 3 waves (W1 M14-01 scaffold+watchdog dep+CI matrix+blocking pkg-legitimacy checkpoint; W2 M14-02 lifecycle spine `_WATCHERS`+`_read_log_cursor`+`watch/backend.py`; W3 parallel M14-03 `fs_watch`/`fs_watch_poll` tools ∥ M14-04 `voss watch` CLI+`watch/daemon.py`, file-disjoint), 3 RESEARCH Open Questions resolved+locked. gsd-plan-checker iter 1: **VERIFICATION PASSED clean** (all 12 dims; 1 cosmetic Open-Questions-heading note fixed directly per O1 precedent, non-plan-defect). gsd-sdk plan-phase verbs (check.decision-coverage-plan / state.planned-phase) absent this build — req coverage 5/5 grep-verified, decision coverage satisfied via plan-checker Dim-7, STATE updated manually per T6/A3/O1 precedent. Commits: 9d8e360 SPEC, 5672889 CONTEXT, cf2f280 RESEARCH+VALIDATION, PATTERNS, 1f604b1 plans+ROADMAP-goal-correction. Resume: `/gsd:execute-phase M14`.
- 2026-05-18 — **A2-03 (Solid xterm pane + Channel transport) complete — autonomous.** `pty-ipc.ts` PtyTransport: Tauri Channel + D-02 frontend half (per-rAF coalescing + watermark backpressure HIGH=100k/LOW=10k, injected-write for testability) + spawn/write/resize/kill. `PaneComponent.tsx`: Solid pane, xterm v5.5.0 verbatim UI-SPEC theme, CanvasAddon AFTER term.open() (Pitfall 2, grep-verified order), Fit/Search/WebLinks, onData→pty_write, 150ms-debounced ResizeObserver→fit+pty_resize, DPR re-fit, 22px Variant B header, focus bg-lift, onCleanup kill+dispose. `pane.css` token-only/transition:none/radius:0. 3/3 pty-ipc unit tests green (coalescing/pause/resume); tsc --noEmit 0 errors. 2 in-flight fixes: vitest include glob .tsx→{ts,tsx} (A2-01 config-defect, transport is .ts), backpressure tests need spawn() for sessionId. PasteGuard RED (2 fails) is A2-01 scaffold owned by A2-04, not a regression. Resume: execute A2-04 (PasteGuard/copy/OSC8/search/clear/status-dot).
- 2026-05-18 — **A2-02 (Rust PTY backend) complete — autonomous.** Implemented full `voss-app-core` PTY engine: `PtySession` (Mutex-wrapped master/writer/slave-held/child — Send+Sync for Tauri state), `spawn_session()` Tauri-free core ($SHELL + TERM=xterm-256color + COLORTERM=truecolor + UUID v4), Arc-managed `PtyRegistry`, 7 Tauri commands (spawn/write/resize/pause/resume/kill/get_fg_process) + `PtyEvent` enum, spawn_blocking reader with watermark pause/resume + EOF→Exit+bounded-reap, `validate_write` guard (empty/>1MiB), platform foreground resolver (macOS tcgetpgrp→pids_by_type→name, Linux /proc/comm, Windows None stub). `cargo test -p voss-app-core` → 4 passed/0 failed (A2-01's 3 RED panics green + validation test); build 0 warnings. 4 in-flight fixes: Send+Sync Mutex-wrap; libproc 0.14 API-defect (plan said listpids/ProcPGRPOnly+pgid → actual pids_by_type/ByProgramGroup+pgrpid); test_foreground_pgid hang (unbounded child.wait on interactive zsh → bounded try_wait ≤2s) + race ("zsh" not "sleep" → exec-sleep + drain + poll-until-resolved). pty_write-location ambiguity resolved (all commands in commands.rs). Resume: execute A2-03 (frontend xterm pane + scrollback/clear/title).
- 2026-05-18 — **A2-01 (Wave 0 PTY foundation) complete.** Expanded `crates/voss-app-core` from A1 placeholder → compiling Tauri-plugin crate (pty submodule skeleton: mod.rs + commands/reader/writer/foreground doc-stubs + `PtyRegistry::default()` + `init()` plugin shell, NO PTY logic). Pinned PTY deps (portable-pty 0.9.0, nix 0.31 signal/term/process, libproc 0.14 macOS-gated, tauri 2 wry) + xterm v5 EXACT (@xterm/xterm 5.5.0 + addon-canvas/fit/search/web-links). RED suite stood up: 3 cargo-test panics, 2 Vitest fails, 7 Playwright e2e fails, 1 flood-perf exit-1 — every A2-VALIDATION Wave-0 command resolves to a real failing test. Task 0 human gate approved (D-01 v5.5.0 canvas pin + 8-pkg legitimacy). 2 plan-defects patched: pnpm.overrides relocated apps→root (pnpm ignores non-root workspace overrides; T-A2-01 needed it effective), A1/A2 crate-creation overlap reconciled (A1 made the placeholder, A2 expanded in place). User direction captured to memory: voss-app grid = Warp-style locked tiling (⌘D right / ⌘⇧D below / Win-key on Windows, auto-shrink siblings) — A3 scope. Resume: execute A2-02 (real PtySession + invoke_handler).
- 2026-05-18 — **Phase A1 (voss-app Tauri Shell) COMPLETE — 4 plans, 4 waves, all human-verified.** A1-01: `pnpm create tauri-app` solid-ts scaffold harvested into `apps/voss-app/`, hand-written configs for exact pins (tauri 2.11.2 / @tauri-apps/cli 2.11.2 / @tauri-apps/api 2.11.0 / solid-js 1.9.13 / vite 8.0.13 / tailwindcss 4.3.0 / tauri-plugin-os 2.3.2), root `pnpm-workspace.yaml` + `package.json`, `crates/voss-app-core` placeholder crate, root `Cargo.toml` surgical 2-member append; icon set regenerated then **repadded** to Big Sur safe-area (820/1024 content) after user flagged oversized Dock icon. A1-02: full Variant B token taxonomy in `variant-b.css`, Tailwind v4 `@theme inline` map in `index.css`, `get_theme_overrides` Rust command (home_dir().join(".config"), bare HashMap silent-fallback), `applyTheme.ts`, invoke-before-render seam; 3-path human verify (absent→#0a0b0e / valid→#1a1a2e / malformed→silent fallback + stderr line). **Plan-defect patched in flight:** App.tsx not in A1-02 files_modified but needed `background:#0a0b0e`→`var(--bg-0)` for the seam to be visible. A1-03: `WindowControls.tsx` (MacTrafficLights + StubControls + plugin-os platform gate), `PresetSwitcher.tsx` (visual-only, theme-driven, pipeline default), `Titlebar.tsx` (22px), App.tsx flex-column root; **2 in-flight visual fixes:** drag spacers needed `align-self:stretch` (collapsed to 0px under align-items:center), and `data-tauri-drag-region` added to the title-text div for macOS title-drag convention (count 2→3 — plan "exactly 2" guard over-strict, logged for plan-checker). A1-04: hardened `app.security.csp` (default-src 'self', no unsafe-eval, no remote origins, style 'unsafe-inline' kept for inline-style components + applyThemeOverrides), unsigned ad-hoc `APPLE_SIGNING_IDENTITY="-" pnpm tauri build` smoke (exit 0, .app+.dmg, launches with full Variant B chrome, About panel = "Voss ADE"/"0.1.0"). A10 code-signing cert-procurement clock acknowledged + started (Apple Developer Program + Windows Authenticode, 1-4wk CA lead). SHL-01..06 satisfied. Carried-forward: 11px Retina legibility re-check in A2; App.tsx + drag-region-count plan-defects for plan-checker; A10 cert clock started 2026-05-18. Resume: execute A2-01.
- 2026-05-18 — **Phase M11 (Voss-aware Tools, CAPS-01b) planned.** Created `M11-RESEARCH.md`, `M11-PATTERNS.md`, `M11-VALIDATION.md`, `M11-PLAN-OUTLINE.md`, and 5 executable plans across 5 serial waves: M11-01 recorded-data inspect core; M11-02 probable/budget CLI+slash+tool surfaces; M11-03 lint schema consumer; M11-04 `.voss` to Python diff; M11-05 TUI read-only modals + final no-emit guards. Research confirmed `/budget` is already the T6 USD slash, so M11 uses `/btrace`; Graphify query was attempted but no `graphify-out/graph.json` exists. Local GSD agents are not installed, so planning/checking was performed inline against the plan-phase contract. Resume: `/gsd:execute-phase M11`.
- 2026-05-18 — **Phase M11 (Voss-aware Tools, CAPS-01b) context gathered.** `/gsd-discuss-phase M11`: user declined pre-discuss SPEC ("Discuss now, no SPEC") then directed auto-resolve of all gray areas with recommended answers. M11-CONTEXT.md captures D-01..D-07: (D-01) no-new-emit ROADMAP constraint binding/not-escalated — `RunRecord.decisions` confirmed `{title,body,confidence}` only, so "propagation graph" downgrades to derived confidence-annotated decision *sequence* and "frame-by-frame budget" to per-agent-iteration timeline (explicit M10-style ROADMAP-wording divergence); (D-02) deliverable 1 = verify/expose only — T7 SKL-06 lint skill already shipped with frozen M11 JSON schema, no `.voss`-exec path (T7-rejected); (D-03) CLI-first all 4, reuse M9 `ConfidenceBar`/`BudgetMeter`/`BudgetModal`/`DiffModal` via modals, NO M9-08-style amendment; (D-04) inspector `<session>[--decision N]`, tracer `<session>` frame=iteration; (D-05) `.voss`→Py diff on-demand only, pair `*.voss`↔`.voss-cache/harness/*.py`, two-pane no source map, dogfood-capable; (D-06) all tools `is_mutating=False` + M1 redaction reuse; (D-07) candidate slashes `/probable /budget /vdiff`, with planner to confirm collisions. M11 reframed as wiring/exposure phase, not greenfield — dominant risk is over-building + a downstream SPEC re-asserting literal "propagation graph". No SPEC: VTOOL-01..05 recommended mapping written for planner. `gsd-sdk` commit/state verbs absent this build — committed (d0d6d3e) + STATE updated manually per T6/O1 precedent. Resume: `/gsd:plan-phase M11`.
- 2026-05-18 — **T8-05 reverse-search + paste-image complete.** Added Ctrl-R inline reverse-i-search over live `app.history`, Pillow dev dependency, clipboard-image probe with graceful ImportError/NotImplementedError/ChildProcessError/OSError handling, model-name vision gate, pending image indicator, no-vision local notice, and snapshot anchors 8-11. Focused T8 suite passed 53 tests / 11 snapshots; full non-e2e suite still fails on existing outside-T8 blockers (`platformdirs` isolated subprocess, runtime-surface baseline drift in recorder.py, npm shim expectation, live Anthropic 401, missing Ollama model). Resume: T8 completion audit.
- 2026-05-18 — **T8-04 TUI submit wiring complete.** Added `VossTUIApp.history`, `_turn_dispatch`, `on_input_bar_submitted`, and `on_local_event`; submitted input values now schedule turn tasks through existing `register_turn_task` cancellation plumbing. `_run_repl` has an added TextualRenderer branch that wires app state and runs `app.run_async`, leaving the non-TUI input loop as fallback. Verified app interrupt/full-flow/app-shell/prefix/InputBar selections, constructor/handler introspection, py_compile, and diff check. Resume: execute `T8-05`.
- 2026-05-18 — **T8-03 prefix dispatch complete.** Added LocalBlock shell/note renderers, local-block styles, `RecorderBridge.emit`, `append_voss_notes_bullet`, and `InputBar` submit branches for `!cmd` and `#note`. `!cmd` uses existing `sandbox.shell_allowed` + `split_command` before `asyncio.create_subprocess_exec`; denied commands render locally and do not exec. `#note` appends to the human `## Notes` section and preserves machine fences. Snapshot anchors 5-7 generated. Focused T8 tests passed; full TUI collection still hits the existing `tests.harness` import issue in cli/plain parity tests. Resume: execute `T8-04`.
- 2026-05-18 — **Phase A3 (voss-app Grid Engine) planned — ready to execute.** 6 plans / 5 waves (W1 A3-01 tree+store+Rust mirror+sync seam; W2 A3-02 split/fork/close/equalize+20×5 floor ∥ A3-03 focus+resize, zero file overlap; W3 A3-04 recursive renderer+keymap+focus paint; W4 A3-05 22px Variant B header+⋯ menu+close-confirm banner; W5 A3-06 app integration+e2e+**blocking human-verify** 9-pane Canvas perf/flood benchmark + mirror parity). `--skip-research` operator path: no RESEARCH.md/VALIDATION.md (Nyquist Dim-8 operator-waived "continue anyway", T6 precedent — D-01's 9-pane Canvas perf bar becomes A3-06 blocking checkpoint, WebGL NOT pre-adopted). A3-PATTERNS.md = greenfield (0 code analogs, contracts-as-patterns; A1/A2 referenced as upstream contracts per CONCEPT §9). gsd-plan-checker iteration 1: **VERIFICATION PASSED** (all blocking dims; D1 8/8 GRD covered, D7 D-01..04 honored verbatim, threat_model in all 6 plans); 1 non-blocking WARNING (A3-03 key_links frontmatter advertised forbidden geometry.ts import) fixed surgically (8630b4f, frontmatter-only). Commits: c2803ab plans+ROADMAP-goal-correction, 8630b4f A3-03 key_links, c88c6b9 PATTERNS, 68e2fb8 UI-SPEC. gsd-sdk plan-phase verbs (check.decision-coverage-plan/state.planned-phase/roadmap.annotate-dependencies) absent this build — gates satisfied via plan-checker + manual per precedent. NOTE: GRD-01..08 live in A3-SPEC.md, not REQUIREMENTS.md (phase_req_ids null) — gap-analysis 64/64 "uncovered" is M/T-track noise, not an A3 defect. **A3 depends on A1+A2 being executed first (strict A1→A2→A3 build order, CONCEPT §9) — neither executed yet.** Resume: `/gsd:execute-phase A3`.
- 2026-05-18 — **T8-02 TextArea InputBar complete.** Reworked the TUI input bar onto a TextArea wrapper with `.text` and no `.value`, Enter submit, Shift+Enter newline, preserved empty-only slash palette guard, `#input height: auto`, and one additive `ctrl+r` keymap row. Generated INPUT-01 snapshot anchors 1-4. Verified focused INPUT-01 tests, slash/full-flow/keymap regressions, combined 38-test T8-02 selection, py_compile, and diff check. Resume: execute `T8-03`.
- 2026-05-18 — **T8-01 Input Bar Ergonomics scaffold complete.** Added the `pytest-textual-snapshot` dev dependency, T8 TUI fixtures (`seeded_history`, `stub_provider`, `mock_recorder_bridge`), collectable xfail scaffolds for INPUT-01..05, and TextArea-tolerant migrations for existing `.value`-based input tests. Verified snapshot import/dev-only placement, focused existing TUI tests, scaffold collection, and xfail-only scaffold execution. Resume: execute `T8-02`.
- 2026-05-18 — **Phase A3 (voss-app Grid Engine) UI-SPEC approved.** A3-UI-SPEC.md authored by gsd-ui-researcher (0 user questions — fully derived from locked A1-UI-SPEC token system + A3-SPEC GRD-06/07 + A3-CONTEXT D-01..04 + sketch 001). gsd-ui-checker: 1 revision (rev 1/2) → 6/6 PASS. Blocking fix: `⋯` menu separator `margin: 2px 0` → `margin: 0` (off-4-grid removed) + Named Structural Constants row. 2 non-blocking FLAGs also applied: close-confirm buttons `Keep`/`Close` → `Keep open`/`Close anyway` (verb+adverb, mirrors warning copy); assistive aria-labels for `●` dot + `⋯` trigger (not-rendered, density unchanged). Zero token redefinitions, GRD-06/07 covered, no A4/A9/L2 scope creep, zero-Voss-exposure clean. Committed 68e2fb8. Resume: `/gsd:plan-phase A3` (UI gate now satisfied).
- 2026-05-18 — **Phase O1 (Session-Tree Substrate + Budget Fan-out) planned.** O-track keystone. 2 plans, 2 serial waves (shared `session_tree.py` + `test_session_tree.py` force O1-02 depends_on O1-01): O1-01 = `SessionTreeNode` + `SessionTreeManager` fan-out allocator (asyncio.Lock check-and-append, no-oversell race closed) + D-04 guarded `mutate_envelope` + per-node 0o600 `.voss/sessions/<root>/<id>.json` persistence (D-01/D-02/D-04); O1-02 = D-03 always-finalize boundary in `run_subagent` + `_finalized` double-finalize guard + reserve carve (`token_budget = limit − reserve`) + parent linkage (closes Leak 4). RESEARCH (HIGH confidence) resolved the 3 deferred questions: asyncio.Lock at allocator only (live race-tested), D-03 handles BOTH soft `exit_reason="budget"` + hard `BudgetExceededError`, resume path needs zero `session.py` change (`_scan_dir` globs flat `*.json` only). PATTERNS.md (5/5 analogs). VALIDATION.md drafted (Nyquist). gsd-plan-checker: 0 blockers, 1 warning (RESEARCH `## Open Questions` missing `(RESOLVED)` suffix — fixed directly, not a plan defect; all 12 plan dimensions PASS). Strict harness-additive: zero field diff on SessionRecord/RunRecord/BudgetScope, `test_session_redaction.py` gated unmodified in both plans. SDK `state.planned-phase`/`roadmap.annotate-dependencies`/`commit` verbs absent in this build — done manually per T6 precedent. Resume: `/gsd-execute-phase O1`.
- 2026-05-18 — **Phase A3 (voss-app Grid Engine) SPEC + context gathered.** A3-SPEC.md authored (8 reqs GRD-01..08, ambiguity 0.15, 13 acceptance criteria; 2 Socratic rounds — locks: no disk I/O in A3 [Solid tree + voss-app-core Rust mirror only; A4/A6 add file I/O], `⌘D` fork = A3 sibling-split inheriting cwd+shell, close-confirm reuses A2 D-07, 20×5 min floor, 9-pane ceiling, binary-split-tree grid model locked, N-pane ~60fps + flood-isolation perf bar). A3-CONTEXT.md captured 4 HOW decisions D-01..04: Canvas per-pane (research-validate @9, WebGL fallback gated) · 50/50 split + global `⌘=` · i3 nearest-edge-midpoint directional focus · close→expanded-sibling + last-pane-respawn (app never empty). Resume: `/gsd:plan-phase A3`.
- 2026-05-18 — **Phase T6 (PRD §2.4 Slash Debt, v0.1.1 patch) complete.** All 3 waves executed and verified: T6-01 `/cost --by-tool` approximation + test rewrite; T6-02 grouped `/help` (4 named buckets + Other) + dual-CLI one-line signpost (operator-widened D-04); T6-03 per-slash SC#1 happy-path tests + D-07 `/why` audit (SC#2) + D-03 `/resume` resolution confirmation. 27/27 tests in `test_repl_slash.py` green. Zero production code changes. T6-CONTEXT status updated, DISCUSSION-LOG closed, ROADMAP success criteria marked Met.
- 2026-05-18 — Completed T5-05 `voss jobs` and active-session lifecycle. Wired `_run_repl` to `make_toolset(..., session_id=record.id)`, added `jobs_cmd` sidecar inventory with table and D-11 JSON output, added `.active-session` write/remove plus explicit `reap_jobs()` in a `finally`, threaded `--keep-logs`, documented deliberate `_nosession` call sites, and restored `shell_monitor` reap-reason suffix required by watchdog tests. T5 and adjacent harness checks passed; full non-live run still stops early on the isolated subprocess `platformdirs` import issue outside T5.
- 2026-05-17 — Completed T5-04 monitor/signal/permissions. Implemented `shell_monitor` cursor reads and `shell_signal` INT/TERM control, registered monitor read-only and signal mutating, extended `SHELL`, closed D-12 edit-mode denial for `shell_run_background`/`shell_signal` while allowing `shell_monitor`, and updated TUI permission bridge verbs/targets. Focused monitor/signal/edit-mode/tool tests passed; full non-live merge check still stops early on the isolated subprocess `platformdirs` import issue outside T5.
- 2026-05-17 — Completed T5-03 background job engine. Added lifecycle `JobRecord`, per-session `bg-NNN` handles, atomic `.meta.json` sidecars, merged log files, supervisor tasks with no-output/RSS watchdogs, `reap_jobs` SIGTERM→SIGKILL timing, `signal_job`, `_tree_rss_bytes`, atexit/reap/reset wiring, `shell_run_background`, and additive `make_toolset(session_id=...)`. Updated tool classification tests for the new background/monitor tools. Focused T5/lifecycle/tool checks passed; full non-live merge check still stops early on an isolated subprocess `platformdirs` import issue outside T5.
- 2026-05-17 — Completed T5-02 SHELL-01 cap raise. Raised both `shell_run` and `_shell_capture` truncation caps from 4096 to 30720, updated the `shell_run` descriptor to 30KB, and preserved the 30s timeout plus existing `[exit N]` and `<truncated, total N bytes>` envelopes. Verified the cap and timeout source guards, the T5 truncation test, source inspection for two `30720` literals and no `_shell_capture` `4096`, py_compile, full `test_shell_timeout.py`, and `git diff --check`.
- 2026-05-17 — Completed T5-01 shell ergonomics Wave 0 scaffold. Approved and added runtime `psutil>=5.9,<8`, added lifecycle `_JOBS` reset coverage, created deterministic `emit.py`, authored 13 collecting red T5 shell/background/job tests, and added the red `30720` source-inspection guard. Verified psutil import, lifecycle reset smoke, py_compile, collect-only checks, intentional red T5 failures, intentional red `-k cap` guard, emitter smoke, and `git diff --check`.
- 2026-05-17 — Completed T4-06 cassette integration using Claude OAuth per operator direction. Added replay/record tests and `cache_two_turn_session.yaml`, preserved OAuth system block cache markers, extracted streaming cache usage from Anthropic `message_start`, and verified first-turn cache creation plus second-turn cache read in replay mode. Focused T4 cache tests passed; full `tests/harness/` now gets past the stale T1 block-list assertion and still fails on the known TUI `#budget-title` issue outside T4.
- 2026-05-17 — Completed T4-05 cost truthfulness and CLI work: updated `/cost --by-tool` to cite `T6 SLASH-07`, updated the existing slash assertion, added `/cost --by-model` 4-decimal verification, and added a LiteLLM `cost_per_token` cache-creation differential test without introducing Voss-owned pricing constants. Verified focused T4-05 tests, combined cost/repl slash tests, py_compile, stale-placeholder greps, pricing-constant grep, and `git diff --check`.
- 2026-05-17 — Completed T4-04 streaming telemetry recorder work: added additive cache creation/read fields to streaming `Usage`, propagated them through `agent.py` into flat `provider.response` telemetry and `RunRecorder.end_iteration`, added matching default-zero `IterationRecord` fields, and turned the streaming/telemetry/round-trip stubs green. Verified focused T4-04 tests, session/OAuth/stream/agent regressions, py_compile, cache-field greps, flat telemetry grep, and `git diff --check`; OAuth-provider cache-token capture remains deferred per T4 research.
- 2026-05-17 — Completed T4-03 agent composition work: added `_compose_system_blocks`, switched `run_turn` static system prefix to typed cached blocks in `messages[0]`, kept the rider uncached in `messages[1]`, and turned CACHE-01/CACHE-06 tests green. Updated existing system-prompt inspection tests for block-list content. Verified focused caching/invalidation tests, agent-loop/integration/VOSS.md regressions, provider T4-02 sanity tests, py_compile, grep checks, and `git diff --check`; OAuth-provider marker preservation remains out of T4 scope per research Pitfall 3.
- 2026-05-17 — Completed T4-02 extractor and non-streaming provider response work: added stdlib-only `_cache_tokens.extract_cache_tokens`, extended `ProviderResponse` with additive cache creation/read fields, wired `LiteLLMProvider.complete`, and turned `test_cache_tokens.py` plus `test_provider_response.py` green. Verified focused T4 tests, provider hermetic regressions, selected harness integration regressions, py_compile, import/default-construction smokes, grep checks, and `git diff --check`; broader harness run still has an unrelated TUI budget-modal failure.
- 2026-05-17 — Completed T4-01 Wave 0 test scaffold: pyproject pins raised for `litellm>=1.74.0` and `vcrpy>=8.0.0,<9`, nine CACHE red-test stub files added under `tests/harness/`, cassette README added under `tests/harness/fixtures/cassettes/`, and ROADMAP marked T4-01 complete. Required collect-only check found 22 tests; quick T4-02/T4-03 red check fails intentionally on `pytest.fail`.
- 2026-05-15 — Phase T3 context gathered (4 decision areas, 16 decisions D-01..D-16: MCP subsystem shape — 3-file layout + lazy-on-first-call + new `voss/harness/lifecycle.py` reap hook + mcp.yml schema with env-interp/{cwd}/timeout/env-allowlist; httpx + tool plumbing — single shared AsyncClient in new `voss/harness/net.py` NetSession + flat `web_search.py` BraveBackend + `make_toolset(cwd, *, net=None)` backward-compatible kwarg; permission gate — `is_network` stored on ToolEntry + net-check before mode-tier in `PermissionGate.check` + MCP scope applied at registration time + tool-result-string denial UX; CLI + telemetry — `voss mcp {list,call}` argparse subparser + `--arg key=value` JSON-typed + `redact_url` pure helper in telemetry.py wrapped by NetSession + `NetSession.acquire(tool_name)` rate-limit site). Open questions for researcher: locate existing teardown hook before adding lifecycle.py, pin MCP `destructiveHint` protocol version, source Codex launcher path.
- 2026-05-15 — Phase T3 SPEC.md authored (7 reqs NET-01..NET-07, ambiguity 0.13, 13 acceptance criteria, 10-item out-of-scope list). 5 Socratic rounds: web_fetch (1MB cap, 30s timeout, HTTP GET only), web_search (Brave only, env-key opt-in), MCP stdio client (Codex pattern lift, filesystem ref server only in CI), allow_net gate, query-stripped URL telemetry, per-tool token bucket (web_fetch 30/min, web_search 10/min, MCP unlimited), streaming OUT.
- 2026-05-15 — Phase T2 planned (6 plans across 5 waves: T2-01 BatchRecord + recorder API, T2-02 max_parallel_reads config, T2-03 partition scheduler + BatchInvariantError + telemetry, T2-04 fs_edit_many atomic multi-edit, T2-05 fs_read_many bundle, T2-06 micro-benchmark + human-verify checkpoint); plan-checker passed iter 1/3 with 1 blocker (VALIDATION map numbering misalignment — fixed by orchestrator) + 3 advisory warnings (T2-02 T1-04 hedging, 5-wave vs 4-wave, T2-06 Plan import path) shipped as-is.
- 2026-05-07 — Project initialized via `/gsd-new-project`.
- 2026-05-07 — Initial runtime/compiler/language roadmap created for phases 1-6.
- 2026-05-09 — Harness and Rust planning added, including a later Rust port.
- 2026-05-10 — Scope lock reframed v0.1 around a harness-led MVP with `.voss` as workflow control layer.
- 2026-05-10 — Roadmap rebaselined to M-prefixed phases M0-M5; Rust deferred until Python harness usage is proven.
- 2026-05-10 — Phase M1 context gathered (4 decision areas: voss edit scope, permission modes, voss doctor, session redaction).
- 2026-05-10 — Phase M1 planned: 7 plans across 3 waves; plan-checker passed after 1 revision (3 blockers + 4 warnings cleared).
- 2026-05-10 — Phase M2 context gathered (4 decision areas: analyze + index lifecycle, cognition file schemas, session move + per-run ledger, context injection on resume).
- 2026-05-11 — Phase M2 planned: 7 plans across 6 waves (M2-00 scaffold + M2-01..06); plan-checker passed after 1 revision (4 blockers + 5 warnings cleared).
- 2026-05-11 — Phase M3 context gathered (4 decision areas: hermetic run + check speed via auto-StubProvider + static check, sample coverage via support memory.episodic + research try/catch + use, slimmed legacy-06 test plan under tests/examples/, framing via README section + sample headers + docs/voss-vs-python.md).
- 2026-05-11 — Phase M3 planned: 6 plans across 4 waves (M3-01 analyzer guard + M3-02 auto-stub + M3-03 coverage fixtures parallel in W0; M3-04 sample extensions W1; M3-05 e2e suite repoint W2; M3-06 speed gate + framing docs W3); plan-checker passed iteration 2 after 1 revision (3 warnings cleared: M3-04 promoted to W1 for M3-02 hook ordering; M3-02 must_haves 4-test alignment; M3-06 D-14 negation phrasing audit).
- 2026-05-11 — Phase M4 context gathered (5 decision areas: pipeline split across loop/router/planner/executor/reviewer.voss with thin .voss control flow only and Python imports compiled functions; voss check + compile extended to walk directories static-only; VOSS_HARNESS=compiled env-flag opt-in with voss/harness/agent.py kept as parity oracle and loud stale-cache failure; per-file `.voss-cache/harness/*.py` artifacts sha-keyed via _manifest.json; M4 success bar = stub-provider real turn end-to-end, live-provider parity deferred to M5).
- 2026-05-11 — Phase M4 planned: 5 plans across 5 waves (M4-01 compiler sub-plan grammar use..as + codegen auto-await W0; M4-02 dir-walk + cache.py + StaleHarnessCacheError + sandbox.write_cache W1; M4-03 5 .voss files + _run_step_loop + ToolEntry.invoke_dict + _resolve_run_turn W2; M4-04 parity test FakeProvider + DOG-07 smoke W3; M4-05 CI gate + README one-liner + doctor cache row W4); plan-checker passed iteration 2 after 1 revision (3 warnings cleared: Open Questions RESOLVED markers, M4-03 stub fallback removed with LOC-floor enforcement 20/8/8/6/12, VALIDATION config.py mismatch reconciled).
- 2026-05-12 — Phase M4 completed all five waves. DOG-01..08 are covered by the five authored `.voss` harness files, `voss check/compile voss/harness/agent/`, cache freshness validation, compiled backend parity/smoke tests, CI gate, README eager-compile one-liner, and `voss doctor` harness-cache row.
- 2026-05-11 — Phase M5 context gathered (7 decision areas: `voss eval` CLI subcommand emitting JSONL + Markdown under `.voss/eval/<timestamp>/`; five golden task fixtures under `tests/eval/golden/NN-slug/` with per-task `task.toml` + isolated temp git repos; LLM-as-judge scorer with rubric per task and JSON-mode `Verdict {verdict, confidence, rationale}`; live-by-default with `--stub` for hermetic smoke at k=3 runs per task; cost from `RunRecord.cost_usd`, confidence from `Plan.confidence`, Pearson r reported; wheel-in-tempvenv packaging smoke in `tests/packaging/` with PyPI publish deferred and README install polish; measurement-only ship posture — no CI threshold gate, human reads report).
- 2026-05-12 — Roadmap extended with M6 npm Wrapper phase. Goal: publish `voss` as an npm package that vendors a pinned Python interpreter + the v0.1 wheel so JS-ecosystem developers can `npm i -g voss` (or `npx voss`) without managing Python. Pattern: pyright-style bundled-Python distribution; npm package vendors the same wheel M5 verifies — no JS reimplementation. NPM-01..05 added to REQUIREMENTS.md (total v0.1 reqs 54 → 59). Cross-cutting: Python source unchanged, DIST-01 stays deferred, Windows enters v0.1 only via npm with optional drop to mac+linux if vendoring proves expensive.
- 2026-05-12 — SDK contract landed: `docs/sdk.md` + expanded `voss.harness.__all__` (8 symbols) + stability docstrings on `voss_runtime` and `voss.harness` + `tests/packaging/test_public_api.py` pinning both `__all__` sets against drift. Four known public-API gaps filed as v0.2 candidate phase M7 — SDK Polish (SDK-01 Renderer protocol, SDK-02 tool_entry_from_callable, SDK-03 read-only SessionView, SDK-04 RuntimeConfig.from_toml, SDK-05 stable provider registration). Not committed to a v0.1 milestone; lands when embedder demand surfaces.
- 2026-05-13 — M7 SDK Polish promoted from v0.2 candidate to formal v0.1 phase. SDK-01..05 moved into REQUIREMENTS.md active section + traceability row; ROADMAP.md gets full M7 phase block (goal, required surface sketch, capabilities, 5 success criteria, cross-cutting constraints). Total v0.1 reqs 59 → 64. Phase is pure promote-existing-internals — no new behavior, only renames + re-exports + docstrings + test_public_api.py regression entries. Ordering preference: M7 before v0.1.0 publish for surface stability; falls back to 0.1.1 minor bump if M6 ships first (pre-1.0 minor breaks allowed per docs/sdk.md).
- 2026-05-13 — M7 context + plans landed. CONTEXT.md captures 26 base decisions D-01..D-26 (synthesized from ROADMAP M7 block + REQUIREMENTS SDK-01..05 + scouted internals; user declined interactive discuss-phase given scope tightness). RESEARCH.md (gsd-phase-researcher HIGH confidence) surfaced 7 corrections + 5 resolved open questions; CONTEXT refined with R-01..R-13 lock-in (notably: Renderer has 11 methods not 9; factory delegates to existing @tool decorator; sync callables get async-shim wrap; SessionRecord.runs is list[dict] so view_session uses defensive .get(); SDK-05 is ATOMIC — kwarg + ModelProvider isinstance validation + register_provider re-export + 10-callsite audit all in one wave; 6-wave structure with waves 1-4 parallel, wave 5 sequential after 4, wave 6 integration depends on all). 6 PLAN.md files + M7-PATTERNS.md (10 reusable patterns). gsd-plan-checker iter 2/3 PASS clean — iter 1 raised 4 blockers (all in M7-06 Task 2 — the M7 success contract test_sdk_embedding.py) + 5 warnings; planner revision resolved all with deterministic FORBIDDEN_PRIVATE_PATHS allowlist replacing comment-scanning, UUID-fixture provider names replacing hardcoded names, noqa-pinned public-API imports + live-imports test defending against linter strip, positive-registration evidence cross-linked between test 5 (raise-on-duplicate) and test 7 (end-to-end embedding), and a checkpoint:phase-final wrapper on the pytest -x acceptance gate.
- 2026-05-16 — Phase T6 (PRD §2.4 Slash Debt, v0.1.1 patch) planned: 3 plans, strict serial waves W1→W2→W3 (T6-01 `/cost --by-tool` even-split derivation + obsoleted-stub-test rewrite [SLASH-07]; T6-02 grouped `_print_slash_help` + dual-`main` `/help` signpost [SLASH-01..07]; T6-03 per-slash happy-path tests + D-07/D-03 audit [SLASH-01,03,05,06]). Discuss-phase done (CONTEXT D-01..D-08 locked); research SKIPPED by operator (SC#1 per-slash test is the validation contract; Nyquist "continue anyway", no VALIDATION.md); single-shot opus planner timed out so switched to chunked outline→per-plan mode. gsd-plan-checker iter 2/3 PASS — iter 1 raised 0 blockers + 4 warnings, all resolved: W1 T6-PATTERNS.md authored directly from verified live-code anchors after agent failed to persist it 3× (4 edit sites); W2 `pytest | tail -N` exit-code masking removed across all plans; W3 D-04 scope widened by operator decision to signpost BOTH `voss/cli.py:149` (production `voss=voss.cli:main`) and `voss/harness/cli.py:2197` (standalone) docstrings; W4 venv-active note added. T6-PATTERNS.md / state-update / roadmap-annotate gsd-sdk verbs absent in this build — done manually; per-plan committed for crash-resilience.

## Notes

- Existing `.planning/phases/01-*` through `07-*` directories remain historical planning artifacts unless explicitly archived.
- Next operational step after this rebaseline is to plan M0, then M1.

**Planned Phase:** A8 (voss-app-workspaces-ux-polish-theming) — 7 plans — 2026-05-21T00:04:42.143Z

### Roadmap Evolution

- Phase A12 added: voss-app ADE Visual Redesign — left sidebar + warm site palette + branded titlebar + pane chrome accent bars. 8 requirements (ADE-01..08), 8 plans (P1–P8). Source: sketch 002 Variant A winner. (2026-05-22)
