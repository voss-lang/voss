---
phase: 06
plan: "06-02"
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/examples/*
autonomous: true
requirements:
  - EX-02
---

<objective>
Validate EX-02 support end-to-end through the same CLI and generated-Python paths as Wave 1, using fake semantic indexes and encoders so default tests never download sentence-transformers models.
</objective>

<threat_model>
Threats:
- T-06-02-A: Support validation silently runs without the Phase 6 contract marker.
- T-06-02-B: Semantic routing tests instantiate real embedding models or download sentence-transformers assets.
- T-06-02-C: Assertions reimplement support routing instead of comparing to the raw Python oracle.
- T-06-02-D: `.voss-cache` indexes are written under the repository instead of the temp project.

Mitigations:
- Task `06-02-0` requires the `phase6-examples-contract-ok` marker or an exact rerun of `06-01-0`.
- Tests write fake manifests/indexes under `tmp_path/.voss-cache` and monkeypatch `SemanticMatcher._encode` before importing generated or raw support modules.
- Route/fallback outputs are compared to `examples.raw_python.support.handle_message`.
- Helper cleanup checks run after focused support tests and at plan verification.
</threat_model>

<tasks>

<task id="06-02-0">
  <type>execute</type>
  <files>read-only</files>
  <read_first>.planning/phases/06-examples-validation/06-01-PLAN.md, .planning/phases/06-examples-validation/06-01-SUMMARY.md, .planning/phases/06-examples-validation/06-VALIDATION.md</read_first>
  <action>
Before editing support tests, confirm the Phase 6 contract marker. If `.planning/phases/06-examples-validation/06-01-SUMMARY.md` does not contain `phase6-examples-contract-ok`, rerun task `06-01-0` exactly and stop if it fails.
  </action>
  <verify>python3 - <<'PY'
from pathlib import Path
summary = Path(".planning/phases/06-examples-validation/06-01-SUMMARY.md")
if summary.exists() and "phase6-examples-contract-ok" in summary.read_text():
    print("phase6-examples-contract-marker-ok")
else:
    raise SystemExit("Missing phase6-examples-contract-ok marker; rerun 06-01-0 exactly before 06-02.")
PY</verify>
  <acceptance_criteria>
    - The executor confirms `phase6-examples-contract-ok` before editing support tests.
    - If the marker is absent and the gate rerun fails, no Phase 6 source or test files are edited.
  </acceptance_criteria>
</task>

<task id="06-02-1">
  <type>tdd</type>
  <files>tests/examples/test_support_e2e.py, tests/examples/helpers.py</files>
  <read_first>tests/parser/examples/support.voss, examples/raw_python/support.py, tests/integration/test_support_example.py, tests/codegen/test_examples.py, voss_runtime/semantic.py, .planning/phases/06-examples-validation/06-PATTERNS.md</read_first>
  <action>
Create `tests/examples/test_support_e2e.py` for EX-02.

Required tests:
- `test_support_check_passes_without_cache_artifacts`
  - Copy `support.voss` into `tmp_path`.
  - Run `voss check support.voss`.
  - Assert exit `0`, no analyzer errors, and no `.voss-cache` is created by check.
- `test_support_compile_emits_temp_semantic_index_only`
  - Run `voss compile support.voss -o tmp_path/out/support.py` from the temp project.
  - Assert any emitted index/manifest is under `tmp_path/.voss-cache`, not the repo root.
  - Assert generated source parses with `ast.parse`.
- `test_support_generated_routes_match_raw_python`
  - Patch `SemanticMatcher._encode` using the same synthetic vector strategy as `tests/integration/test_support_example.py`.
  - Exercise angry, refund, auth, and generic messages against generated functions and raw `handle_message`.
  - Assert generated outputs equal raw outputs.
- `test_support_compile_python_and_run_have_identical_stdout`
  - Run `python3 tmp_path/out/support.py` and `voss run support.voss` with deterministic stub provider configuration.
  - Assert both exit `0`, no traceback appears, and normalized stdout is identical.
- `test_support_fallback_uses_stub_provider_without_live_calls`
  - Generic message falls through to the context path and returns the configured `StubProvider` response.

Do not add sentence-transformers downloads, live provider calls, or a second implementation of semantic routing in the test assertions. Fake embeddings and fake indexes are test fixtures only.
  </action>
  <verify>pytest tests/examples/test_support_e2e.py -q</verify>
  <acceptance_criteria>
    - `test -f tests/examples/test_support_e2e.py`
    - `grep -F "SemanticMatcher._encode" tests/examples/test_support_e2e.py` matches
    - `grep -F "voss check" tests/examples/test_support_e2e.py` matches
    - `grep -F "voss compile" tests/examples/test_support_e2e.py` matches
    - `grep -F "python3" tests/examples/test_support_e2e.py` matches
    - `grep -F "voss run" tests/examples/test_support_e2e.py` matches
    - `pytest tests/examples/test_support_e2e.py -q` passes without network/model downloads
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
```bash
pytest tests/examples/test_support_e2e.py -q
pytest tests/examples/test_contract.py tests/examples/test_helpers.py tests/examples/test_classify_e2e.py tests/examples/test_support_e2e.py -q
python3 - <<'PY'
from pathlib import Path
bad = [str(p) for p in Path(".").glob("**/.voss-cache/*.idx") if ".pytest_cache" not in p.parts]
if bad:
    raise SystemExit(f"repo-local indexes left behind: {bad}")
print("phase6-support-no-repo-local-cache-ok")
PY
git diff --check -- tests/examples
```
</verification>

<success_criteria>
- EX-02 passes `voss check`, `voss compile` plus `python3`, and `voss run`.
- Angry, refund, auth, and generic fallback paths match the raw Python support oracle.
- Default support tests use fake embeddings/indexes and never download sentence-transformers models.
- No repo-local `.voss-cache` artifacts remain after support validation.
</success_criteria>
