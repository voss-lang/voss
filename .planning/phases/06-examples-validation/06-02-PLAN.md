---
phase: 06
plan: "06-02"
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/examples/test_support_e2e.py
autonomous: true
requirements:
  - EX-02
must_haves:
  truths:
    - "Do not start until `06-01-0` has printed `phase6-examples-contract-ok` and the marker is recorded."
    - "`support.voss` validates semantic routing and ctx fallback without downloading sentence-transformers models."
    - "Generated support behavior must match `examples.raw_python.support.handle_message` for angry, refund, auth, and generic fallback messages."
  artifacts:
    - "`tests/examples/test_support_e2e.py`."
  constraints:
    - "Do not edit parser examples, raw Python examples, analyzer, codegen, CLI, runtime, ROADMAP, STATE, or SecondBrain in this plan."
    - "If semantic-index or generated support behavior fails because prior phases are incomplete, record the blocker rather than weakening tests."
---

<objective>
Validate EX-02: `support.voss` checks, compiles, runs, and matches raw Python semantic routing under fake embeddings/indexes and deterministic provider stubs.
</objective>

<threat_model>
Threats:
- T-06-02-A: Support tests instantiate real sentence-transformers models or call live providers.
- T-06-02-B: Generated support routing uses labels/indexes that drift from raw Python behavior.
- T-06-02-C: `voss check` or `voss compile` leaks `.voss-cache` files into the repo root.

Mitigations:
- Use fake analyzer indexes and synthetic `SemanticMatcher._encode` vectors exactly like existing raw Python support integration tests.
- Compare generated route outputs to `examples.raw_python.support.handle_message`.
- Run all CLI commands from a temp cwd and scan repo root for `.voss-cache/*.idx`.
</threat_model>

<tasks>

<task id="06-02-0">
  <type>execute</type>
  <files>read-only contract marker</files>
  <read_first>.planning/phases/06-examples-validation/06-01-PLAN.md, .planning/phases/06-examples-validation/06-VALIDATION.md</read_first>
  <action>
Confirm the Phase 6 contract gate marker before editing files for this wave. Require the literal `phase6-examples-contract-ok` in the `06-01` execution summary or rerun the exact read-only gate from `06-01-0`.
  </action>
  <verify>python3 - <<'PY'
from pathlib import Path
import importlib
import tomllib
summary = Path(".planning/phases/06-examples-validation/06-01-SUMMARY.md")
if summary.exists() and "phase6-examples-contract-ok" in summary.read_text():
    print("phase6-examples-contract-marker-ok")
else:
    runtime = importlib.import_module("voss_runtime")
    voss = importlib.import_module("voss")
    runtime_exports = ["ProbableValue", "ContextScope", "BudgetScope", "BudgetExceededError", "SemanticMatcher", "VossAgent", "gather", "EpisodicMemory", "SemanticMemory", "WorkingMemory", "tool", "StubProvider", "configure", "reset_config"]
    compiler_exports = ["parse", "to_dict", "analyze", "AnalysisResult", "Diagnostic", "EmittedIndex", "generate_python", "CodegenResult", "CodegenError"]
    missing_runtime = [name for name in runtime_exports if not hasattr(runtime, name)]
    missing_compiler = [name for name in compiler_exports if not hasattr(voss, name)]
    required_files = [Path("tests/parser/examples/classify.voss"), Path("tests/parser/examples/support.voss"), Path("tests/parser/examples/research.voss"), Path("examples/raw_python/classify.py"), Path("examples/raw_python/support.py"), Path("examples/raw_python/research.py"), Path("tests/analyzer/test_examples.py"), Path("tests/codegen/test_examples.py"), Path("voss/cli.py"), Path("samples/classify.voss"), Path("samples/support.voss"), Path("samples/research.voss")]
    missing_files = [str(path) for path in required_files if not path.exists()]
    phase5_summary = Path(".planning/phases/05-cli-packaging-linguist/05-01-SUMMARY.md")
    missing_phase5_marker = not (phase5_summary.exists() and "phase5-cli-contract-ok" in phase5_summary.read_text())
    scripts = tomllib.loads(Path("pyproject.toml").read_text()).get("project", {}).get("scripts", {})
    missing_script = scripts.get("voss") != "voss.cli:main"
    if missing_runtime or missing_compiler or missing_files or missing_script or missing_phase5_marker:
        raise SystemExit(f"Phase 6 contract incomplete; missing_runtime={missing_runtime}; missing_compiler={missing_compiler}; missing_files={missing_files}; missing_voss_script={missing_script}; missing_phase5_marker={missing_phase5_marker}")
    print("phase6-examples-contract-ok")
    print("phase6-examples-contract-marker-ok")
PY</verify>
  <acceptance_criteria>
    - The executor confirms `phase6-examples-contract-ok` before code edits.
    - If the marker is absent and the gate cannot be rerun successfully, this plan is blocked.
  </acceptance_criteria>
</task>

<task id="06-02-1">
  <type>tdd</type>
  <files>tests/examples/test_support_e2e.py</files>
  <read_first>tests/parser/examples/support.voss, examples/raw_python/support.py, tests/integration/test_support_example.py, tests/analyzer/test_examples.py, tests/examples/helpers.py</read_first>
  <action>
Create `tests/examples/test_support_e2e.py`.

Required tests:
- `test_support_check_has_no_errors_and_no_cache`
  - Copy `support.voss` into `tmp_path`.
  - Run `voss check support.voss`.
  - Assert exit `0`, no errors, no generated `.py`, and no `.voss-cache`.
- `test_support_compile_emits_temp_semantic_index`
  - Run `voss compile support.voss -o out/support.py` with `project_root=tmp_path` and cache under temp project.
  - Assert generated Python parses.
  - Assert any emitted `support.idx` is under `tmp_path/.voss-cache`, never repo root.
  - Assert manifest has a `matches` entry with three cases.
- `test_support_generated_routes_match_raw_python`
  - Use synthetic encoder vectors for angry/refund/auth/generic messages.
  - Execute generated support behavior and raw `handle_message`.
  - Assert route outputs match for:
    - `"I'm so angry, fix it"` -> escalated
    - `"Can I get a refund?"` -> refund flow
    - `"I can't log in"` -> auth support
    - `"What pricing tiers do you have?"` -> stub fallback text
- `test_support_voss_run_matches_compile_python`
  - Run `voss run support.voss` in deterministic env.
  - Assert exit `0`, no traceback, and stdout matches the compile+`python3` representative path.
  </action>
  <verify>pytest tests/examples/test_support_e2e.py -q</verify>
  <acceptance_criteria>
    - `grep -F "support.voss" tests/examples/test_support_e2e.py` matches.
    - `grep -F "SemanticMatcher" tests/examples/test_support_e2e.py` or `grep -F "_encode" tests/examples/test_support_e2e.py` matches.
    - `grep -F "support.idx" tests/examples/test_support_e2e.py` matches.
    - `pytest tests/examples/test_support_e2e.py -q` passes.
    - No repo-local `.voss-cache/*.idx` artifacts remain.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
```bash
pytest tests/examples/test_support_e2e.py -q
pytest tests/examples -q
python3 - <<'PY'
from pathlib import Path
bad = [str(p) for p in Path(".").glob("**/.voss-cache/*.idx") if ".pytest_cache" not in p.parts]
if bad:
    raise SystemExit(f"repo-local indexes left behind: {bad}")
print("phase6-support-no-repo-cache-ok")
PY
git diff --check -- tests/examples/test_support_e2e.py
```
</verification>

<success_criteria>
- `support.voss` passes `voss check`, `voss compile` + `python3`, and `voss run`.
- Support semantic routing and fallback behavior match raw Python under deterministic fake embeddings/stubs.
- No default support test downloads models, calls providers, or writes repo-local cache artifacts.
</success_criteria>
