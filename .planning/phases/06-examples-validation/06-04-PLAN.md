---
phase: 06
plan: "06-04"
type: tdd
wave: 4
depends_on: ["06-03"]
files_modified:
  - tests/examples/test_cli_matrix.py
  - tests/examples/test_live_examples.py
  - tests/examples/helpers.py
  - voss/cli.py
  - voss/codegen.py
  - voss/analyzer.py
  - voss_runtime/*
autonomous: true
requirements:
  - EX-01
  - EX-02
  - EX-03
must_haves:
  truths:
    - "Do not start until `06-01-0` has printed `phase6-examples-contract-ok` and the marker is recorded."
    - "The full matrix runs all three examples through `voss check`, `voss compile`, `python3 generated.py`, and `voss run`."
    - "Optional live-provider tests are marked `live` and skip unless provider configuration is explicit."
    - "Source fixes in this final wave are allowed only for direct e2e contract defects exposed by the matrix; cite the failing example/mode."
  artifacts:
    - "`tests/examples/test_cli_matrix.py`."
    - Optional "`tests/examples/test_live_examples.py`."
  constraints:
    - "Do not add parser/analyzer/codegen/runtime/CLI fallback implementations just for tests."
    - "Do not weaken existing parser/analyzer/codegen/CLI tests."
    - "Do not edit ROADMAP, STATE, REQUIREMENTS, or SecondBrain in this plan."
    - "The listed source surfaces may only be touched if a Phase 6 e2e test proves a direct defect in that owning surface; keep fixes minimal."
---

<objective>
Close Phase 6 with the full examples matrix, optional live-provider smoke coverage, full suite/install verification, and artifact hygiene checks.
</objective>

<threat_model>
Threats:
- T-06-04-A: Individual example tests pass but the installed CLI matrix fails.
- T-06-04-B: Live provider tests become default CI gates or assert unstable natural-language text.
- T-06-04-C: Late source fixes broaden into cross-phase refactors.
- T-06-04-D: The final suite leaves repo-local `.voss-cache` or generated `.py` artifacts.

Mitigations:
- Matrix tests exercise literal CLI commands for all three examples.
- `test_live_examples.py` uses `@pytest.mark.live` and skips without explicit provider config.
- Source fixes are limited to proven direct contract defects and must be cited in the execution summary.
- Final verification scans repo root for cache/generated artifacts.
</threat_model>

<tasks>

<task id="06-04-0">
  <type>execute</type>
  <files>read-only contract marker</files>
  <read_first>.planning/phases/06-examples-validation/06-01-PLAN.md, .planning/phases/06-examples-validation/06-03-PLAN.md</read_first>
  <action>
Confirm the Phase 6 contract gate marker before editing files for this wave. Require the literal `phase6-examples-contract-ok` in the `06-01` execution summary or rerun the exact read-only gate from `06-01-0`.
  </action>
  <verify>python3 - <<'PY'
from pathlib import Path
import importlib
import tomllib
summary = Path(".planning/phases/06-examples-validation/06-01-SUMMARY.md")
if summary.exists() and "phase6-examples-contract-ok" in summary.read_text():
    print("phase6-examples-contract-marker-ok")
else:
    runtime = importlib.import_module("voss_runtime")
    voss = importlib.import_module("voss")
    runtime_exports = ["ProbableValue", "ContextScope", "BudgetScope", "BudgetExceededError", "SemanticMatcher", "VossAgent", "gather", "EpisodicMemory", "SemanticMemory", "WorkingMemory", "tool", "StubProvider", "configure", "reset_config"]
    compiler_exports = ["parse", "to_dict", "analyze", "AnalysisResult", "Diagnostic", "EmittedIndex", "generate_python", "CodegenResult", "CodegenError"]
    missing_runtime = [name for name in runtime_exports if not hasattr(runtime, name)]
    missing_compiler = [name for name in compiler_exports if not hasattr(voss, name)]
    required_files = [Path("tests/parser/examples/classify.voss"), Path("tests/parser/examples/support.voss"), Path("tests/parser/examples/research.voss"), Path("examples/raw_python/classify.py"), Path("examples/raw_python/support.py"), Path("examples/raw_python/research.py"), Path("tests/analyzer/test_examples.py"), Path("tests/codegen/test_examples.py"), Path("voss/cli.py"), Path("samples/classify.voss"), Path("samples/support.voss"), Path("samples/research.voss")]
    missing_files = [str(path) for path in required_files if not path.exists()]
    phase5_summary = Path(".planning/phases/05-cli-packaging-linguist/05-01-SUMMARY.md")
    missing_phase5_marker = not (phase5_summary.exists() and "phase5-cli-contract-ok" in phase5_summary.read_text())
    scripts = tomllib.loads(Path("pyproject.toml").read_text()).get("project", {}).get("scripts", {})
    missing_script = scripts.get("voss") != "voss.cli:main"
    if missing_runtime or missing_compiler or missing_files or missing_script or missing_phase5_marker:
        raise SystemExit(f"Phase 6 contract incomplete; missing_runtime={missing_runtime}; missing_compiler={missing_compiler}; missing_files={missing_files}; missing_voss_script={missing_script}; missing_phase5_marker={missing_phase5_marker}")
    print("phase6-examples-contract-ok")
    print("phase6-examples-contract-marker-ok")
PY</verify>
  <acceptance_criteria>
    - The executor confirms `phase6-examples-contract-ok` before code edits.
    - If the marker is absent and the gate cannot be rerun successfully, this plan is blocked.
  </acceptance_criteria>
</task>

<task id="06-04-1">
  <type>tdd</type>
  <files>tests/examples/test_cli_matrix.py</files>
  <read_first>tests/examples/test_classify_e2e.py, tests/examples/test_support_e2e.py, tests/examples/test_research_e2e.py, tests/examples/helpers.py, .planning/phases/06-examples-validation/06-VALIDATION.md</read_first>
  <action>
Create `tests/examples/test_cli_matrix.py`.

Required matrix:
- Parametrize over `classify`, `support`, and `research`.
- For each example:
  - copy `{name}.voss` to a temp project
  - run `voss check {name}.voss`
  - run `voss compile {name}.voss -o out/{name}.py`
  - assert generated Python parses and has no compiler imports
  - run `python3 out/{name}.py`
  - run `voss run {name}.voss`
  - assert exit codes are `0`, stderr has no traceback, and normalized stdout from compile+`python3` equals `voss run`
- Assert no repo-local `.voss-cache/*.idx` files and no generated `.py` files outside temp dirs.
  </action>
  <verify>pytest tests/examples/test_cli_matrix.py -q</verify>
  <acceptance_criteria>
    - `grep -F "pytest.mark.parametrize" tests/examples/test_cli_matrix.py` matches.
    - `grep -F "classify" tests/examples/test_cli_matrix.py` matches.
    - `grep -F "support" tests/examples/test_cli_matrix.py` matches.
    - `grep -F "research" tests/examples/test_cli_matrix.py` matches.
    - `pytest tests/examples/test_cli_matrix.py -q` passes.
  </acceptance_criteria>
</task>

<task id="06-04-2">
  <type>tdd</type>
  <files>tests/examples/test_live_examples.py</files>
  <read_first>tests/examples/test_cli_matrix.py, pyproject.toml, .planning/phases/06-examples-validation/06-RESEARCH.md</read_first>
  <action>
Create optional `tests/examples/test_live_examples.py`.

Rules:
- Every test must be decorated with `@pytest.mark.live`.
- Tests must skip unless explicit provider/model configuration and credentials are present.
- Do not assert exact natural-language output. Assert command success, non-empty stdout, no traceback, and no repo-local artifacts.
- Cover at least one smoke path for each example if provider config exists.

If the project already has a live-test helper pattern, reuse it. Otherwise keep the file simple and self-contained.
  </action>
  <verify>pytest tests/examples/test_live_examples.py -q -m "not live"</verify>
  <acceptance_criteria>
    - `test -f tests/examples/test_live_examples.py`
    - `grep -F "@pytest.mark.live" tests/examples/test_live_examples.py` matches.
    - `grep -F "pytest.skip" tests/examples/test_live_examples.py` matches.
    - `pytest tests/examples/test_live_examples.py -q -m "not live"` exits 0 with live tests deselected/skipped from default execution.
  </acceptance_criteria>
</task>

<task id="06-04-3">
  <type>execute</type>
  <files>tests/examples/*, voss/cli.py, voss/codegen.py, voss/analyzer.py, voss_runtime/*</files>
  <read_first>tests/examples, voss/cli.py, voss/codegen.py, voss/analyzer.py, voss_runtime, .planning/phases/06-examples-validation/06-VALIDATION.md</read_first>
  <action>
Run the full Phase 6 verification suite and make only minimal fixes for direct e2e contract defects.

Allowed fixes:
- CLI check/compile/run option, cwd, env, stdout/stderr, exit-code, or cache-dir bugs exposed by examples.
- Codegen bugs for the three PRD examples, such as missing runtime imports, async entrypoint issues, manifest consumption, or generated stdout behavior.
- Analyzer index/cache defects directly exposed by support/research examples.
- Runtime behavior defects where generated examples disagree with raw Python examples.

Not allowed:
- New language syntax or parser rewrites.
- Test-only compiler/runtime fallback behavior.
- Live-provider requirements in default tests.
- Broad refactors beyond the failing example contract.
  </action>
  <verify>pytest tests/parser tests/analyzer tests/codegen tests/cli tests/examples -q</verify>
  <acceptance_criteria>
    - `pytest tests/examples -q` passes.
    - `pytest tests/parser tests/analyzer tests/codegen tests/cli tests/examples -q` passes.
    - `python3 -m pip install -e . && voss --help` succeeds in active env or an isolated venv verification is recorded.
    - Any optional `pytest tests/examples -q -m live` run is recorded as run or skipped with a concrete reason.
    - No repo-local `.voss-cache/*.idx` files or unexpected generated `.py` files remain.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
```bash
pytest tests/examples -q
pytest tests/parser tests/analyzer tests/codegen tests/cli tests/examples -q
python3 -m pip install -e . && voss --help
python3 - <<'PY'
from pathlib import Path
bad_cache = [str(p) for p in Path(".").glob("**/.voss-cache/*.idx") if ".pytest_cache" not in p.parts]
if bad_cache:
    raise SystemExit(f"repo-local indexes left behind: {bad_cache}")
print("phase6-no-repo-local-cache-ok")
PY
git diff --check -- tests/examples voss/cli.py voss/codegen.py voss/analyzer.py voss_runtime
```

Optional:
```bash
pytest tests/examples -q -m live
```
</verification>

<success_criteria>
- All three PRD examples pass `voss check`, `voss compile` + `python3`, and `voss run`.
- Generated behavior matches raw Python behavior for classify, support, and research representative paths.
- Optional live-provider tests are present but not part of default CI.
- Full parser/analyzer/codegen/CLI/example suite and install smoke pass.
- No default verification requires live providers, network, model downloads, or leaves repo-local artifacts.
</success_criteria>
