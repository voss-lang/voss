---
phase: 06
plan: "06-03"
type: tdd
wave: 3
depends_on: ["06-02"]
files_modified:
  - tests/examples/*
autonomous: true
requirements:
  - EX-03
---

<objective>
Validate EX-03 research end-to-end through parser, analyzer, codegen, CLI, generated Python, and runtime agent primitives, including both spawn/gather happy path and `within/fallback` timeout behavior under `StubProvider`.
</objective>

<threat_model>
Threats:
- T-06-03-A: Research validation runs without the Phase 6 contract marker.
- T-06-03-B: Agent tests become flaky because they rely on wall-clock provider calls or live web/search behavior.
- T-06-03-C: Timeout/fallback tests pass generated code only and drift from the raw Python oracle.
- T-06-03-D: Generated subprocess behavior differs from direct `voss run` but is not compared.

Mitigations:
- Task `06-03-0` requires the `phase6-examples-contract-ok` marker or an exact rerun of `06-01-0`.
- Default tests use `StubProvider(default_response="STUB SUMMARY")` and fake/local `webSearch` behavior only.
- Timeout fallback monkeypatching mirrors `tests/integration/test_research_example.py` for both generated and raw modules.
- CLI tests compare normalized stdout from `voss compile` plus `python3` against `voss run`.
</threat_model>

<tasks>

<task id="06-03-0">
  <type>execute</type>
  <files>read-only</files>
  <read_first>.planning/phases/06-examples-validation/06-01-PLAN.md, .planning/phases/06-examples-validation/06-01-SUMMARY.md, .planning/phases/06-examples-validation/06-VALIDATION.md</read_first>
  <action>
Before editing research tests, confirm the Phase 6 contract marker. If `.planning/phases/06-examples-validation/06-01-SUMMARY.md` does not contain `phase6-examples-contract-ok`, rerun task `06-01-0` exactly and stop if it fails.
  </action>
  <verify>python3 - <<'PY'
from pathlib import Path
summary = Path(".planning/phases/06-examples-validation/06-01-SUMMARY.md")
if summary.exists() and "phase6-examples-contract-ok" in summary.read_text():
    print("phase6-examples-contract-marker-ok")
else:
    raise SystemExit("Missing phase6-examples-contract-ok marker; rerun 06-01-0 exactly before 06-03.")
PY</verify>
  <acceptance_criteria>
    - The executor confirms `phase6-examples-contract-ok` before editing research tests.
    - If the marker is absent and the gate rerun fails, no Phase 6 source or test files are edited.
  </acceptance_criteria>
</task>

<task id="06-03-1">
  <type>tdd</type>
  <files>tests/examples/test_research_e2e.py, tests/examples/helpers.py</files>
  <read_first>tests/parser/examples/research.voss, examples/raw_python/research.py, tests/integration/test_research_example.py, tests/codegen/test_examples.py, voss_runtime/agent.py, voss_runtime/budget.py, .planning/phases/06-examples-validation/06-PATTERNS.md</read_first>
  <action>
Create `tests/examples/test_research_e2e.py` for EX-03.

Required tests:
- `test_research_check_passes_without_cache_artifacts`
  - Copy `research.voss` into `tmp_path`.
  - Run `voss check research.voss`.
  - Assert exit `0`, no analyzer errors, and no `.voss-cache`.
- `test_research_compile_python_matches_raw_happy_path`
  - Run `voss compile research.voss -o tmp_path/out/research.py`.
  - Run generated function-level behavior under `StubProvider(default_response="STUB SUMMARY")`.
  - Compare happy-path shape to raw `examples.raw_python.research.run_research`: both return non-empty strings and call the stub provider at least four times.
- `test_research_run_matches_compile_python_stdout`
  - Run `python3 tmp_path/out/research.py` and `voss run research.voss` with deterministic stub provider configuration.
  - Assert both exit `0`, have no traceback, and normalized stdout is identical.
- `test_research_timeout_fallback_matches_raw_python`
  - Monkeypatch generated `Synthesizer.run` to sleep and generated budget helper to force `latency_ms=50`.
  - Mirror the raw test pattern from `tests/integration/test_research_example.py`.
  - Assert generated fallback equals raw fallback and equals `"\n---\n".join(["STUB SUMMARY"] * 4)`.

Do not call live web search, live model providers, network APIs, or model downloads in default tests.
  </action>
  <verify>pytest tests/examples/test_research_e2e.py -q</verify>
  <acceptance_criteria>
    - `test -f tests/examples/test_research_e2e.py`
    - `grep -F "voss check" tests/examples/test_research_e2e.py` matches
    - `grep -F "voss compile" tests/examples/test_research_e2e.py` matches
    - `grep -F "python3" tests/examples/test_research_e2e.py` matches
    - `grep -F "voss run" tests/examples/test_research_e2e.py` matches
    - `grep -F "run_research" tests/examples/test_research_e2e.py` matches
    - `grep -F "STUB SUMMARY" tests/examples/test_research_e2e.py` matches
    - `pytest tests/examples/test_research_e2e.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
```bash
pytest tests/examples/test_research_e2e.py -q
pytest tests/examples/test_contract.py tests/examples/test_helpers.py tests/examples/test_classify_e2e.py tests/examples/test_support_e2e.py tests/examples/test_research_e2e.py -q
python3 - <<'PY'
from pathlib import Path
bad = [str(p) for p in Path(".").glob("**/.voss-cache/*.idx") if ".pytest_cache" not in p.parts]
if bad:
    raise SystemExit(f"repo-local indexes left behind: {bad}")
print("phase6-research-no-repo-local-cache-ok")
PY
git diff --check -- tests/examples
```
</verification>

<success_criteria>
- EX-03 passes `voss check`, `voss compile` plus `python3`, and `voss run`.
- Research happy path validates spawn/gather behavior under `StubProvider`.
- Research timeout validates `within/fallback` parity against the raw Python oracle.
- Default research tests remain hermetic and leave no repo-local cache artifacts.
</success_criteria>
