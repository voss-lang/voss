# Phase O3: Discussion Log

**Workflow:** `/gsd-discuss-phase O3` (default mode, SPEC.md present)
**Date:** 2026-05-19
**SPEC.md:** `.planning/phases/O3-board-state-machine/O3-SPEC.md` (9 requirements locked)

## Gray areas presented

Per workflow `analyze_phase`, SPEC.md locks WHAT/WHY. Generated gray areas covered HOW-decisions only:

1. Module layout
2. Gate-predicate registry shape
3. `Board.tick()` driver
4. `RunRecord` transition-delta payload schema

User selected: **all 4**.

## Question / answer log

| Area | Options presented | User selection | Locked in CONTEXT.md §decisions |
|---|---|---|---|
| Module layout | `voss/harness/board/` package (Recommended) · single `board.py` · split `board.py` + `board_verdict.py` | **Package** | `__init__.py`, `verdict.py` (typing+dataclasses only), `machine.py`, `gates.py`, `tick.py`, `errors.py`, `stub.py` |
| Gate-predicate registry | Transition-table dataclass (Recommended) · `@gate` decorator · big switch in `Board.move` | **Transition-table dataclass** | Frozen `dict[(Column,Column), tuple[Predicate, ...]]`; `Predicate` Protocol with `name` + `evaluate(GateContext)`; predicate order pinned (cheap → expensive); `dry_run_gate` returns ordered failing-clause names |
| `Board.tick()` driver | External asyncio task (Recommended) · `threading.Timer` · lazy on-transition | **External asyncio task** | `Board.start()` → `asyncio.create_task(_tick_loop())`; `Board.stop()` cancels; `_tick_once(now)` is sync test entry; `interval_s` injectable (default 1.0) |
| Transition-delta payload | Structured `{kind, from, to, outcome, failing_clauses, reason, verdict_snapshot, retry_count}` (Recommended) · free-form `dict[str, Any]` · 3 split event kinds | **Structured** | `TypedDict` in `voss/harness/board/machine.py`; rides existing `RunRecord.payload`; audit invariant: delta count == transition attempt count |

## Deferred / scope-creep redirects

None raised in this session. SPEC.md's Out-of-scope list was the perimeter; no out-of-scope items surfaced during the discussion.

## Claude's discretion (left for plan/exec)

- `Card` frozen-rebuild vs mutable-with-setters
- Exact `team_config.board` key names (`tick_interval_s` etc.)
- Predicate-name constants module vs class attribute (style)

## Open questions for downstream

Captured in CONTEXT.md `<open_questions>` — 4 items, mostly about reading O2 outputs vs imposing typed shapes.

---

*Generated by `/gsd-discuss-phase O3`.*
