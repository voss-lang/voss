---
phase: V13.2-rust-local-native-client-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/voss-sdk/Cargo.toml
  - crates/voss-sdk/src/lib.rs
  - crates/voss-sdk/src/error.rs
  - crates/voss-sdk/src/types/mod.rs
  - crates/voss-sdk/src/types/events.rs
  - crates/voss-sdk/src/types/rest.rs
autonomous: false
requirements: [VSDK-RS-01, VSDK-RS-07]
user_setup: []

must_haves:
  truths:
    - "crates/voss-sdk is a workspace member that compiles with cargo build"
    - "AgentEvent is a serde-tagged (tag = \"type\") enum with all 21 members and no Other catch-all"
    - "An exhaustive match over AgentEvent compiles; omitting a member is a compile error"
    - "VossError is a thiserror enum used as the public error type; the bearer token never appears in its Debug/Display output"
    - "cargo-typify CLI is available for the later drift gate"
  artifacts:
    - path: "crates/voss-sdk/Cargo.toml"
      provides: "library crate manifest with workspace pins + eventsource-stream/futures-util"
      contains: "[lib]"
    - path: "crates/voss-sdk/src/types/events.rs"
      provides: "21-member AgentEvent tagged-enum stub (PLACEHOLDER for typify output)"
      contains: "serde(tag = \"type\")"
    - path: "crates/voss-sdk/src/error.rs"
      provides: "VossError thiserror enum"
      contains: "enum VossError"
    - path: "crates/voss-sdk/src/types/rest.rs"
      provides: "REST request/response structs (SavedSession, DoctorReport, CostInfo, SessionInfo)"
      contains: "struct SavedSession"
  key_links:
    - from: "Cargo.toml"
      to: "crates/voss-sdk"
      via: "workspace members array"
      pattern: "crates/voss-sdk"
    - from: "crates/voss-sdk/src/lib.rs"
      to: "src/types/events.rs"
      via: "pub mod types"
      pattern: "pub mod (types|error)"
---

<objective>
Scaffold the new `voss-sdk` library crate as a workspace member and lay down the type/error foundation every later plan builds on: the public `VossError` enum (thiserror, token-safe), the REST request/response structs, and a **stub** 21-member `AgentEvent` serde-tagged enum that the entire SDK can compile and test against before V13.1 ships `contracts/`. This plan also installs `cargo-typify` and verifies the typify tagged-enum precondition so Wave 5's codegen replacement is unblocked.

Purpose: Unblock parallel development of client/stream/supervisor/projection (Waves 2–4) without waiting on the V13.1 `contracts/` snapshot. The stub `AgentEvent` has identical variant names and field names to the eventual typify output (sourced from `voss/harness/server/events.py`), so downstream code written against it does not change when stubs are replaced in Wave 5.

Output: A compiling `crates/voss-sdk` lib crate (`cargo build -p voss-sdk` green), workspace membership, `VossError`, `types::rest`, and a stub `types::events::AgentEvent` carrying `#[serde(tag = "type")]`.
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-SPEC.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-CONTEXT.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md

<interfaces>
<!-- Source of truth for the 21 AgentEvent members + their fields: voss/harness/server/events.py.
     The stub enum MUST match these variant names and field names exactly so typify output (Wave 5) is a drop-in replacement. -->

The 21 union members (events.py lines 34-216), with the wire `type` value and key fields:
- ServerConnected           type="server.connected"   (no fields)
- SessionIdle               type="session.idle"        session_id: String
- PermissionUpdated         type="permission.updated"  id, tool_name, args(map), dimension
- BannerEvent               type="banner"              model, cwd, git
- UserEvent                 type="user"                task
- ThinkingEvent             type="thinking"            label
- PlanEvent                 type="plan"                confidence(f64), steps(Vec<PlanStep>), cost_usd
- ToolEvent                 type="tool"                name, args(map), summary, state
- ClarifyEvent              type="clarify"             question, confidence
- FinalEvent                type="final"               text, confidence, cost_usd
- StreamDelta               type="stream.delta"        text
- StreamFinalize            type="stream.finalize"     role, confidence(Option<f64>), cost_usd(Option<f64>), timestamp(Option<String>)
- StatusEvent               type="status"              model, tokens(u64), cost_usd, ctx_pct
- CognitionLoaded           type="cognition_loaded"    architecture_tokens, constraints_count, plans_loaded, decisions_loaded
- CognitionOverflow         type="cognition_overflow"  architecture_tokens, budget
- PrinciplesOverflow        type="principles_overflow" principles_tokens, budget
- WarningEvent              type="warning"             message
- ProbableEvent             type="probable"            text, probability, alternatives(Vec<Alternative>)
- BudgetUpdated             type="budget.updated"      session_id, spent, limit, remaining, unit
- ConfidenceUpdated         type="confidence.updated"  session_id, message_id(Option<String>), score
- GateUpdated               type="gate.updated"        session_id, gate, decision

Helper structs: PlanStep { name, args(map) }; Alternative { text, probability }.

VossError variant shape (from PATTERNS.md src/error.rs section): Http(#[from] reqwest::Error), HttpStatus { status: u16, detail: String }, Sse(String), Decode(String), Handshake(String), Spawn(#[from] std::io::Error).

types::rest structs (from PATTERNS.md): SavedSession, DoctorCheck, DoctorReport, CostInfo { total_usd, turns }, SessionInfo { id, cwd, model }.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install cargo-typify and confirm package legitimacy (D-03 / RESEARCH package-legitimacy gate)</name>
  <read_first>
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"Package Legitimacy Audit" — typify/cargo-typify are [ASSUMED], Oxide Computer; §"Environment Availability" — cargo-typify NOT installed)
  </read_first>
  <what-built>
    cargo-typify is required by Wave 5's codegen drift gate (D-08) but is not yet installed (verified: `command -v cargo-typify` returns nothing). Two new crates also enter the build via this phase: `typify`/`cargo-typify` (codegen) and `eventsource-stream`/`futures-util` (already voss-tui pins, promoted here). RESEARCH flags all as [ASSUMED] (slopcheck unavailable). cargo-typify and eventsource-stream are the two genuinely new additions to the workspace surface.
  </what-built>
  <how-to-verify>
    1. Confirm typify legitimacy at https://crates.io/crates/cargo-typify (publisher: oxidecomputer; repo github.com/oxidecomputer/typify).
    2. Confirm eventsource-stream at https://crates.io/crates/eventsource-stream (already pinned by crates/voss-tui/Cargo.toml line 33 — same version 0.2).
    3. Run `cargo install cargo-typify --locked` and confirm `command -v cargo-typify` now resolves.
  </how-to-verify>
  <action>Operator approves the two new package additions (cargo-typify, eventsource-stream) against crates.io and installs cargo-typify via `cargo install cargo-typify --locked`. This is a blocking-human legitimacy checkpoint per RESEARCH (no auto-approve). T-V13.2-SC.</action>
  <acceptance_criteria>
    - `command -v cargo-typify` resolves to an installed binary (operator confirms).
    - Operator has eyeballed crates.io pages for cargo-typify and eventsource-stream and approved.
  </acceptance_criteria>
  <verify><human-check>Operator types "approved" after install + crates.io verification.</human-check></verify>
  <done>cargo-typify installed; both new packages legitimacy-approved by the operator.</done>
  <resume-signal>Type "approved" or report a legitimacy concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the voss-sdk crate manifest, lib root, and workspace membership</name>
  <files>Cargo.toml, crates/voss-sdk/Cargo.toml, crates/voss-sdk/src/lib.rs, crates/voss-sdk/src/types/mod.rs</files>
  <read_first>
    - Cargo.toml (root — `[workspace] members` lines 3-14; `[workspace.dependencies]` lines 30-59, including `tempfile = "3"` dev pin line 58)
    - crates/voss-tui/Cargo.toml (analog manifest — `[lib]` line 14, `[dependencies]` line 22, `eventsource-stream = "0.2"` line 33, `futures-util = "0.3"` line 35, `[dev-dependencies]` line 37)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md (§"crates/voss-sdk/Cargo.toml" — exact manifest deltas, lib-only, no anyhow/clap/crossterm/ratatui)
  </read_first>
  <action>Add `"crates/voss-sdk"` to the root `Cargo.toml` `[workspace] members` array (alongside `crates/voss-tui`). Create `crates/voss-sdk/Cargo.toml`: `[package] name = "voss-sdk"`, version/edition/rust-version/license/repository `.workspace = true`, a description; `[lib] name = "voss_sdk", path = "src/lib.rs"` with NO `[[bin]]`. `[dependencies]`: workspace pins `tokio`, `reqwest { workspace = true, features = ["stream"] }`, `serde`, `serde_json`, `thiserror`; plus `eventsource-stream = "0.2"` and `futures-util = "0.3"` (same direct pins voss-tui uses). Do NOT add `anyhow` to dependencies (library-grade rule — VossError is the public error type; anyhow may only appear in private helpers if ever needed). `[dev-dependencies]`: `tokio = { workspace = true, features = ["full"] }`, `tempfile = { workspace = true }`. Create `crates/voss-sdk/src/lib.rs` declaring `pub mod error; pub mod types;` (other modules added by later plans) with a crate-level doc comment. Create `crates/voss-sdk/src/types/mod.rs` declaring `pub mod events; pub mod rest;`.</action>
  <verify>
    <automated>cargo metadata --format-version 1 --no-deps 2>/dev/null | grep -q '"name":"voss-sdk"' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'crates/voss-sdk' Cargo.toml` returns ≥1 (workspace member added).
    - `crates/voss-sdk/Cargo.toml` has a `[lib]` section and NO `[[bin]]` section.
    - `grep -v '^#' crates/voss-sdk/Cargo.toml | grep -c 'anyhow'` returns 0 (no anyhow dependency).
    - `cargo metadata --no-deps` lists `voss-sdk` as a workspace member.
  </acceptance_criteria>
  <done>voss-sdk is a recognized workspace member with a lib-only manifest reusing workspace pins; lib.rs + types/mod.rs declare the module tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write VossError (thiserror, token-safe) and the stub 21-member AgentEvent tagged enum + REST structs</name>
  <files>crates/voss-sdk/src/error.rs, crates/voss-sdk/src/types/events.rs, crates/voss-sdk/src/types/rest.rs</files>
  <read_first>
    - voss/harness/server/events.py (THE source of truth — all 21 members lines 34-216, field names + the `type` Literal wire values; helper models PlanStep line 71, Alternative line 154)
    - crates/voss-tui/src/event.rs (lossy analog — the field reads the projection will later need: `.task`, `.label`, `.confidence`, `.steps[].name`, `.name`/`.state`, `.text`, `.message`)
    - crates/voss-tui/src/sessions.rs, crates/voss-tui/src/doctor.rs (SavedSession + DoctorReport/DoctorCheck struct shapes to copy verbatim)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md (§"src/error.rs", §"src/types/rest.rs", §"src/types/events.rs" stub pattern)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"CRITICAL: typify + Pydantic schema compatibility" — why the enum must be `#[serde(tag = "type")]`; §"Security Domain" — token must never appear in VossError Display)
  </read_first>
  <behavior>
    - Test: `serde_json::from_str::<AgentEvent>(r#"{"v":1,"type":"stream.delta","text":"hi"}"#)` deserializes to `AgentEvent::StreamDelta` with `text == "hi"`.
    - Test: `serde_json::from_str::<AgentEvent>(r#"{"v":1,"type":"session.idle","session_id":"s"}"#)` deserializes to `AgentEvent::SessionIdle`.
    - Test: `serde_json::from_str::<AgentEvent>(r#"{"v":1,"type":"server.connected"}"#)` deserializes to `AgentEvent::ServerConnected`.
    - Test: a `match` over an `AgentEvent` value that names all 21 variants compiles; a test module contains such an exhaustive match with NO wildcard arm (proves no `Other` and compile-enforced completeness).
    - Test: formatting `VossError::HttpStatus { status: 401, detail: "nope".into() }` via `{}` and `{:?}` produces a string that does NOT contain any token-looking secret (assert the literal token string used in a constructed error is absent).
  </behavior>
  <action>Create `crates/voss-sdk/src/error.rs` with `#[derive(Debug, Error)] pub enum VossError` exactly per PATTERNS §src/error.rs: variants `Http(#[from] reqwest::Error)`, `HttpStatus { status: u16, detail: String }`, `Sse(String)`, `Decode(String)`, `Handshake(String)`, `Spawn(#[from] std::io::Error)`, each with a `#[error(...)]` message. CRITICAL: no `#[error]` message may interpolate a bearer token; only `status`/`detail`/error strings appear (security — RESEARCH token-leak threat). Create `crates/voss-sdk/src/types/events.rs` as the PLACEHOLDER stub: a `#[derive(serde::Deserialize, serde::Serialize, Clone, Debug)] #[serde(tag = "type")] pub enum AgentEvent` with all 21 variants named exactly as in the interfaces block (ServerConnected, SessionIdle, PermissionUpdated, BannerEvent, UserEvent, ThinkingEvent, PlanEvent, ToolEvent, ClarifyEvent, FinalEvent, StreamDelta, StreamFinalize, StatusEvent, CognitionLoaded, CognitionOverflow, PrinciplesOverflow, WarningEvent, ProbableEvent, BudgetUpdated, ConfidenceUpdated, GateUpdated), each `#[serde(rename = "<wire type>")]` carrying a newtype payload struct (e.g. `ServerConnected(ServerConnected)`); define each payload struct with the field set from events.py (use `#[serde(default)]` where Pydantic has defaults; `Option<T>` for `| None` fields). Include helper structs `PlanStep { name: String, #[serde(default)] args: serde_json::Value }` and `Alternative { text: String, probability: f64 }`. Add a top-of-file `// PLACEHOLDER: replace with `cargo typify contracts/events.schema.json` output in Wave 5 (V13.2-06).` comment. Add a `#[cfg(test)]` module with the exhaustive-match test (names all 21 variants, no `_` arm) and the three deserialization tests from <behavior>. Create `crates/voss-sdk/src/types/rest.rs` copying `SavedSession` (sessions.rs) and `DoctorCheck`/`DoctorReport` (doctor.rs) verbatim, plus `CostInfo { total_usd: f64, turns: u64 }` and `SessionInfo { id, cwd, model }` per PATTERNS. Mark `rest.rs` types as hand-written (documented discretion call per RESEARCH Open Question 2: REST bodies are simple flat structs; typify is run only for the event union).</action>
  <verify>
    <automated>cargo test -p voss-sdk --lib && grep -q 'serde(tag = "type")' crates/voss-sdk/src/types/events.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p voss-sdk` is green.
    - `grep -c 'serde(tag = "type")' crates/voss-sdk/src/types/events.rs` returns ≥1.
    - The number of `#[serde(rename =` lines in events.rs equals 21 (one per member): `grep -c '#\[serde(rename = ' crates/voss-sdk/src/types/events.rs` returns 21.
    - `grep -v '^//' crates/voss-sdk/src/types/events.rs | grep -c 'Other'` returns 0 (no catch-all variant).
    - `cargo test -p voss-sdk --lib` passes including the exhaustive-match and deserialization tests.
  </acceptance_criteria>
  <done>VossError compiles as the public thiserror error with no token in any message; AgentEvent is a 21-member `#[serde(tag = "type")]` enum with no Other, an exhaustive-match test compiles, and REST structs exist; crate builds and lib tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Coordination gate — confirm V13.1 exporter fix for typify tagged-enum (RESEARCH Pitfall 1)</name>
  <read_first>
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"CRITICAL: typify + Pydantic schema compatibility" + §"Common Pitfalls / Pitfall 1" — Pydantic omits `type` from `required`, so typify emits `#[serde(untagged)]` unless V13.1's exporter adds `"type"` to each variant's `required`)
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-CONTEXT.md (D-10 — V13.1 owns the `contracts/` exporter + drift gate)
  </read_first>
  <what-built>
    The stub `AgentEvent` already carries `#[serde(tag = "type")]` by hand. The risk is that when Wave 5 runs `cargo typify` against V13.1's `contracts/events.schema.json`, typify will emit `#[serde(untagged)]` instead — UNLESS V13.1's exporter adds `"type"` to each event variant's `required` array. This checkpoint records the coordination requirement so Wave 5 is not surprised. As of planning, `contracts/` does NOT exist on disk (V13.1 unexecuted).
  </what-built>
  <how-to-verify>
    1. Check whether `contracts/events.schema.json` exists yet: `ls contracts/events.schema.json 2>/dev/null`.
    2. If it exists: confirm each `$defs.<Variant>.required` array contains `"type"` (e.g. `python -c "import json; s=json.load(open('contracts/events.schema.json')); print([n for n,d in s.get('$defs',{}).items() if 'type' in d.get('properties',{}) and 'type' not in d.get('required',[])])"` should print `[]`).
    3. If it does NOT exist (expected now): record that V13.1 must ship the exporter `required`-patch before Wave 5 (V13.2-06). No code change here — this is a recorded coordination dependency.
  </how-to-verify>
  <action>Verify the V13.1 exporter precondition for typify internal tagging. If `contracts/events.schema.json` is present, assert every event variant lists `"type"` in `required`; if absent, record the blocking coordination note for V13.2-06. No SDK source changes in this task.</action>
  <acceptance_criteria>
    - A documented determination in the SUMMARY: either (a) `contracts/events.schema.json` is present AND all variants include `"type"` in `required`, or (b) it is absent and V13.2-06 is flagged to hard-block on V13.1 shipping the `required`-patched exporter.
  </acceptance_criteria>
  <verify><human-check>Operator confirms the V13.1 coordination status (shipped-and-correct, or not-yet-shipped → Wave 5 blocks).</human-check></verify>
  <done>The typify tagged-enum precondition is verified-or-flagged; Wave 5 has a clear go/no-go signal on V13.1.</done>
  <resume-signal>Type "approved" with the V13.1 status (present/correct, or absent → blocks V13.2-06).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK consumer → VossError | Error values may be logged/printed by downstream callers; must never carry the bearer token |
| codegen toolchain (cargo-typify) → build | New external CLI enters the build/dev surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.2-09 | Information Disclosure | `VossError` Display/Debug | mitigate | No `#[error]` message interpolates a token; only status/detail/error strings. Task 3 behavior asserts a constructed error's string excludes the token literal. |
| T-V13.2-SC | Tampering | cargo-typify / eventsource-stream install | mitigate | Task 1 blocking-human legitimacy checkpoint against crates.io before install; [ASSUMED] packages from Oxide / existing voss-tui pin. |
</threat_model>

<verification>
- `cargo build -p voss-sdk` green.
- `cargo test -p voss-sdk --lib` green (exhaustive-match + deserialization + token-redaction tests).
- `grep 'serde(tag = "type")' crates/voss-sdk/src/types/events.rs` matches.
- `git diff --exit-code crates/voss-tui/` is clean (voss-tui untouched).
</verification>

<success_criteria>
- voss-sdk is a workspace member that builds.
- AgentEvent is a 21-member `#[serde(tag = "type")]` enum with no Other; exhaustive match compiles.
- VossError is the public thiserror error and never leaks the token.
- cargo-typify installed; V13.1 typify-tag precondition verified-or-flagged for Wave 5.
</success_criteria>

<output>
Create `.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-01-SUMMARY.md` when done. Record the V13.1 coordination determination (present/correct vs absent→blocks V13.2-06).
</output>
