---
phase: V13.2-rust-local-native-client-sdk
plan: 05
type: execute
wave: 4
depends_on: [V13.2-02, V13.2-03, V13.2-04]
files_modified:
  - crates/voss-sdk/tests/integration.rs
autonomous: true
requirements: [VSDK-RS-02, VSDK-RS-03, VSDK-RS-05, VSDK-RS-06, VSDK-RS-07]
user_setup: []

must_haves:
  truths:
    - "An integration suite spawns a real `voss serve` under VOSS_SERVE_FAKE_TURN=1 and exercises REST, SSE, auth, and supervisor against it"
    - "REST path: create session -> post message (202) -> read cost -> delete session (204) succeeds"
    - "A wrong bearer token yields VossError::HttpStatus { status: 401 } and a message during a running turn yields status 409"
    - "SSE: the stream yields ServerConnected first and SessionIdle last; dropping the stream mid-turn ends it without panic or leaked task"
    - "Supervisor: after shutdown/drop, no orphan `voss serve` process survives"
    - "Every integration test SKIPS (does not fail) when .venv/bin/python is absent"
  artifacts:
    - path: "crates/voss-sdk/tests/integration.rs"
      provides: "VOSS_SERVE_FAKE_TURN integration suite covering reqs 2/3/5/6"
      contains: "VOSS_SERVE_FAKE_TURN"
  key_links:
    - from: "crates/voss-sdk/tests/integration.rs"
      to: "voss_sdk public API"
      via: "spawn_with + VossClient + event_stream"
      pattern: "voss_sdk::"
    - from: "crates/voss-sdk/tests/integration.rs"
      to: ".venv/bin/python"
      via: "venv_python() skip-not-fail guard"
      pattern: "venv_python"
---

<objective>
Build the integration test harness (VSDK-RS-07) that proves the SDK works against a real `voss serve`, covering REST (req 2), SSE (req 3), handshake/bearer auth (req 5), and supervisor lifecycle (req 6) using the `VOSS_SERVE_FAKE_TURN` seam so CI needs no provider credentials. Every test skips cleanly when `.venv/bin/python` is absent, matching the `voss-tui` `over_the_wire.rs` precedent.

Purpose: This is the faithful end-to-end proof of the handshake, Bearer auth, SSE framing, and child supervision — the SPEC acceptance criteria that unit tests cannot cover. It also exercises the public API surface exactly as a downstream consumer would.

Output: `crates/voss-sdk/tests/integration.rs` with the REST/auth/SSE/supervisor tests; `cargo test -p voss-sdk --test integration` green (or cleanly skipped without venv).
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-SPEC.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-VALIDATION.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-02-SUMMARY.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-03-SUMMARY.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-04-SUMMARY.md

<interfaces>
<!-- Public SDK API exercised by the suite (from Plans 01-04): -->
voss_sdk::spawn_with(python: &str, extra_env: &[(&str,&str)]) -> Result<Supervisor, VossError>   // returns Supervisor { pub client: VossClient }
voss_sdk::VossClient::new(base: String, token: String) -> VossClient
  .create_session(cwd) -> String   .post_message(sid, text, mode) -> ()   .cost(sid) -> CostInfo
  .permission_reply(sid, id, choice) -> ()   .abort(sid) -> ()   // delete session: DELETE /session/:id (add a client method if not present, else call via reqwest)
voss_sdk::event_stream(client: VossClient, session_id: String) -> impl Stream<Item = Result<AgentEvent, VossError>>
voss_sdk::types::events::AgentEvent — ServerConnected, StreamDelta, SessionIdle, ...
voss_sdk::VossError — HttpStatus { status, detail }

<!-- venv guard (copy verbatim from over_the_wire.rs lines 18-21): -->
fn venv_python() -> Option<String> { Path::new(env!("CARGO_MANIFEST_DIR")).join("../../.venv/bin/python") ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST + auth integration (create/message/cost/delete, 401, 409) under FAKE_TURN</name>
  <files>crates/voss-sdk/tests/integration.rs</files>
  <read_first>
    - crates/voss-tui/tests/over_the_wire.rs (THE analog — `venv_python()` lines 18-21; `fake_turn_streams_over_the_wire` lines 143-199 for the spawn_with(FAKE_TURN)+create+post pattern; the skip-not-fail guard lines 30-33)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md (§"tests/integration.rs" — SDK-API adaptation: `voss_sdk::supervisor::spawn_with`, `supervisor.client`, the bad_token_yields_401 test sketch)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"Pitfall 6" — 409 is a typed error with correct status, not a failure; §"Validation Architecture / Test Map" — rest_/auth_bad_token/post_while_busy command names)
    - voss/harness/server/app.py (endpoint status codes: message→202, delete→204, 401 middleware, 409 busy)
  </read_first>
  <action>Create `crates/voss-sdk/tests/integration.rs`. Copy `venv_python()` verbatim from over_the_wire.rs (anchor `../../.venv/bin/python` resolves from crates/voss-sdk). Add the skip-not-fail guard (`let Some(python) = venv_python() else { eprintln!("skipping..."); return; };`) to EVERY `#[tokio::test]`. Write `rest_roundtrip`: `spawn_with(&python, &[("VOSS_SERVE_FAKE_TURN","1")])`, then via `supervisor.client`: `create_session(".")` → `post_message(sid, "ping", "plan")` (assert Ok — server 202) → `cost(sid)` (assert Ok CostInfo) → delete the session (assert success — server 204; if VossClient lacks a `delete_session` method, this task adds `pub async fn delete_session(&self, sid) -> Result<(), VossError>` calling DELETE /session/:id with ok_or_detail in client.rs, but PREFER it already exists — check the Plan 02 summary). `supervisor.shutdown().await` at the end. Write `auth_bad_token` (T-V13.2-09 / 401): spawn a server, build a SECOND `VossClient::new(supervisor.client base, "bad-token")` (obtain the base — if not publicly readable, capture the port by spawning and reading, or add a `pub fn base(&self) -> &str` accessor in client.rs), call `create_session(".")`, assert `Err(VossError::HttpStatus { status: 401, .. })`. Write `post_while_busy` (409): create a session, `post_message` once to start a turn, immediately `post_message` again, assert the second returns `Err(VossError::HttpStatus { status: 409, .. })` (FAKE_TURN keeps the turn briefly in-flight; if timing is flaky, open the SSE stream first to hold the turn, then post). Wrap each test body in a `tokio::time::timeout` (≤20s) so a stuck server fails fast rather than hanging.</action>
  <verify>
    <automated>cargo test -p voss-sdk --test integration -- rest_ auth_bad_token post_while_busy 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p voss-sdk --test integration` compiles and the three tests pass (or skip cleanly when `.venv/bin/python` is absent — assert by running and seeing "skipping" or pass, never a failure).
    - `grep -c 'venv_python' crates/voss-sdk/tests/integration.rs` returns ≥1 and the guard appears in every test.
    - `grep -c 'status: 401' crates/voss-sdk/tests/integration.rs` returns ≥1 and `grep -c 'status: 409' ...` returns ≥1.
    - `grep -c 'VOSS_SERVE_FAKE_TURN' crates/voss-sdk/tests/integration.rs` returns ≥1.
  </acceptance_criteria>
  <done>REST roundtrip (create/message/cost/delete) plus 401 bad-token and 409 busy-session are proven against a real FAKE_TURN server, each skipping cleanly without venv.</done>
</task>

<task type="auto">
  <name>Task 2: SSE sequence + mid-stream drop + supervisor no-orphan integration</name>
  <files>crates/voss-sdk/tests/integration.rs</files>
  <read_first>
    - crates/voss-tui/tests/over_the_wire.rs (lines 143-199 — the FAKE_TURN stream consumption + assertion shape to adapt from mpsc to the typed Stream)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"Code Examples / FAKE_TURN integration test pattern" — `event_stream(...).collect::<Result<Vec<_>,_>>()`, assert ServerConnected first / StreamDelta present / SessionIdle last; §"Pitfall 5" — server.connected first; §"Disconnect-cancels-turn" — drop ends the stream; §"Validation / Test Map" — sse_event_sequence, sse_drop_midstream, supervisor_ names)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md (§"tests/integration.rs" — typed Stream collect via `futures_util::StreamExt`)
    - crates/voss-sdk/src/stream.rs, crates/voss-sdk/src/supervisor.rs (the Wave 3 surfaces under test)
  </read_first>
  <action>Add three `#[tokio::test]`s to `crates/voss-sdk/tests/integration.rs`, each with the venv skip guard and a `tokio::time::timeout` bound. `sse_event_sequence`: spawn FAKE_TURN, create session, post message, then `event_stream(supervisor.client.clone(), sid).collect::<Result<Vec<AgentEvent>,_>>().await` (use `futures_util::StreamExt`); assert `matches!(events.first(), Some(Ok(AgentEvent::ServerConnected(_)))` (or unwrap the Vec), assert some `AgentEvent::StreamDelta(_)` present, assert `matches!(events.last(), Some(AgentEvent::SessionIdle(_)))`. `sse_drop_midstream`: open the stream, take a few items with `.next().await` (enough to be mid-turn), then `drop(stream)` explicitly; assert no panic occurs and the test completes within the timeout (the dropped reqwest Response closes the connection — PROTOCOL §8); optionally assert the server is still responsive afterward via a cheap `supervisor.client.cost(sid)` or `doctor` call (proves no leaked/poisoned state). `supervisor_no_orphan`: capture the child PID before shutdown — read it via a `pub fn pid(&self) -> Option<u32>` accessor added to Supervisor (returns `self.child.id()`), or capture all `voss serve` PIDs before/after; call `supervisor.shutdown().await` (or drop), then assert the PID is no longer alive (e.g. on unix, `kill -0 <pid>` fails / `/proc` or `ps` shows it gone; cross-platform: poll `child.try_wait()` is not available post-move, so prefer the `pid()` accessor + a `sysinfo`-free check using `std::process::Command::new("kill").args(["-0", &pid])` returning non-zero, guarded `#[cfg(unix)]`, skip the assertion on non-unix). Keep the orphan check unix-gated and skip-not-fail on other platforms.</action>
  <verify>
    <automated>cargo test -p voss-sdk --test integration -- sse_event_sequence sse_drop_midstream supervisor_ 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p voss-sdk --test integration` passes the three tests (or skips cleanly without venv).
    - `grep -c 'ServerConnected' crates/voss-sdk/tests/integration.rs` returns ≥1 and `grep -c 'SessionIdle' ...` returns ≥1 (sequence asserted).
    - `grep -c 'drop(' crates/voss-sdk/tests/integration.rs` returns ≥1 (explicit mid-stream drop).
    - The orphan check is present (`grep -cE 'pid|kill|orphan' crates/voss-sdk/tests/integration.rs` returns ≥1) and unix-gated.
    - `cargo test -p voss-sdk` (full suite) is green / cleanly skipped.
  </acceptance_criteria>
  <done>SSE sequence (connected-first/idle-last with deltas), clean mid-stream drop, and supervisor no-orphan are proven against a real FAKE_TURN server; the full suite is green or cleanly skipped without venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned `voss serve` | Tests own the server child; must reap it (no orphan) even on assertion failure |
| FAKE_TURN seam → real SSE path | Hermetic canned turn over the real event path; no provider creds in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.2-ORPHAN | Denial of Service | spawned test servers | mitigate | `supervisor_no_orphan` asserts no surviving `voss serve` PID after shutdown; every test calls shutdown/drops the supervisor; timeouts bound stuck servers. |
| T-V13.2-09 | Information Disclosure | 401 auth test | mitigate | `auth_bad_token` proves a wrong token is rejected (401) and the typed error carries status+detail only — no token echoed. |
| T-V13.2-13 | Denial of Service | mid-stream drop | mitigate | `sse_drop_midstream` proves dropping the stream ends cleanly with no panic and the server stays responsive (no leaked task). |
</threat_model>

<verification>
- `cargo test -p voss-sdk --test integration` green or cleanly skipped (no failures) without venv.
- `cargo test -p voss-sdk` (full suite incl. lib) green / skipped.
- 401, 409, ServerConnected-first/SessionIdle-last, mid-stream drop, no-orphan all asserted.
- `git diff --exit-code crates/voss-tui/` clean.
</verification>

<success_criteria>
- Integration suite covers reqs 2, 3, 5, 6 against a real `voss serve` via FAKE_TURN.
- Every test skips (not fails) without `.venv/bin/python`.
- No orphan server process survives the suite.
</success_criteria>

<output>
Create `.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-05-SUMMARY.md` when done.
</output>
