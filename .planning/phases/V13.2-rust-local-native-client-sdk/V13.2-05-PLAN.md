---
phase: V13.2-rust-local-native-client-sdk
plan: 05
type: execute
wave: 4
depends_on: [V13.2-02, V13.2-03, V13.2-04]
files_modified:
  - crates/voss-sdk/tests/integration.rs
autonomous: false
requirements: [VSDK-RS-02, VSDK-RS-03, VSDK-RS-05, VSDK-RS-06, VSDK-RS-07]
user_setup: []

must_haves:
  truths:
    - "An integration suite spawns a real `voss serve` under VOSS_SERVE_FAKE_TURN=1 and exercises REST, SSE, auth, and supervisor against it"
    - "REST path: create session -> post message (202) -> read cost -> delete session (204) succeeds"
    - "A wrong bearer token yields VossError::HttpStatus { status: 401 } and a message during a running turn yields status 409"
    - "SSE: the stream yields ServerConnected first and SessionIdle last; dropping the stream mid-turn ends it without panic or leaked task"
    - "Supervisor: after shutdown/drop, no orphan `voss serve` process survives"
    - "A permission round-trip test covers permission.updated -> permission_reply(allow/deny) -> subsequent tool event state (real-turn, skip-not-fail on missing provider creds, since FAKE_TURN emits no permission gate)"
    - "Every integration test SKIPS (does not fail) when .venv/bin/python is absent, and the permission test additionally SKIPS when no provider credentials are available"
  artifacts:
    - path: "crates/voss-sdk/tests/integration.rs"
      provides: "VOSS_SERVE_FAKE_TURN integration suite covering reqs 2/3/5/6 + permission round-trip (req 2 AC)"
      contains: "VOSS_SERVE_FAKE_TURN"
  key_links:
    - from: "crates/voss-sdk/tests/integration.rs"
      to: "voss_sdk public API"
      via: "spawn_with + VossClient + event_stream + permission_reply"
      pattern: "voss_sdk::"
    - from: "crates/voss-sdk/tests/integration.rs"
      to: ".venv/bin/python"
      via: "venv_python() skip-not-fail guard"
      pattern: "venv_python"
---

<objective>
Build the integration test harness (VSDK-RS-07) that proves the SDK works against a real `voss serve`, covering REST (req 2, incl. delete + the permission round-trip AC), SSE (req 3), handshake/bearer auth (req 5), and supervisor lifecycle (req 6) using the `VOSS_SERVE_FAKE_TURN` seam so CI needs no provider credentials. Every test skips cleanly when `.venv/bin/python` is absent, matching the `voss-tui` `over_the_wire.rs` precedent; the permission round-trip additionally skips when no provider creds are available (the FAKE_TURN seam emits no permission gate — verified in app.py).

Purpose: This is the faithful end-to-end proof of the handshake, Bearer auth, SSE framing, child supervision, and the permission allow/deny protocol — the SPEC acceptance criteria that unit tests cannot cover. It also exercises the public API surface exactly as a downstream consumer would.

Output: `crates/voss-sdk/tests/integration.rs` with the REST/auth/SSE/supervisor/permission tests; `cargo test -p voss-sdk --test integration` green (or cleanly skipped without venv/creds).
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-SPEC.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-VALIDATION.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-02-SUMMARY.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-03-SUMMARY.md
@.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-04-SUMMARY.md

<interfaces>
<!-- Public SDK API exercised by the suite (from Plans 01-04): -->
voss_sdk::spawn_with(python: &str, extra_env: &[(&str,&str)]) -> Result<Supervisor, VossError>   // returns Supervisor { pub client: VossClient }
voss_sdk::VossClient::new(base: String, token: String) -> VossClient
  .create_session(cwd) -> String   .post_message(sid, text, mode) -> ()   .cost(sid) -> CostInfo
  .delete_session(sid) -> ()   // GUARANTEED by Plan 02 Task 1 (DELETE /session/:id, 204) — no conditional patch here
  .permission_reply(sid, id, choice) -> ()   .abort(sid) -> ()
voss_sdk::event_stream(client: VossClient, session_id: String) -> impl Stream<Item = Result<AgentEvent, VossError>>
voss_sdk::types::events::AgentEvent — ServerConnected, StreamDelta, SessionIdle, PermissionUpdated, ToolEvent, ...
voss_sdk::VossError — HttpStatus { status, detail }

<!-- venv guard (copy verbatim from over_the_wire.rs lines 18-21): -->
fn venv_python() -> Option<String> { Path::new(env!("CARGO_MANIFEST_DIR")).join("../../.venv/bin/python") ... }

<!-- VERIFIED in voss/harness/server/app.py: the VOSS_SERVE_FAKE_TURN branch of `_run_turn` (lines 166-178)
     emits ONLY user/thinking/plan/stream.delta/stream.finalize/final/session.idle — it does NOT install a
     PermissionGate and emits NO `permission.updated` event. Permission gates fire only on the real-turn path
     (line 188 `_install_server_permissions`), which requires provider creds + a tool call that trips a gate.
     => the permission round-trip AC (SPEC req 2) cannot be covered hermetically by FAKE_TURN today; cover it
     via a real-turn test gated on provider creds with skip-not-fail, and flag the FAKE_TURN seam as a candidate
     for a server-side `VOSS_SERVE_FAKE_TURN_PERMISSION`-style extension (out of THIS phase's scope — no PROTOCOL/
     server edits per CONTEXT fences). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST + auth integration (create/message/cost/delete, 401, 409) under FAKE_TURN</name>
  <files>crates/voss-sdk/tests/integration.rs</files>
  <read_first>
    - crates/voss-tui/tests/over_the_wire.rs (THE analog — `venv_python()` lines 18-21; `fake_turn_streams_over_the_wire` lines 143-199 for the spawn_with(FAKE_TURN)+create+post pattern; the skip-not-fail guard lines 30-33)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md (§"tests/integration.rs" — SDK-API adaptation: `voss_sdk::supervisor::spawn_with`, `supervisor.client`, the bad_token_yields_401 test sketch)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"Pitfall 6" — 409 is a typed error with correct status, not a failure; §"Validation Architecture / Test Map" — rest_/auth_bad_token/post_while_busy command names)
    - voss/harness/server/app.py (endpoint status codes: message→202, delete→204, 401 middleware, 409 busy)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-02-SUMMARY.md (confirm `delete_session` shipped in Plan 02)
  </read_first>
  <action>Create `crates/voss-sdk/tests/integration.rs`. Copy `venv_python()` verbatim from over_the_wire.rs (anchor `../../.venv/bin/python` resolves from crates/voss-sdk). Add the skip-not-fail guard (`let Some(python) = venv_python() else { eprintln!("skipping..."); return; };`) to EVERY `#[tokio::test]`. Write `rest_roundtrip`: `spawn_with(&python, &[("VOSS_SERVE_FAKE_TURN","1")])`, then via `supervisor.client`: `create_session(".")` → `post_message(sid, "ping", "plan")` (assert Ok — server 202) → `cost(sid)` (assert Ok CostInfo) → `delete_session(sid)` (assert Ok — server 204; this method is GUARANTEED present from Plan 02 Task 1, call it directly — do NOT add it here). `supervisor.shutdown().await` at the end. Write `auth_bad_token` (T-V13.2-09 / 401): spawn a server, build a SECOND `VossClient::new(<base>, "bad-token")` (obtain the base via the `pub(crate) fn base()` accessor added in Plan 02, or capture the port from the handshake), call `create_session(".")`, assert `Err(VossError::HttpStatus { status: 401, .. })`. Write `post_while_busy` (409): create a session, `post_message` once to start a turn, immediately `post_message` again, assert the second returns `Err(VossError::HttpStatus { status: 409, .. })` (FAKE_TURN keeps the turn briefly in-flight; if timing is flaky, open the SSE stream first to hold the turn, then post). Wrap each test body in a `tokio::time::timeout` (≤20s) so a stuck server fails fast rather than hanging.</action>
  <verify>
    <automated>cargo test -p voss-sdk --test integration -- rest_ auth_bad_token post_while_busy 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p voss-sdk --test integration` compiles and the three tests pass (or skip cleanly when `.venv/bin/python` is absent — assert by running and seeing "skipping" or pass, never a failure).
    - `grep -c 'venv_python' crates/voss-sdk/tests/integration.rs` returns ≥1 and the guard appears in every test.
    - `grep -c 'delete_session' crates/voss-sdk/tests/integration.rs` returns ≥1 (rest_roundtrip deletes the session — req 2 delete AC).
    - `grep -c 'status: 401' crates/voss-sdk/tests/integration.rs` returns ≥1 and `grep -c 'status: 409' ...` returns ≥1.
    - `grep -c 'VOSS_SERVE_FAKE_TURN' crates/voss-sdk/tests/integration.rs` returns ≥1.
  </acceptance_criteria>
  <done>REST roundtrip (create/message/cost/delete) plus 401 bad-token and 409 busy-session are proven against a real FAKE_TURN server, each skipping cleanly without venv; delete_session is called (not defined) here.</done>
</task>

<task type="auto">
  <name>Task 2: SSE sequence + mid-stream drop + supervisor no-orphan integration</name>
  <files>crates/voss-sdk/tests/integration.rs</files>
  <read_first>
    - crates/voss-tui/tests/over_the_wire.rs (lines 143-199 — the FAKE_TURN stream consumption + assertion shape to adapt from mpsc to the typed Stream)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"Code Examples / FAKE_TURN integration test pattern" — `event_stream(...).collect::<Result<Vec<_>,_>>()`, assert ServerConnected first / StreamDelta present / SessionIdle last; §"Pitfall 5" — server.connected first; §"Disconnect-cancels-turn" — drop ends the stream; §"Validation / Test Map" — sse_event_sequence, sse_drop_midstream, supervisor_ names)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-PATTERNS.md (§"tests/integration.rs" — typed Stream collect via `futures_util::StreamExt`)
    - crates/voss-sdk/src/stream.rs, crates/voss-sdk/src/supervisor.rs (the Wave 3 surfaces under test)
  </read_first>
  <action>Add three `#[tokio::test]`s to `crates/voss-sdk/tests/integration.rs`, each with the venv skip guard and a `tokio::time::timeout` bound. `sse_event_sequence`: spawn FAKE_TURN, create session, post message, then `event_stream(supervisor.client.clone(), sid).collect::<Result<Vec<AgentEvent>,_>>().await` (use `futures_util::StreamExt`); assert `matches!(events.first(), Some(Ok(AgentEvent::ServerConnected(_)))` (or unwrap the Vec), assert some `AgentEvent::StreamDelta(_)` present, assert `matches!(events.last(), Some(AgentEvent::SessionIdle(_)))`. `sse_drop_midstream`: open the stream, take a few items with `.next().await` (enough to be mid-turn), then `drop(stream)` explicitly; assert no panic occurs and the test completes within the timeout (the dropped reqwest Response closes the connection — PROTOCOL §8); optionally assert the server is still responsive afterward via a cheap `supervisor.client.cost(sid)` or `doctor` call (proves no leaked/poisoned state). `supervisor_no_orphan`: capture the child PID before shutdown — read it via a `pub fn pid(&self) -> Option<u32>` accessor added to Supervisor (returns `self.child.id()`), or capture all `voss serve` PIDs before/after; call `supervisor.shutdown().await` (or drop), then assert the PID is no longer alive (e.g. on unix, `kill -0 <pid>` fails / `/proc` or `ps` shows it gone; cross-platform: poll `child.try_wait()` is not available post-move, so prefer the `pid()` accessor + a check using `std::process::Command::new("kill").args(["-0", &pid])` returning non-zero, guarded `#[cfg(unix)]`, skip the assertion on non-unix). Keep the orphan check unix-gated and skip-not-fail on other platforms.</action>
  <verify>
    <automated>cargo test -p voss-sdk --test integration -- sse_event_sequence sse_drop_midstream supervisor_ 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p voss-sdk --test integration` passes the three tests (or skips cleanly without venv).
    - `grep -c 'ServerConnected' crates/voss-sdk/tests/integration.rs` returns ≥1 and `grep -c 'SessionIdle' ...` returns ≥1 (sequence asserted).
    - `grep -c 'drop(' crates/voss-sdk/tests/integration.rs` returns ≥1 (explicit mid-stream drop).
    - The orphan check is present (`grep -cE 'pid|kill|orphan' crates/voss-sdk/tests/integration.rs` returns ≥1) and unix-gated.
    - `cargo test -p voss-sdk` (full suite) is green / cleanly skipped.
  </acceptance_criteria>
  <done>SSE sequence (connected-first/idle-last with deltas), clean mid-stream drop, and supervisor no-orphan are proven against a real FAKE_TURN server; the full suite is green or cleanly skipped without venv.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Permission round-trip (allow/deny → tool event) via real turn, skip-not-fail on creds (SPEC req 2 AC)</name>
  <read_first>
    - voss/harness/server/app.py (VERIFIED: `_run_turn` FAKE_TURN branch lines 166-178 emits NO permission gate; `_install_server_permissions` at line 188 runs only on the real-turn path; the `permission.updated` event payload `{id, tool_name, args, dimension}` line 133; `POST /session/:id/permission` reply route line 379)
    - voss/harness/server/events.py (PermissionUpdated lines 43-49 + ToolEvent lines 83-89 — the `state` field the assertion checks)
    - crates/voss-tui/tests/over_the_wire.rs (lines 27-90 — `real_turn_over_the_wire` `#[ignore]` + skip-not-fail-on-no-creds pattern: `create_session` Err → eprintln skip + shutdown + return)
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-SPEC.md (Acceptance: "Permission helper unblocks a gate with an allow reply and blocks with a deny reply (verified via the next `tool` event state)")
    - .planning/phases/V13.2-rust-local-native-client-sdk/V13.2-RESEARCH.md (§"code_context"/permission round-trip PROTOCOL §7: `permission.updated {id,tool_name,args,dimension}` → reply choice ∈ `a|A|d`)
    - crates/voss-sdk/src/client.rs (`permission_reply(sid,id,choice)` — GUARANTEED present from Plan 02; this task only CALLS it)
  </read_first>
  <what-built>
    The SPEC req-2 acceptance "permission helper unblocks with allow / blocks with deny, verified via the next `tool` event state" cannot be exercised by the hermetic FAKE_TURN seam: the FAKE_TURN branch of `_run_turn` (app.py lines 166-178) emits no `permission.updated` event and installs no `PermissionGate`. Faithful coverage therefore requires a REAL turn that trips a tool gate (e.g. a tool call requiring confirmation), gated on provider credentials and skipped-not-failed when absent — mirroring `over_the_wire.rs::real_turn_over_the_wire`. The autonomous executor writes the test but cannot guarantee provider creds in CI; a human confirms the test passes in a creds-available environment (or accepts the documented skip + the FAKE_TURN-extension follow-up). The FAKE_TURN seam may need a server-side `VOSS_SERVE_FAKE_TURN_PERMISSION`-style extension to make this hermetic — that is OUT of THIS phase's scope (no server/PROTOCOL edits per CONTEXT fences) and is recorded as a follow-up.
  </what-built>
  <how-to-verify>
    1. Executor writes `permission_roundtrip` as a `#[tokio::test]` in `crates/voss-sdk/tests/integration.rs` (tests-only; `permission_reply` already exists in client.rs — files_modified stays tests-only): venv-guard, then `spawn_with(&python, &[])` (NO FAKE_TURN — real path), `create_session(".")`; if that errors (no creds), `eprintln!("skipping permission roundtrip: no provider credentials")`, shutdown, return (skip-not-fail). Otherwise post a message whose execution trips a tool-permission gate; open `event_stream`, collect until an `AgentEvent::PermissionUpdated { id, .. }` is seen, call `supervisor.client.permission_reply(sid, &id, "a")` (allow), and assert a subsequent `AgentEvent::ToolEvent` shows the tool proceeded (state `"ok"`/`"pending"` not blocked). If feasible in the same or a sibling test, exercise the deny path with choice `"d"` and assert the tool is blocked. Bound with a `tokio::time::timeout`.
    2. Human runs `cargo test -p voss-sdk --test integration -- permission_roundtrip -- --nocapture` in a credentials-available environment (e.g. Claude OAuth in keychain) and confirms the allow path unblocks the gate and the tool event reflects it.
    3. If no creds environment is available, human accepts the documented skip + records the FAKE_TURN-permission-extension follow-up.
  </how-to-verify>
  <action>Add a `permission_roundtrip` integration test (tests-only; `permission_reply` is reused from Plan 02 — do NOT modify client.rs, so `files_modified` stays `tests/integration.rs` only) that drives a REAL turn tripping a permission gate, receives `AgentEvent::PermissionUpdated`, replies allow (`"a"`) via `permission_reply`, and asserts the subsequent `tool` event state reflects the allow; cover the deny (`"d"`) path too if feasible. Gate the whole test on provider creds with the `over_the_wire.rs` skip-not-fail pattern (create_session Err → skip). Add a top-of-test comment documenting that FAKE_TURN emits no permission gate (app.py 166-178) and that a hermetic version needs a server-side FAKE_TURN-permission seam (follow-up, out of scope). Human verifies the allow/deny behavior in a creds-available run, or accepts the documented skip.</action>
  <acceptance_criteria>
    - `crates/voss-sdk/tests/integration.rs` contains a `permission_roundtrip` test: `grep -cE 'permission_roundtrip|permission_reply|PermissionUpdated' crates/voss-sdk/tests/integration.rs` returns ≥1.
    - The test compiles and runs: `cargo test -p voss-sdk --test integration -- permission_roundtrip` either PASSES (creds present, allow unblocks + tool event reflects it, deny blocks if exercised) or SKIPS cleanly (no creds / no venv) — never fails.
    - A comment records that FAKE_TURN emits no permission gate (app.py 166-178) and the FAKE_TURN-permission server extension is a documented follow-up.
    - `files_modified` is tests-only (client.rs unchanged — `git diff --exit-code crates/voss-sdk/src/client.rs` clean relative to Plan 02 output).
  </acceptance_criteria>
  <verify><human-check>In a provider-credentials environment, the operator runs `cargo test -p voss-sdk --test integration -- permission_roundtrip -- --nocapture` and confirms the allow reply unblocks the gate (tool event proceeds) and the deny reply blocks it; OR the operator accepts the documented skip + the FAKE_TURN-permission-extension follow-up.</human-check></verify>
  <done>The SPEC permission allow/deny → tool-event AC is covered by a real-turn round-trip test (skip-not-fail on creds), the FAKE_TURN-no-gate fact and follow-up are documented, and the AC is not silently dropped.</done>
  <resume-signal>Type "approved" after confirming the allow/deny behavior in a creds run, or "accept-skip" to accept the documented skip + follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned `voss serve` | Tests own the server child; must reap it (no orphan) even on assertion failure |
| FAKE_TURN seam → real SSE path | Hermetic canned turn over the real event path; no provider creds in CI (but no permission gate — see Task 3) |
| real-turn permission gate → SDK | permission.updated event + reply choice cross the boundary; allow/deny must drive the gate correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.2-ORPHAN | Denial of Service | spawned test servers | mitigate | `supervisor_no_orphan` asserts no surviving `voss serve` PID after shutdown; every test calls shutdown/drops the supervisor; timeouts bound stuck servers. |
| T-V13.2-09 | Information Disclosure | 401 auth test | mitigate | `auth_bad_token` proves a wrong token is rejected (401) and the typed error carries status+detail only — no token echoed. |
| T-V13.2-13 | Denial of Service | mid-stream drop | mitigate | `sse_drop_midstream` proves dropping the stream ends cleanly with no panic and the server stays responsive (no leaked task). |
| T-V13.2-17 | Elevation of Privilege | permission allow/deny round-trip | mitigate | `permission_roundtrip` proves an allow reply only proceeds the gated tool and a deny reply blocks it (verified via the next tool event state) — the SDK cannot bypass the server-side gate. |
</threat_model>

<verification>
- `cargo test -p voss-sdk --test integration` green or cleanly skipped (no failures) without venv; `permission_roundtrip` additionally skips without creds.
- `cargo test -p voss-sdk` (full suite incl. lib) green / skipped.
- 401, 409, delete_session call, ServerConnected-first/SessionIdle-last, mid-stream drop, no-orphan, permission allow/deny all asserted (permission via real turn, human-verified or accept-skip).
- `git diff --exit-code crates/voss-tui/` clean; `crates/voss-sdk/src/client.rs` unchanged by this plan.
</verification>

<success_criteria>
- Integration suite covers reqs 2 (incl. delete + permission allow/deny), 3, 5, 6 against a real `voss serve` via FAKE_TURN, with permission covered by a real-turn test.
- Every test skips (not fails) without `.venv/bin/python`; permission test also skips without creds.
- No orphan server process survives the suite; the FAKE_TURN-no-permission-gate fact + follow-up are documented (AC not silently dropped).
</success_criteria>

<output>
Create `.planning/phases/V13.2-rust-local-native-client-sdk/V13.2-05-SUMMARY.md` when done. Record whether the permission round-trip was human-verified in a creds run or accepted as a documented skip + FAKE_TURN-extension follow-up.
</output>
