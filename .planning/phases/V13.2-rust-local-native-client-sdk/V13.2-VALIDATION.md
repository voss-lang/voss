---
phase: V13.2
slug: rust-local-native-client-sdk
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-07
---

# Phase V13.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust built-in) |
| **Config file** | none — workspace default; Wave 0 adds `crates/voss-sdk` to root `Cargo.toml` members |
| **Quick run command** | `cargo test -p voss-sdk --lib` |
| **Full suite command** | `cargo test -p voss-sdk` |
| **Estimated runtime** | ~30–90 seconds (integration spawns a real `voss serve` via `VOSS_SERVE_FAKE_TURN`) |

---

## Sampling Rate

- **After every task commit:** Run `cargo build -p voss-sdk` + `cargo test -p voss-sdk --lib`
- **After every plan wave:** Run `cargo test -p voss-sdk`
- **Before `/gsd-verify-work`:** Full suite green + `git diff --exit-code crates/voss-tui/` (zero-diff AC) + codegen drift gate green
- **Max feedback latency:** ~90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| V13.2-01-* | 01 | 0 | scaffold | — | N/A | build | `cargo build -p voss-sdk` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-01 | — | N/A | unit+drift | `cargo test -p voss-sdk -- codegen_is_current` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-01 | — | N/A | compile | `cargo build -p voss-sdk` (exhaustive `match` in a test) | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-02 | — | N/A | integration | `cargo test -p voss-sdk --test integration -- rest_` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-02 | T-09 (token leak) | 401 surfaces as typed error; token never logged | integration | `cargo test -p voss-sdk --test integration -- auth_bad_token` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-02 | — | 409 busy → typed error | integration | `cargo test -p voss-sdk --test integration -- post_while_busy` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-03 | — | N/A | integration | `cargo test -p voss-sdk --test integration -- sse_event_sequence` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-03 | — | drop cancels turn, no leak | integration | `cargo test -p voss-sdk --test integration -- sse_drop_midstream` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-04 | — | N/A | unit | `cargo test -p voss-sdk --lib -- projection` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-04 | — | voss-tui untouched | manual/CI | `git diff --exit-code crates/voss-tui/` | — | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-05 | T-09 (token leak) | handshake parse; bearer attached; token redacted in Debug | unit+integration | `cargo test -p voss-sdk -- handshake_ auth_bearer` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-06 | T-orphan (DoS) | no orphan PID on drop/shutdown | integration | `cargo test -p voss-sdk --test integration -- supervisor_` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-06 | — | bad interpreter → typed spawn error | unit | `cargo test -p voss-sdk --lib -- bad_interpreter` | ❌ W0 | ⬜ pending |
| V13.2-*-* | — | — | VSDK-RS-07 | — | N/A | build+suite | `cargo test -p voss-sdk` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/voss-sdk/Cargo.toml` + root `Cargo.toml` `members += "crates/voss-sdk"`
- [ ] `crates/voss-sdk/src/types/events.rs` — typify output from `contracts/events.schema.json` (HARD-BLOCKS on V13.1) OR a `// PLACEHOLDER` stub matching the 21-member structure to unblock the rest of the crate
- [ ] `crates/voss-sdk/src/types/rest.rs` — generated/hand-written REST bodies
- [ ] `crates/voss-sdk/tests/integration.rs` — `VOSS_SERVE_FAKE_TURN` harness (copy pattern from `crates/voss-tui/tests/over_the_wire.rs`)
- [ ] `cargo install cargo-typify --locked` — for the drift gate (checkpoint:human-verify before add)
- [ ] **Coordination gate:** confirm V13.1's exporter adds `"type"` to each event variant's `required` array (else typify emits `#[serde(untagged)]` — Pitfall 1). If V13.1 hasn't shipped, Wave 0 stubs types and a Wave-N task replaces them with typify output.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `git diff crates/voss-tui/` empty | VSDK-RS-04 | Cross-crate invariant, not a unit test | Run `git diff --exit-code crates/voss-tui/` at phase close; must exit 0 |
| typify emits `#[serde(tag = "type")]` not `untagged` | VSDK-RS-01 | Depends on V13.1 schema fix; inspect generated file | `grep 'serde(tag = "type")' crates/voss-sdk/src/types/events.rs` |

*Integration tests skip (not fail) when `.venv/bin/python` is absent — same as the voss-tui over_the_wire.rs precedent.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (entire crate is greenfield)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
