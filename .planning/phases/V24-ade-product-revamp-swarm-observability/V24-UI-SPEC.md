---
phase: V24
slug: ade-product-revamp-swarm-observability
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-14
---

# Phase V24 — UI Design Contract
# ADE Product Revamp + Swarm Observability

> Visual and interaction contract for V24 frontend implementation.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> **Sources used:** V24-SPEC.md (VADE2-01..08), V24-CONTEXT.md (D-01..D-11),
> ROADMAP.md §V24, .planning/sketches/MANIFEST.md, apps/voss-app/CONCEPT.md,
> apps/voss-app/FEATURES.md, src/styles/variant-b.css, src/index.css,
> src/org/cockpit/cockpitStyles.css, src/org/orgStyles.css,
> src/components/sidebar/sidebar.css, src/themes/schema.ts.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (Tailwind v4 + custom CSS vars) | Detected — no components.json |
| Preset | not applicable | n/a |
| Component library | none (hand-rolled SolidJS components) | Existing codebase |
| Icon library | `lucide-solid` for portal rail nav/toggle icons; existing Unicode glyphs remain for composer/replay/review affordances (`❯` `⏵` `※` `⊕`) | V24-09 + MANIFEST.md + FEATURES.md L2.3.2 |
| Mono font | JetBrains Mono (primary), SF Mono / Menlo fallback | `--font-mono` in variant-b.css |
| UI font | Inter (secondary — labels, section heads) | `--font-ui` in variant-b.css |
| Display font | Poppins 500/600 (section labels only) | sidebar.css, cockpitStyles.css |

**Registry safety gate:** not applicable. No shadcn, no third-party registries.

---

## Spatial Model

This is the most important structural decision in V24 and governs every surface layout.

### Canvas-Swap (D-01)

The app has exactly two z-layers that can occupy the main canvas:

1. **Terminal grid** (`GridRoot`) — always mounted, never torn down, `display:none` when a portal surface is active
2. **Portal surface** — takes the canvas via `position:absolute; inset:0` over the hidden grid

Switching surfaces: instant opacity swap (no slide, no push — the grid restores immediately). There is no persistent split between portal and grid.

### Layout Zones

```
┌─────────────────────────────────────────────────────────────────┐
│ TOP CHROME (28px)                                               │
│  [project name]   [command palette]   [mode chip]  [status]     │
├───────┬─────────────────────────────────────────────────────────┤
│ LEFT  │                                                         │
│ PORTAL│   MAIN CANVAS                                           │
│ (48px │   (terminal grid OR portal surface — canvas-swap)       │
│  icon │                                                         │
│  rail │                                                         │
│  OR   │                                                         │
│ 220px │                                                         │
│ icon +│                                                         │
│ name) │                                                         │
│       │                                                         │
├───────┤                                                         │
│       │                                                         │
│ [ask] │                                                         │
│       │                                                         │
└───────┴─────────────────────────────────────────────────────────┘
```

- **Top chrome:** 28px fixed height. Background `--bg-1`. Bottom border `1px solid --border`.
- **Left portal rail:** collapsible 48px icon-only width or 220px expanded icon + name width. Background `--bg-1`. Right border `1px solid --border`. Contains: 9 portal items (stacked, Workspaces first), and at rail bottom the "Ask Voss to…" icon trigger.
- **Main canvas:** fills the remaining area. `background: --bg-0`.

---

## Spacing Scale

Declared values — all are in the standard 8-point set. Pulled from existing patterns in cockpitStyles.css and sidebar.css.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, inline padding, kv cell inset |
| sm | 8px | Compact list row padding, gap between related controls |
| lg | 16px | Default element vertical rhythm, mission-control row horizontal padding |
| xl | 24px | Section breaks, empty-state padding |
| 2xl | 32px | Major layout gaps |
| 3xl | 48px | Portal rail icon touch target (also listed under exceptions as a geometry constant) |

All main-scale values are members of the standard set {4, 8, 16, 24, 32, 48, 64}.

**Exceptions (off-scale values, each justified):**

- **Panel section padding: 12px** — existing cockpit pattern (`padding: 12px` from cockpitStyles.css `.cockpit-dhdr` / `.cockpit-dsec` / `.cockpit-drawer__title`) — preserves visual continuity with V14 cockpit components. Used for: drawer/panel section insets, surface-header internal padding, and the Swarm Map legend panel. Where it does not break V14 continuity (new mission-control rows, composer fields), prefer the on-scale `sm` (8px) / `lg` (16px) tokens instead.
- **Left portal icon buttons: 48px** tall touch target (a fixed rail geometry, not a layout spacing token; ensures each nav item is tap-safe at density). Derived from sidebar header `height: 44px` pattern; rounded to 48px for 8-point alignment. (Numerically equals the `3xl` token but is consumed as a geometry constant.)
- **Status dot indicators: 6–8px** diameter (fixed glyph size, not spacing).
- **Pane header height: 22px** (existing A-track lock from MANIFEST.md and CONCEPT.md — do NOT change).
- **Top chrome height: 28px** (existing V14 titlebar convention — extend, do not change).
- **Timeline scrubber thumb: 12px** diameter.

---

## Typography

All font tokens already exist in `--font-mono`, `--font-ui`, `--font-display` (variant-b.css / index.css). V24 adds no new fonts.

The system uses three weights total (400 / 500 / 600), but **each surface uses at most two** — see the per-surface enforcement column and notes below.

| Role | Font | Size | Weight | Line Height | Used by surface(s) |
|------|------|------|--------|-------------|--------------------|
| Terminal / mono body | `--font-mono` | 13px | 400 | 1.5 | Terminal grid, Swarm Map node IDs, all numeric values |
| UI body | `--font-ui` | 12px | 400 | 1.4 | Mission-control rows, drawer body, descriptions |
| UI label / chip | `--font-ui` | 11px | 500 | 1.2 | Status chips, surface-header controls, button text, kv keys |
| Display / section heading | `--font-display` (Poppins) | 11px | 600 | 1.2 | Section group headings (ALL CAPS, letter-spacing 0.08em) |
| Drawer title | `--font-display` (Poppins) | 14px | 600 | 1.2 | Card/step drawer title (reuses cockpit-drawer__title) |
| Task title (Overview/Tasks list) | `--font-ui` | 13px | 500 | 1.3 | Primary Task name in a mission-control row |

**Per-surface weight pairing (each surface uses exactly two weights):**

| Surface | Allowed weights | What gets which |
|---------|-----------------|----------------|
| Terminal grid / pane body | 400 + 500 | 400 body text; 500 for the rare pane-header emphasis |
| Mission-control rows (Overview/Tasks/Agents row body) | 400 + 500 | 400 metadata/body; 500 the Task name |
| Section/group headings (any surface) | 400 + 600 | 400 surrounding body; 600 the ALL-CAPS Poppins heading |
| Composer | 400 + 600 | 400 ask field + labels; 600 the "Create Task" CTA |
| Drawer (reused cockpit) | 400 + 600 | 400 body/kv values; 600 the 14px drawer title |
| Swarm Map legend panel | 400 + 500 | 400 kv values; 500 the selected-node header |

**Rules:**

- Only 4 sizes in active use across the app: 11 / 12 / 13 / 14. (Terminal pane body uses the 13px baseline set by `html, body` in index.css.)
- No single surface mixes more than two weights. The 600 weight is reserved for Poppins display headings and the primary CTA; the 500 weight is reserved for inline emphasis (labels, Task names). 400 is the default everywhere.
- Mono font is used for: all numeric values (cost, token counts, run IDs, timestamps), glyph-prefixed terminal lines, Swarm Map node IDs.
- Inter (UI font) is used for: human-readable labels, status text, Task names.
- Poppins (display font) is used only for section headings (the existing `.cockpit-sect` / `.sidebar-section-label` pattern) and the 14px drawer title.

---

## Color

Reuse all tokens from `src/styles/variant-b.css`. V24 declares no new hex values. Use only `color-mix()` for tints (existing pattern in cockpitStyles.css).

### Surface Hierarchy (60 / 30 / 10)

| Role | Token | Approx share | Usage |
|------|-------|--------------|-------|
| Dominant | `--bg-0` (`#0a0b0e`) | ~60% | Terminal canvas, main panel background, Swarm Map canvas |
| Secondary | `--bg-1` (`#11131a`) | ~30% | Top chrome, left portal rail, cockpit sidebar, drawer, timeline rail |
| Card layer | `--bg-2` (`#171a23`) | surface within secondary | Mission-control row hover, kv cells, Swarm Map node fills |
| Raised card | `--bg-3` (`#1f232e`) | used sparingly | Dropdowns, tooltips, Swarm Map cluster overlay |

### Foreground Scale

| Token | Value | Usage |
|-------|-------|-------|
| `--fg-0` | `#e8eaf0` | Primary text: Task names, active nav items, drawer titles |
| `--fg-1` | `#aab0c0` | Secondary text: row metadata, body descriptions, step copy |
| `--fg-2` | `#6a7080` | Tertiary: timestamps, cost labels, inactive tab text |
| `--fg-3` | `#444a5a` | Muted: empty-state placeholders, disabled states, group separators |

### Accent Allocation (10%)

Accents are reserved for specific semantic roles ONLY. Do NOT apply accents to decorative elements.

| Token | Hex | Reserved for |
|-------|-----|-------------|
| `--focus` | `#5a7cff` | Keyboard focus rings (`outline: 1px solid --focus`), active portal nav item left-border, primary CTA button fill, Swarm Map edge for active/live delegation |
| `--accent-green` | `#6fd28f` | Status: done / adopted / passing / accepted; Swarm Map "done" node ring; diff `+` line tint; composer "Ask Voss to…" send button on hover |
| `--accent-amber` | `#e8b86c` | Status: reviewing / pending / in-review; reviewer glyph `※`; attention queue warning; Swarm Map review/validation edge |
| `--accent-red` | `#e87b7b` | Status: blocked / failed / error / destructive; Swarm Map blocker edge; budget-exceeded chip; destructive confirm button; diff `-` line tint |
| `--accent-cyan` | `#6cc7d4` | Status: active / live / streaming; live chip dot; Swarm Map live edge pulse; in-progress node ring |
| `--accent-magenta` | `#c084d4` | Tool-call glyph `⏵` color; Swarm Map tool-call edge; DSL hot-reload badge |
| `--accent-blue` | `#7aa2ff` | User message glyph `❯` color; Swarm Map message edge |

### Border Tokens

| Token | Usage |
|-------|-------|
| `--border` (`#262b38`) | All structural dividers: chrome, panels, list separators |
| `--border-bright` (`#353b4a`) | Hover/active dividers, focus-adjacent borders |

### Semantic Color Notes

- **Safety mode chips:** "Read only" = `--fg-3` text on `--bg-3` (intentionally muted — it is the safe default, not an alarm). "Can edit" = `--accent-amber` text. "Autopilot" = `--accent-red` text (highest risk, most salient).
- **Swarm Map placeholder nodes** (missing signal): `--bg-3` fill, `--border-bright` ring, `--fg-3` dashed ring (2px dashed). No accent color. Must be visually distinct from real nodes without being distracting.
- **Honest-signal contract:** if a node or edge has no real source, it renders in the placeholder style or is omitted entirely. Never use a color that implies "this is live data" on a fabricated signal.

---

## Component Inventory

New components introduced in V24. Existing components reused as-is are noted but not re-specified.

### 1. Left Portal Rail (`PortalRail`)

**Purpose:** Primary navigation for the 9 portal items, with **Workspaces** as the explicit L1 home for the persistent terminal/tmux grid.

**Geometry:**
- Collapsed width: 48px (icon-only)
- Expanded width: 220px (icon + name)
- Background: `--bg-1`
- Right border: `1px solid --border`
- Item height: 48px (touch target)
- Item width: 100% of the current rail width
- Expansion pushes the main canvas as a flex sibling. It must not overlap or float over the canvas.
- Expanded/collapsed state is App-owned and persisted in `localStorage` at `voss:portalExpanded`.

**Collapse / expand control:**
- A top rail toggle flips between collapsed and expanded.
- Collapsed state: `aria-label="Expand portal"` and `aria-expanded="false"`.
- Expanded state: `aria-label="Collapse portal"` and `aria-expanded="true"`.
- Toggle icons use `lucide-solid` `PanelLeftOpen` / `PanelLeftClose`, size 20, `currentColor`.

**Item states:**
- Default: icon centered in collapsed mode; icon + label left-aligned with 12px gap in expanded mode; `--fg-3` color
- Hover: `background: --bg-2`, icon `--fg-1`
- Active (current surface): `box-shadow: inset 2px 0 0 --focus` on the left edge (matching the `.agent-item--active` / sidebar pattern), icon `--fg-0`
- Focus-visible: `outline: 1px solid --focus; outline-offset: -1px` (matching `.cockpit-sidebar:focus-visible` pattern)

**Items (top to bottom):**
1. Workspaces — `lucide-solid` icon: `LayoutGrid`; routes to `activeView='grid'` and the already-mounted `GridRoot` canvas via canvas-swap (not a new surface)
2. Overview — `lucide-solid` icon: `LayoutDashboard`
3. Tasks — `lucide-solid` icon: `ListChecks`
4. Agents — `lucide-solid` icon: `Bot`
5. Swarm Map — `lucide-solid` icon: `Network`
6. Review — `lucide-solid` icon: `ClipboardCheck`
7. Context — `lucide-solid` icon: `FileText`
8. Memory — `lucide-solid` icon: `Brain`
9. Settings — `lucide-solid` icon: `Settings`

**Icon contract:** portal item icons render as `lucide-solid` SVGs at `size={20}`, `color="currentColor"`, and consistent stroke weight. Raw Unicode glyphs are no longer the primary portal nav icon implementation.

**Surface wiring (V24-10 — closes V24-SPEC §41/86/91):** every portal item routes to a real surface; the `SurfacePlaceholder` ("Coming in a later V24 plan") is deleted.
- **Review** → `reviewSlot()` (existing OrgViewShell).
- **Context** → `contextSlot()` → `ContextSurface`, a full-canvas wrapper around the shipped `components/ContextPanel` (the F4 drawer), fed by the focused pane's `ContextData` from App. No re-derivation; ContextPanel owns its own empty state.
- **Settings** → `SettingsSurface`, backed by the existing appearance store (`appearance/settings.ts`): font size, high contrast, reduced motion, bell behavior, cursor shape/blink. Each change calls `applyAppearanceSettings` (live) + `saveAppearanceSettings` (persist). Theme/font-family selection deferred (theme catalog lives behind `themeRuntime`).
- **Memory** → `MemorySurface`, an honest state: memory is harness-backed (the `/memory` slash command); no live in-app data until a server `/memory` route exists (follow-up). Renders no fabricated rows — same honest-signal discipline as the Swarm Map.

**Bottom anchor:** "Ask Voss to…" trigger button at rail bottom (separate from nav items). Height: 44px. Full width. Background: `--bg-2` on hover. Icon: `❯` in `--accent-green`.

**Label reveal:** expanded rail labels are the primary label-reveal mechanism. This supersedes the previously specced hover tooltip as the primary way to read portal item names; tooltips may remain as supplemental hints only.

**ARIA:** `<nav aria-label="Voss portal">` wrapping the rail. The item stack is `role="tablist"`, and each item is `<button role="tab" aria-selected="{true|false}" aria-label="{item name}">`. Icon-only collapsed items must retain non-empty `aria-label` accessible names. The toggle carries `aria-expanded` and a state-specific `aria-label` ("Expand portal" / "Collapse portal").

### 2. Quiet Top Chrome (`TopChrome`)

**Purpose:** Project identity, command palette access, mode status only. All layout preset controls removed.

**Height:** 28px (existing titlebar height — do not change)

**Content (left to right):**
- Traffic light buttons (macOS) or close/min/max (Linux/Win) — existing shell, unchanged
- Project name: `--font-ui`, 12px, `--fg-1`, max-width 180px, truncated with ellipsis
- Spacer (flex-grow)
- Command palette trigger button: `⌘K` label, `--font-mono`, 11px, `--fg-3`, `background: --bg-2`, `border: 1px solid --border`, padding 2px 8px. Opens the "Ask Voss to…" composer.
- Mode status chip: shows current safety mode of the most recently created Task. `--font-ui`, 11px, colored per mode (see safety mode chips in Color section). Hidden when no active Task.
- Live indicator chip (if SSE active): same as existing `.titlebar-livechip` — reuse as-is.

**Removed from top chrome:** `PresetSwitcher` (fanout/pipeline/swarm/watchers), Plan/Edit/Auto mode toggle, run selector, any raw `runId` display.

**Layout presets:** demoted to a button inside the left portal rail bottom area OR a keyboard shortcut in the command palette. Not in top chrome.

### 3. "Ask Voss to…" Composer (`VossComposer`)

**Purpose:** Global, always-reachable run intake. Replaces `RunCommandBar`.

**Trigger:** `⌘K` from anywhere, or clicking the `❯` button at the bottom of the portal rail.

**Presentation:** modal overlay — centered, `min-width: 560px`, `max-width: 720px`, `background: --bg-2`, `border: 1px solid --border-bright`, `border-radius: 0` (Variant B: no radius). Dims the background with `rgba(0,0,0,0.4)`.

**Default view (collapsed):**
```
┌───────────────────────────────────────────────────────────┐
│ ❯  Ask Voss to...                                  [⌘K]  │
│                                                           │
│ Safety:  [Read only ▼]           [Advanced ▸]  [Create]  │
└───────────────────────────────────────────────────────────┘
```
- Ask field: full-width textarea, `--font-mono`, 13px, `--fg-0`, `background: --bg-1`, `border: 1px solid --border`, `min-height: 44px`, `resize: vertical`, `padding: 8px`. Placeholder: `"Describe what you want Voss to do..."` in `--fg-3`.
- Safety mode selector: `<select>` or button group — **defaults to "Read only"** (D-04). Options: "Read only" | "Can edit" | "Autopilot". Colored per mode chip (see Color section).
- Advanced toggle: `"Advanced ▸"` in `--fg-3`, 11px Inter. Clicking expands the advanced section; label flips to `"Advanced ▾"`.
- Create button: `background: --focus`, `color: --fg-0`, 11px Inter, font-weight 600, `padding: 8px 16px`. Label: `"Create Task"` (locked vocabulary D-08). Disabled when ask field is empty.

**Advanced panel (expanded, shown below the default row):**
- Scope: text input `"Scope (paths, patterns)..."` — `--font-mono`, 11px
- Agent target: dropdown from active agent roster or `"Any available"`
- Team: dropdown (existing team definitions or `"Solo"`)
- Budget: number input + `$` label, `--font-mono`, 11px, tabular-nums
- Attach context: file path input or drag-drop zone, `"Attach context file..."`

**ARIA:** `<dialog aria-label="Ask Voss to create a Task">`. Ask field has `aria-required="true"`. Safety selector has `aria-label="Safety mode"`. Advanced section uses `aria-expanded` on the toggle button and `aria-controls` pointing to the expanded panel.

**Focus management:** on open, focus lands on the ask textarea. On close (Escape or Cancel), focus returns to the trigger element.

### 4. Mission-Control Surfaces (Overview / Tasks / Agents)

These three surfaces share a list-row pattern inspired by Linear's issue grouping.

**Surface layout:**
```
┌─────────────────────────────────────────────────────────────────┐
│ SURFACE HEADER (40px)                                           │
│  [Overview / Tasks / Agents]    [filter]  [sort]               │
├─────────────────────────────────────────────────────────────────┤
│ GROUP: ACTIVE (accent-cyan dot)   N items          [collapse ▾] │
│  ▸ [status dot]  Task name                   [agent] [elapsed]  │
│  ▸ [status dot]  Task name                   [agent] [elapsed]  │
├─────────────────────────────────────────────────────────────────┤
│ GROUP: BLOCKED (accent-red dot)   N items          [collapse ▾] │
│  ▸ [status dot]  Task name          [!] attention action badge  │
├─────────────────────────────────────────────────────────────────┤
│ GROUP: REVIEWING (accent-amber dot)                             │
│ GROUP: DONE (accent-green dot)                                  │
│ GROUP: ADOPTED (accent-green, dimmer)                           │
│ GROUP: TERMINAL AGENT (fg-3 dot)                                │
└─────────────────────────────────────────────────────────────────┘
```

**Focal point (Overview / Tasks):** the Task name in `--fg-0` 13px Inter weight 500 is the primary focal point of each row — every other element (status dot, agent, elapsed time) is lower-contrast metadata that reads as secondary support around it.

**Focal point (Agents):** the agent name in `--fg-0` 13px is the primary focal point of each row, with the live status dot as the secondary draw and model/cost/elapsed reading as muted `--fg-3` metadata.

**Surface header:**
- Height: 40px. Background: `--bg-1`. Bottom border `1px solid --border`.
- Title: `--font-display` (Poppins), 13px, 600, `--fg-0`.
- Right controls: filter + sort — `--font-ui`, 11px, 500, `--fg-2`. Background `--bg-2` on hover.

**Group header row:**
- Height: 28px. Background: `--bg-0`. No bottom border (visual separation via 8px gap between groups).
- Status dot: 6px circle, colored per group accent.
- Group name: ALL CAPS, `--font-display`, 11px, 600, letter-spacing 0.08em, `--fg-3`.
- Count badge: `--font-mono`, 11px, `--fg-3`.
- Collapse chevron: `▾` / `▸` in `--fg-3`. Full group row is clickable for collapse.

**Task/item row:**
- Height: 32px min (36px when two lines needed).
- Padding: 0 16px (left aligned with the rest of content area).
- Hover: `background: --bg-2`.
- Status dot: 7px circle on left, colored per status.
- Task name: `--font-ui`, 13px, 500, `--fg-0`, truncated. (Primary focal point — see above.)
- Metadata right cluster: agent name or count (`--fg-3`, 11px), elapsed time (`--font-mono`, 11px, `--fg-3`, tabular-nums).
- Attention badge (blocked items): `"!"` red chip, `background: color-mix(in srgb, --accent-red 15%, transparent)`, `color: --accent-red`, 11px, padding 0 4px. Clicking triggers the attention action (inline, not a new modal).
- Deep link: clicking the row opens the corresponding pane/drawer via `org/selection` (`openInGridRequest` / `openInReviewRequest`). The row renders as a `<button>` (not `<a>`). `aria-label="Open Task: {name}"`.

**Empty state (per group when no items):** groups with zero items are hidden by default. Entire surface empty state: centered text `"No active Tasks"` in `--fg-3`, 12px Inter, with a secondary line `"Use ⌘K to ask Voss to start one."` in `--fg-3`, 11px.

**Agents surface distinction:** same row pattern but grouped by agent role (Voss main, reviewer, tester, terminal-agent) rather than status. Each row shows agent name, model, status dot, elapsed time, and cost (`--font-mono`, tabular-nums).

### 5. Swarm Map Canvas (`SwarmMap`)

**Purpose:** Radial observability graph of one or more runs. The most visual surface in V24.

**Canvas:** fills the main canvas area (`--bg-0`). SVG or Canvas 2D. No axes, no legends cluttering the canvas (legend lives in a collapsible 200px right panel).

**Focal point:** the center objective/run node (40px circle, `2px solid --focus` ring) is the primary focal point of each cluster — its size and `--focus` ring make it read as the gravitational center, with agent nodes as the secondary ring and work/artifact/alert nodes as supporting detail radiating outward.

**Node types:**

| Node type | Shape | Fill | Ring | Label |
|-----------|-------|------|------|-------|
| Objective / Run (center) | Circle 40px | `--bg-3` | `2px solid --focus` | Task name, 11px Inter, below node |
| Agent node | Circle 28px | `--bg-2` | `2px solid --accent-cyan` (live) or `--border` (idle) | Agent role, 11px, below |
| Work node (step/card) | Square 20px | `--bg-2` | `1px solid --border` | Step name truncated 100px, 11px |
| Artifact node (file/diff/test) | Diamond 16px | `--bg-2` | `1px solid --border` | File name, 11px |
| Alert node (permission/budget/blocker) | Triangle 16px | `color-mix(in srgb, --accent-red 15%, transparent)` | `1px solid --accent-red` | Alert type, 11px, `--accent-red` |
| Placeholder node (missing signal) | Circle 20px | transparent | `2px dashed --border-bright` | `"—"`, `--fg-3`, 11px |

**Edge types:**

| Edge type | Style | Color |
|-----------|-------|-------|
| Delegation | Solid arrow (→) | `--focus` |
| Message | Dashed arrow | `--accent-blue` |
| Tool call | Dotted arrow | `--accent-magenta` |
| File edit | Solid thin | `--accent-green` |
| Review/validation | Dashed | `--accent-amber` |
| Blocker | Solid bold, pulsing | `--accent-red` |

**Honest signal contract:** every rendered edge MUST have a source field (`edge.source: "sse_event" | "board_card" | "audit_artifact" | "registry_entry"`). The no-fake-signal guard test asserts `edge.source !== undefined` for every rendered edge. Missing signals → placeholder node or omission, never a fabricated edge.

**Multi-run layout (D-07):** one radial cluster per run, packed via force simulation across the canvas. Each cluster: center node at cluster centroid, agents on radius 120px, work/artifact nodes on radius 220px, alert nodes at outermost radius 300px. Clusters must not overlap (minimum 80px gap between cluster boundaries).

**Click-to-focus a cluster:** clicking the center node of a multi-run canvas zooms/scales that cluster to fill ~80% of the canvas, fading others to 30% opacity. Clicking the canvas background restores full multi-run view. Clicking any non-center node opens its detail in the right legend panel.

**Right legend panel (200px):**
- Background: `--bg-1`. Left border `1px solid --border`.
- Header: selected node name, 13px Inter 500, `--fg-0`.
- Body: key-value pairs (matches cockpit-kvgrid pattern): status, role, last-event, cost.
- Deep link button at bottom: `"Open in grid →"` or `"Open in review →"`, 11px, `--focus` color, as a text button.
- When nothing selected: `"Select a node to inspect"` in `--fg-3`, 11px, centered.

**Swarm Map empty state:**
- No active runs with signal data: centered message `"No run data yet"` (`--fg-3`, 13px Inter) + secondary `"Start a Task with ⌘K to see agents appear here."` (`--fg-3`, 11px). No decorative nodes or fake graph.

### 6. Live Swarm Map Connectors + Animation

**Motion is semantic only** — every animation communicates a real live state change.

**Animated edge states:**
- Active delegation in flight: edge draws a traveling dot (3px circle) from source to destination node at 2s traversal speed. Color matches edge type accent.
- New SSE event arriving: the destination node briefly gains a 300ms outer ring pulse (`outline: 0 → 4px, opacity 1 → 0`). Color matches edge type accent.
- Blocker edge: slow 1.5s pulse (`opacity: 1 → 0.4 → 1`).

**All animations must use CSS `@keyframes` (not JS `setInterval`), with `animation-play-state: paused` when the tab is not focused.**

**MANDATORY reduced-motion fallback:**
- `@media (prefers-reduced-motion: reduce)` OR `html.reduced-motion` class (A8 pattern from index.css): ALL Swarm Map animations cease. Traveling dots do not appear.
- Instead: an **Event Trace list** is shown in the right legend panel. The trace list is always present but collapsed by default in motion mode; it is expanded and pinned in reduced-motion mode.
- Event Trace row: `[timestamp]  [edge type]  [source → destination]` — `--font-mono`, 11px, `--fg-2`.
- The trace list must carry equivalent information to what the animations convey (VADE2-07 acceptance criterion).

### 7. Completed-Run Replay Scrubber

**Placement:** bottom strip of the Swarm Map canvas, visible only when a completed run is selected and replay mode is active.

**Geometry:** 32px height. Full canvas width. Background `--bg-1`. Top border `1px solid --border`.

**Controls (left to right):**
- Play/Pause button: glyph `⏵` (play) / `⏸` (pause), `--fg-1`, 20px touch target. **ARIA:** when stopped/paused, `aria-label="Play replay"`; when playing, `aria-label="Pause replay"`. The label toggles with state so screen-reader users always hear the action the button will perform.
- Scrubber track: `flex-grow: 1`, `height: 4px`, `background: --border`, track filled up to scrub position with `--focus`.
- Thumb: 12px circle, `background: --focus`.
- Current time label: `--font-mono`, 11px, tabular-nums, `--fg-2`
- End time label: `--font-mono`, 11px, `--fg-3`

**Interaction:** dragging the thumb scrubs the graph state to that event timestamp. All node/edge states reflect the scrubbed moment (past events shown, future events hidden). Live mode chip in top chrome is hidden or shows "replay" when scrubber is active.

**ARIA:** the track is `<input type="range" aria-label="Replay timeline" aria-valuenow="{ms}" aria-valuemin="0" aria-valuemax="{total_ms}">`; the play/pause button carries its own state-dependent `aria-label` (above).

---

## Interaction Contracts

### Canvas-Swap (D-01)

- Selecting a portal nav item: instant swap, no transition animation (grid mounts/unmounts display). Grid state (pane tree, sessions, scrollback) is fully preserved.
- Fresh/project-less workspace: boots to terminal grid (portal rail visible, Overview not auto-selected).
- Workspace with active managed runs: restores last-used portal surface.
- Keyboard: `⌘⌥1`–`⌘⌥9` correspond to the 9 portal items in order. (Avoids conflict with existing `⌘1`–`⌘9` pane focus shortcuts.)

### Portal Rail Focus

- `Tab` moves through portal rail items.
- `Enter` / `Space` activates a portal item.
- Rail is `role="tablist"`, each item is `role="tab"` with `aria-selected`.
- The main canvas area has `role="tabpanel"` with `aria-labelledby` pointing to the active tab.

### Composer (⌘K)

- Opens from any surface, including within the terminal grid.
- `Escape` closes; focus returns to trigger or last focused element.
- `Enter` in the ask textarea (without modifier) does NOT submit — use `⌘Enter` to create the Task (power-user expectation in terminal-dense app).
- Advanced section keyboard: Tab-navigable in sequence.

### Swarm Map

- Pan: click-drag canvas background.
- Zoom: scroll wheel or trackpad pinch.
- Node click: selects and populates right legend panel.
- Node double-click: triggers deep link (open in grid/review).
- Cluster center click: focus/expand that cluster.
- Canvas background click: deselect / restore multi-run view.
- Keyboard: `Tab` cycles through visible nodes. `Enter` selects. `⌘Enter` deep-links.

### Attention Actions (Mission Control)

- Blocked item attention badge click: opens an inline action row below the item row (not a modal). Action row shows: description of the blocker + a primary action button (e.g., "Approve", "Provide context"). Action row is 44px tall, `background: color-mix(in srgb, --accent-red 8%, transparent)`.
- After action taken, item status updates (SSE or local optimistic update) and moves to the appropriate group.

---

## Glyph Affordances

Consistent with existing MANIFEST.md + FEATURES.md contract. V24 extends:

| Glyph | Context | Color |
|-------|---------|-------|
| `❯` | User message prefix, composer trigger | `--accent-green` (existing `--user-msg` maps to `#8ab4ff` but `❯` in composer = green CTA) |
| `⏵` | Tool call prefix, replay play button | `--accent-magenta` |
| `※` | Reviewer glyph, Review portal item | `--accent-amber` |
| `⊕` | Patch/diff glyph | `--fg-1` |
| `▸` / `▾` | Collapse chevron, advanced toggle | `--fg-3` |
| `!` | Attention/blocker badge | `--accent-red` |
| `◉` | Memory node, live status dot | context-dependent |
| `—` | Placeholder node label | `--fg-3` |

---

## Copywriting Contract

All copy follows the locked vocabulary from VADE2-01 / D-08 / D-09 / D-10 / D-11.

| Element | Copy | Notes |
|---------|------|-------|
| Portal item 1 | "Workspaces" | Locked vocabulary (VADE2-09) — terminal/tmux grid home |
| Portal item 2 | "Overview" | Title case |
| Portal item 3 | "Tasks" | Locked vocabulary (D-08) — NOT "Runs" |
| Portal item 4 | "Agents" | Title case |
| Portal item 5 | "Swarm Map" | Locked vocabulary (D-10) |
| Portal item 6 | "Review" | Title case |
| Portal item 7 | "Context" | Title case |
| Portal item 8 | "Memory" | Title case |
| Portal item 9 | "Settings" | Title case |
| Composer placeholder | "Describe what you want Voss to do..." | Sentence case, trailing ellipsis |
| Composer title | "Ask Voss to…" | Unicode ellipsis (…), not `...` |
| Primary CTA | "Create Task" | Locked vocabulary (D-08); verb + locked noun |
| Safety mode default | "Read only" | Two words, no hyphen (D-11) |
| Safety mode 2 | "Can edit" | Two words (D-11) |
| Safety mode 3 | "Autopilot" | One word (D-11) |
| Advanced toggle (collapsed) | "Advanced ▸" | No colon; chevron glyph |
| Advanced toggle (expanded) | "Advanced ▾" | |
| Board work-items | "steps" / "cards" | lowercase; never "tasks" inside a Task (D-09) |
| Mission control group: active | "ACTIVE" | ALL CAPS (section heading pattern) |
| Mission control group: blocked | "BLOCKED" | ALL CAPS |
| Mission control group: reviewing | "REVIEWING" | ALL CAPS |
| Mission control group: done | "DONE" | ALL CAPS |
| Mission control group: adopted | "ADOPTED" | ALL CAPS |
| Mission control group: terminal agent | "TERMINAL AGENT" | ALL CAPS |
| Empty state: no Tasks | "No active Tasks" (heading) + "Use ⌘K to ask Voss to start one." (body) | |
| Empty state: Swarm Map | "No run data yet" (heading) + "Start a Task with ⌘K to see agents appear here." (body) | |
| Empty state: Agents | "No agents running" + "Create a Task to deploy agents." | |
| Placeholder node tooltip | "Signal not available" | Honest missing-signal language |
| Generic surface error: Retry button | "Try again" | Verb-first button label on the `.org-error-state` retry control |
| Error: SSE connection lost | "Lost connection to Voss. Reconnecting..." | Action is automatic; no user action needed |
| Error: Task creation failed | "Couldn't create Task. Check that Voss is running." | Problem + diagnostic hint |
| Error: replay load failed | "Couldn't load replay data. The run may still be in progress." | Problem + explanation |
| Destructive: cancel Task | "Cancel Task" (action) → "Stop this Task? Work in progress will be saved." [Cancel] [Stop Task] | Modal confirm; "Stop Task" is the destructive confirm label |
| Destructive: discard context | "Remove context?" → "This context attachment will be removed from this Task." [Keep] [Remove] | |
| Code identifiers (never shown) | `runId`, `RunData`, `currentRunId` | Internal only; zero display exposure (D-09) |

---

## States to Handle (per surface)

### Every portal surface

| State | Visual treatment |
|-------|-----------------|
| Loading | Centered `⟳` spinner glyph (existing `.org-spinner` pattern), `--fg-2`, 16px |
| Empty | Surface-specific empty state copy (see Copywriting above) |
| Error | `.org-error-state` pattern: heading 14px Poppins 500, body 12px Inter, "Try again" retry button |
| Active | Content as specified |

### Swarm Map specific

| State | Visual treatment |
|-------|-----------------|
| No data | Canvas-centered empty state (see above) |
| Partial data | Render real nodes; placeholder nodes for missing agents/signals |
| Single run | One radial cluster centered on canvas |
| Multi-run | Packed clusters, force-laid out |
| Focused cluster | Foreground cluster 100% opacity; others 30% opacity |
| Replay active | Scrubber strip visible; future events hidden; play/pause control |
| Reduced motion | Animations off; event trace list expanded in legend panel |

### Composer

| State | Visual treatment |
|-------|-----------------|
| Default | Ask field + safety selector + Create button |
| Advanced expanded | Additional scope/agent/team/budget/context fields below |
| Submitting | Create button shows `"Creating..."` text, disabled |
| Error | Inline error below ask field: `--accent-red`, 11px Inter |

---

## Accessibility

All requirements from VADE2-08 are design-level contracts here:

- **Focus rings:** all interactive elements use `outline: 1px solid var(--focus); outline-offset: -1px` on `:focus-visible`. No `:focus` without `:focus-visible` (avoids outline on mouse click).
- **Color contrast:** minimum WCAG AA (4.5:1) for all text on its background. Verified by existing `contrastRatio()` utility in `src/themes/schema.ts`. The `--fg-0` on `--bg-0` combination (#e8eaf0 on #0a0b0e) exceeds AAA.
- **Reduced motion:** see Swarm Map §6 and existing `@media (prefers-reduced-motion: reduce)` rules in `index.css`. V24 adds no new animations outside the Swarm Map connector system. All new Swarm Map animations must be governed by both the OS media query AND the `html.reduced-motion` class (A8 pattern — mandatory).
- **Deep-link affordances:** every node/row that navigates uses a `<button>` with a descriptive `aria-label`. Navigation outcome is announced via `aria-live="polite"` on the main canvas region.
- **Portal navigation:** `role="tablist"` / `role="tab"` / `role="tabpanel"` as specified in Component Inventory §1.
- **Swarm Map keyboard:** `Tab` cycles nodes; `Enter` selects; `⌘Enter` deep-links. Announced node name + type on selection via `aria-live`.
- **Composer dialog:** `role="dialog"` with `aria-modal="true"` and `aria-label`. Focus trap while open.
- **Timeline scrubber:** native `<input type="range">` with `aria-label`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`. The adjacent play/pause button carries a state-dependent `aria-label` ("Play replay" / "Pause replay").
- **Status chips:** do not use color alone to communicate status. Each chip includes the text label ("ACTIVE", "BLOCKED", etc.) alongside the color dot.
- **High-contrast mode:** all borders use `--border` / `--border-bright` tokens (not color-mix-only), ensuring they remain visible when the user applies a high-contrast system theme.

---

## Animation Budget

| Animation | Duration | Easing | Reduced-motion |
|-----------|----------|--------|----------------|
| Canvas-swap opacity | 0ms (instant) | — | n/a |
| Portal item hover bg | 80ms ease | `ease` | 0ms |
| Mission-control row hover bg | 80ms | `ease` | 0ms |
| Attention action row expand | 120ms | `ease-out` | 0ms (instant show) |
| Composer open/close | 120ms | `ease-out` | 0ms |
| Swarm Map cluster focus (opacity) | 200ms | `ease` | 0ms |
| Swarm Map node select (scale 1→1.1) | 100ms | `ease-out` | 0ms |
| Swarm Map traveling dot | 2000ms linear | `linear` | none (omit) |
| Swarm Map node ring pulse (SSE event) | 300ms | `ease-out`, fill-mode forward | none (omit) |
| Swarm Map blocker edge pulse | 1500ms | `ease-in-out`, infinite | none (omit) |
| Replay scrubber thumb drag | native | — | n/a |

All new `@keyframes` in V24 must be wrapped in a `@media (not (prefers-reduced-motion: reduce))` block and additionally guarded by the `html.reduced-motion` class (double guard = A8 pattern).

---

## What NOT to Spec (Out of Scope for V24)

Per SPEC.md Boundaries:

- Review / Context / Memory / Settings surface redesign — reuse existing V14 panels as-is
- Cloud/team admin UI
- IDE/editor pane
- Mandatory Voss wrapping for terminal panes
- Generic decorative graph animation (motion must convey real state only)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No third-party UI registries. All components are hand-rolled SolidJS + Tailwind v4 + CSS vars.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: V24-ade-product-revamp-swarm-observability*
*UI-SPEC created: 2026-06-14*
*Researcher: gsd-ui-researcher (claude-sonnet-4-6)*
