---
phase: V13.3
slug: go-local-headless-client-sdk
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-07
---

# Phase V13.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go `testing` (`go test`) |
| **Config file** | `sdk/go/go.mod` (module `github.com/vosslang/voss/sdk/go`, `go 1.22`) — created in Wave 0 (Plan 01) |
| **Quick run command** | `cd sdk/go && go test ./... -short` |
| **Full suite command** | `cd sdk/go && go build ./... && VOSS_PYTHON="$(cd ../.. && pwd)/.venv/bin/python" go test ./... -timeout 120s` |
| **Estimated runtime** | unit < 5s; integration ~20-40s (spins one real `voss serve` via TestMain + `VOSS_SERVE_FAKE_TURN`) |

> Integration tests launch ONE real `voss serve` (shared via `TestMain`, Plan 06) and use the `VOSS_SERVE_FAKE_TURN` 8-event canned turn (no provider creds). When `.venv/bin/python` / `VOSS_PYTHON` is unavailable, integration tests `t.Skip`; unit tests still run. The drift-gate test `t.Skip`s when the official `contracts/openapi.json` is absent (V13.1 unshipped) and otherwise regenerates from it; pre-V13.1 codegen uses `sdk/go/testdata/openapi.fixture.json` (captured Wave 0).

---

## Sampling Rate

- **After every task commit:** `cd sdk/go && go test ./... -short` (or `-run TestUnit` subset).
- **After every plan wave:** `cd sdk/go && go build ./... && go test ./... -timeout 60s` (unit + httptest-stub integration; real-server suite once Plan 05/06 TestMain lands).
- **Before `/gsd-verify-work`:** Full suite green with `VOSS_PYTHON` set; drift gate green (or skipped with fixture if V13.1 unshipped); `grep -rn 'import "C"' sdk/go/` clean.
- **Max feedback latency:** < 5s (unit), < 45s (full incl. integration).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01 | 0 | VSDK-GO-01, VSDK-GO-08 | T-V13.3-01-SC | Pinned, proxy-verified codegen deps; module floor | build | `cd sdk/go && go mod verify && grep -q "module github.com/vosslang/voss/sdk/go" go.mod` | ❌ creates | ⬜ pending |
| 01-T2 | 01 | 0 | VSDK-GO-01 | T-V13.3-01-04 | Fixture token is placeholder, not a live secret | build+diag | `cd sdk/go && grep -q EventEnvelope types.gen.go && go build ./... && go test ./... -run TestDiscriminatorProbe -v` | ❌ creates | ⬜ pending |
| 01-T3 | 01 | 0 | VSDK-GO-01, VSDK-GO-08 | T-V13.3-01-01, T-V13.3-01-02 | Drift gate; skip-when-upstream-absent | go test | `cd sdk/go && go build ./... && go vet ./... && go test ./... -run TestTypesAreUpToDate` | ❌ creates | ⬜ pending |
| 02-T1 | 02 | 1 | VSDK-GO-02 | T-V13.3-02-02, T-V13.3-02-03 | Token-safe typed error; malformed-body tolerant | unit | `cd sdk/go && go test ./... -run TestVossError` | ❌ creates | ⬜ pending |
| 02-T2 | 02 | 1 | VSDK-GO-02 | T-V13.3-02-01, T-V13.3-02-04 | No silent drop; unknown→ErrUnknownEventType | unit | `cd sdk/go && go test ./... -run 'TestDecodeAllMembers\|TestDecodeUnknownType' -v` | ❌ creates | ⬜ pending |
| 03-T1 | 03 | 2 | VSDK-GO-06 | T-V13.3-03-02, T-V13.3-03-03 | Handshake timeout; attach owns no child; token-safe | unit | `cd sdk/go && go test ./... -run 'TestHandshakeParse\|TestAttach' -v` | ❌ creates | ⬜ pending |
| 03-T2 | 03 | 2 | VSDK-GO-03 | T-V13.3-03-01, T-V13.3-03-04 | Bearer chokepoint; 401/409 typed | integration/unit | `cd sdk/go && go test ./... -run 'TestIntegrationRestRoundTrip\|TestIntegration401\|TestBearerHeader' -v` | ❌ creates | ⬜ pending |
| 04-T1 | 04 | 2 | VSDK-GO-04 | T-V13.3-04-01, T-V13.3-04-02 | Hand-parsed framing; leak-free cancel | integration/unit | `cd sdk/go && go test ./... -run 'TestIntegrationSSEOrdering\|TestIntegrationSSECancel' -timeout 30s` | ❌ creates | ⬜ pending |
| 05-T1 | 05 | 3 | VSDK-GO-05 | T-V13.3-05-01, T-V13.3-05-02, T-V13.3-05-03 | No command injection; no-orphan PID; no hang | integration | `cd sdk/go && VOSS_PYTHON="$(cd ../.. && pwd)/.venv/bin/python" go test ./... -run 'TestSpawnNoOrphan\|TestSpawnBadInterpreter\|TestAttachRoundTrip' -timeout 60s` | ❌ creates | ⬜ pending |
| 06-T1 | 06 | 4 | VSDK-GO-07 | T-V13.3-06-01, T-V13.3-06-02 | Permission auth; no cgo/FFI; no orchestration import | unit | `cd sdk/go && go test ./... -run 'TestPermission\|TestNoFFI' -v && grep -rn 'import "C"' sdk/go/ \|\| true` | ❌ creates | ⬜ pending |
| 06-T2 | 06 | 4 | VSDK-GO-08, VSDK-GO-01 | T-V13.3-06-04, T-V13.3-06-SC | Real-server harness; no orphan; drift+parity green | integration | `cd sdk/go && VOSS_PYTHON="$(cd ../.. && pwd)/.venv/bin/python" go test ./... -timeout 120s` | ❌ creates | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 = Plan 01 (all created there):

- [ ] `sdk/go/go.mod` + module skeleton (module `github.com/vosslang/voss/sdk/go`, `go 1.22`) + oapi-codegen tool/dep pins (`tools.go` fallback)
- [ ] `sdk/go/oapi-codegen.yaml` + `sdk/go/doc.go` (`//go:generate`) — types-only config
- [ ] `sdk/go/testdata/openapi.fixture.json` — committed `create_app('test-token').openapi()` snapshot (unblocks codegen before V13.1 ships `contracts/openapi.json`)
- [ ] `sdk/go/types.gen.go` — initial codegen run against the fixture (checked in)
- [ ] `sdk/go/discriminator_probe_test.go` — empirically verify the generated union's `Discriminator()` signature (RESEARCH A1) for Plan 02's `Decode()`
- [ ] `sdk/go/internal/drift/drift_test.go` — drift gate (skip when `contracts/` absent) + 21-type parity check
- [ ] RED scaffolds: `events_test.go`, `rest_test.go`, `sse_test.go`, `client_test.go`, `handshake_test.go` — all VALIDATION test names, compiling clean, `t.Skip`/comment for later-plan symbols

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Permission allow/deny via a REAL provider gate over SSE | VSDK-GO-07 | `VOSS_SERVE_FAKE_TURN` emits NO `permission.updated` event (RESEARCH signal 10 / Q3); a live gate needs provider creds unavailable in CI | The automated `TestPermission*` exercises the permission ROUTE + status mapping (ok/stale) directly; the full SSE-observed allow→tool-state-ok / deny→denied loop is verified manually with a real provider, or deferred. Documented in 06-SUMMARY. |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (Plan 01)
- [x] No watch-mode flags
- [x] Feedback latency < 45s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready (set `wave_0_complete: true` after Plan 01 lands)
