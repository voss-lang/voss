---
phase: V13.3
slug: go-local-headless-client-sdk
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-07
---

# Phase V13.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go `testing` (`go test`) |
| **Config file** | `sdk/go/go.mod` (module `github.com/vosslang/voss/sdk/go`, `go 1.22`) — created in Wave 0 |
| **Quick run command** | `cd sdk/go && go test ./... -short` |
| **Full suite command** | `cd sdk/go && go build ./... && go test ./...` |
| **Estimated runtime** | ~TBD (unit fast; integration spins a real `voss serve` via `VOSS_SERVE_FAKE_TURN`) |

> Integration tests launch a real `voss serve` and use the `VOSS_SERVE_FAKE_TURN` seam (8-event canned turn, no provider creds). The drift-gate test `t.Skip`s when the official `contracts/openapi.json` is absent (V13.1 unshipped) and runs against `sdk/go/testdata/openapi.fixture.json`.

---

## Sampling Rate

- **After every task commit:** Run quick run command (`go test ./... -short`).
- **After every plan wave:** Run full suite command (`go build ./... && go test ./...`).
- **Before `/gsd-verify-work`:** Full suite green; drift gate green (or skipped with the fixture if V13.1 unshipped); no-FFI grep clean.
- **Max feedback latency:** TBD seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _planner to fill_ | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Known scaffolds (planner to confirm/expand):

- [ ] `sdk/go/go.mod` + module skeleton (module `github.com/vosslang/voss/sdk/go`, `go 1.22`)
- [ ] `sdk/go/testdata/openapi.fixture.json` — committed `create_app().openapi()` snapshot (unblocks codegen before V13.1 ships `contracts/openapi.json`)
- [ ] Failing test stubs for: 21-event decode (VSDK-GO-02), REST round-trip status codes (VSDK-GO-03), SSE ordering (VSDK-GO-04), spawn no-orphan (VSDK-GO-05), handshake/bearer 401 (VSDK-GO-06), permission allow/deny (VSDK-GO-07), drift gate (VSDK-GO-01)

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| _planner to fill_ | | | |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < TBDs
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
