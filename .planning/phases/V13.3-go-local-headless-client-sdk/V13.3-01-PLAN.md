---
phase: V13.3-go-local-headless-client-sdk
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - sdk/go/go.mod
  - sdk/go/go.sum
  - sdk/go/doc.go
  - sdk/go/oapi-codegen.yaml
  - sdk/go/tools.go
  - sdk/go/testdata/openapi.fixture.json
  - sdk/go/types.gen.go
  - sdk/go/discriminator_probe_test.go
  - sdk/go/events_test.go
  - sdk/go/rest_test.go
  - sdk/go/sse_test.go
  - sdk/go/client_test.go
  - sdk/go/handshake_test.go
  - sdk/go/internal/drift/drift_test.go
autonomous: true
requirements: [VSDK-GO-01, VSDK-GO-08]
must_haves:
  truths:
    - "The Go module builds (go build ./...) and is a valid module github.com/vosslang/voss/sdk/go on go 1.22"
    - "Go contract/event types are generated by oapi-codegen from a committed snapshot, not hand-authored"
    - "A captured live-server OpenAPI fixture unblocks codegen before V13.1 ships contracts/openapi.json"
    - "The exact Discriminator() (or fallback) shape on the generated union is known and recorded for the Decode() dispatcher"
    - "RED test scaffolds exist for all downstream behaviors and reference the planned exported API"
  artifacts:
    - path: "sdk/go/go.mod"
      provides: "Module declaration + oapi-codegen tool/dep pins"
      contains: "module github.com/vosslang/voss/sdk/go"
    - path: "sdk/go/testdata/openapi.fixture.json"
      provides: "Live-server OpenAPI snapshot used as codegen input pre-V13.1"
      contains: "EventEnvelope"
    - path: "sdk/go/types.gen.go"
      provides: "oapi-codegen output: REST request/response structs + EventEnvelope union"
      contains: "EventEnvelope"
    - path: "sdk/go/internal/drift/drift_test.go"
      provides: "Drift gate that regenerates + git-diffs types.gen.go, skipping when contracts/ absent"
      contains: "t.Skip"
  key_links:
    - from: "sdk/go/doc.go"
      to: "contracts/openapi.json (fixture fallback)"
      via: "//go:generate go tool oapi-codegen"
      pattern: "go:generate.*oapi-codegen"
    - from: "sdk/go/internal/drift/drift_test.go"
      to: "types.gen.go"
      via: "go generate + git diff --exit-code"
      pattern: "git.*diff.*exit-code"
---

<objective>
Stand up the greenfield Go SDK module at `sdk/go/`, wire `oapi-codegen` type generation against a committed live-server OpenAPI fixture (because V13.1's `contracts/openapi.json` does not exist on disk yet — D-08), generate `types.gen.go`, empirically verify the generated union's discriminator accessor shape (RESEARCH open question A1), and lay down RED test scaffolds for every downstream behavior.

Purpose: Unblock all downstream Go work NOW without waiting for V13.1, and resolve the one material codegen unknown (`Discriminator()` signature) before the `Decode()` dispatcher is written in Plan 02.
Output: A buildable module skeleton, checked-in generated types, the committed fixture, the drift gate (skip-aware), and failing test scaffolds.
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/V13.3-go-local-headless-client-sdk/V13.3-SPEC.md
@.planning/phases/V13.3-go-local-headless-client-sdk/V13.3-CONTEXT.md
@.planning/phases/V13.3-go-local-headless-client-sdk/V13.3-RESEARCH.md
@.planning/phases/V13.3-go-local-headless-client-sdk/V13.3-PATTERNS.md

<interfaces>
Planned exported API the RED scaffolds must reference (defined across Plans 02-06):
- `events.go`: `type TypedEvent interface{ eventType() string }`; `func Decode(env EventEnvelope) (TypedEvent, error)`; `type ErrUnknownEventType struct{ Type string }` (implements `error`); 21 typed event structs.
- `errors.go`: `type VossError struct{ Status int; Detail string }` (implements `error`).
- `client.go`: `func AttachClient(baseURL, token string) *Client`; `func Spawn(ctx context.Context, extraEnv map[string]string) (*Client, error)`; `func (c *Client) Close() error`; `type SpawnError struct{ ... }`.
- `rest.go`: methods on `*Client`: `CreateSession`, `ListSessions`, `ListSavedSessions`, `GetSession`, `DeleteSession`, `PostMessage`, `Abort`, `Cost`, `PermissionReply`, `Doctor`.
- `sse.go`: `func (c *Client) Events(ctx context.Context, sessionID string) (<-chan TypedEvent, error)`.
- `handshake.go`: `type Handshake struct{ V int; Port uint16; Token string }`; `func readHandshake(r io.Reader, timeout time.Duration) (Handshake, error)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module skeleton + codegen config + tool pin</name>
  <files>sdk/go/go.mod, sdk/go/go.sum, sdk/go/doc.go, sdk/go/oapi-codegen.yaml, sdk/go/tools.go</files>
  <read_first>
    - V13.3-RESEARCH.md §Standard Stack (oapi-codegen v2.7.1, runtime v1.4.1; `go tool` pin on Go 1.24+; `go 1.22` module floor) + §Architecture Patterns Pattern 1 (oapi-codegen.yaml types-only config) — authoritative tool versions and flags.
    - V13.3-PATTERNS.md §`sdk/go/types.gen.go` (oapi-codegen.yaml block + `//go:generate` directive).
    - V13.3-CONTEXT.md D-01/D-02 (module at `sdk/go/`, path `github.com/vosslang/voss/sdk/go`), D-04 (stdlib net/http only — `client: false`), D-05/D-07 (oapi-codegen + go-generate + tool pin).
  </read_first>
  <action>
    Create `sdk/go/go.mod` declaring `module github.com/vosslang/voss/sdk/go` and `go 1.22`. Add the oapi-codegen toolchain: machine runs go1.26 so use `go get -tool github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.7.1` to add a `tool` directive, and `go get github.com/oapi-codegen/runtime@v1.4.1` for the generated union's JSON helpers. Also create `sdk/go/tools.go` as the build-tagged blank-import fallback (`//go:build tools`) importing `github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen` so CI runners predating the 1.24 `tool` directive can still pin via `go run`.
    Create `sdk/go/oapi-codegen.yaml` with `package: voss`, `output: types.gen.go`, and `generate:` set to `models: true` with `client`, `chi-server`, `std-http-server`, `embedded-spec` all `false` (types-only; the REST client is hand-written per D-04).
    Create `sdk/go/doc.go` declaring `package voss` with the generate directive `//go:generate go tool oapi-codegen -config oapi-codegen.yaml ../../contracts/openapi.json`. Document in a comment that the directive reads the official `contracts/openapi.json` once V13.1 ships it; pre-V13.1 development generates from `testdata/openapi.fixture.json` (Task 2/3). Do NOT inline any implementation.
  </action>
  <verify>
    <automated>cd sdk/go && go mod verify && grep -q "module github.com/vosslang/voss/sdk/go" go.mod && grep -q "^go 1.22" go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/go/go.mod` declares `module github.com/vosslang/voss/sdk/go` and `go 1.22`.
    - `cd sdk/go && go mod verify` exits 0.
    - `oapi-codegen.yaml` has `client: false` and `models: true` (grep confirms both).
    - `doc.go` `//go:generate` line targets `oapi-codegen -config oapi-codegen.yaml` (grep `go:generate.*oapi-codegen` matches).
    - `tools.go` carries the `//go:build tools` tag and blank-imports the oapi-codegen cmd package.
  </acceptance_criteria>
  <done>Module builds as an empty package; codegen tooling pinned via both `tool` directive and `tools.go` fallback; generate directive wired.</done>
</task>

<task type="auto">
  <name>Task 2: Capture live-server OpenAPI fixture + initial codegen + Discriminator probe</name>
  <files>sdk/go/testdata/openapi.fixture.json, sdk/go/types.gen.go, sdk/go/discriminator_probe_test.go</files>
  <read_first>
    - V13.3-RESEARCH.md §Contracts/OpenAPI Snapshot Status (the exact `create_app('test-token').openapi()` capture command + what `_force_event_schema` injects: EventEnvelope oneOf + all 21 event schemas + REST request models) — authoritative fixture-generation recipe.
    - V13.3-RESEARCH.md §Open Questions Q1 + §Assumptions Log A1 (Discriminator() signature is ASSUMED — must be empirically verified now) + §Architecture Patterns Pattern 2 (the `AsX()`/`Discriminator()` shape and the direct-unmarshal fallback).
    - V13.3-PATTERNS.md §`sdk/go/types.gen.go` (fixture-strategy block) + critical planner note 4 (Wave 0 must verify the generated `Discriminator()` signature).
    - voss/harness/server/app.py `_force_event_schema` (~line 449-469) — confirms EventEnvelope is forced into OpenAPI components.
  </read_first>
  <action>
    Generate `sdk/go/testdata/openapi.fixture.json` by running `.venv/bin/python -c "from voss.harness.server.app import create_app; import json; print(json.dumps(create_app('test-token').openapi(), indent=2))"` from the repo root and writing stdout to that path. Commit it. This is the codegen input until V13.1 ships `contracts/openapi.json` (D-08).
    Run codegen against the fixture: `cd sdk/go && go tool oapi-codegen -config oapi-codegen.yaml testdata/openapi.fixture.json` (override the directive's default path to the fixture for this initial run) and write the output to `types.gen.go`. Commit `types.gen.go` (checked-in per D-06). Confirm the generated file contains an `EventEnvelope` type and the 21 event model structs.
    Write `sdk/go/discriminator_probe_test.go`: a passing diagnostic test that constructs an `EventEnvelope` from a known event JSON and exercises the generated union accessors to RECORD the real API — specifically whether a `Discriminator()` method exists and its exact return signature (`(string, error)` vs `string`), and that `AsServerConnected()`/`AsGateUpdated()`-style accessors exist. The test must log (via `t.Logf`) the resolved discriminator approach so Plan 02's `Decode()` uses the verified shape; if `Discriminator()` is absent, the test demonstrates the direct `json.Unmarshal` of `struct{ Type string }` fallback from RESEARCH A1. Record the conclusion in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd sdk/go && grep -q "EventEnvelope" types.gen.go && go build ./... && go test ./... -run TestDiscriminatorProbe -v</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/go/testdata/openapi.fixture.json` exists and contains `"EventEnvelope"` and all 21 event schema component names (incl. `PrinciplesOverflow`).
    - `cd sdk/go && go build ./...` exits 0 with `types.gen.go` present.
    - `types.gen.go` contains an `EventEnvelope` type (grep confirms).
    - `TestDiscriminatorProbe` passes and `t.Logf`s the resolved discriminator-read approach (method-or-fallback) consumed by Plan 02.
    - The SUMMARY records the verified `Discriminator()` signature (or the fallback decision).
  </acceptance_criteria>
  <done>Fixture committed; types.gen.go generated and checked in; the union's discriminator-read shape is empirically known and documented for Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Drift gate (skip-aware) + RED test scaffolds for all downstream behaviors</name>
  <files>sdk/go/internal/drift/drift_test.go, sdk/go/events_test.go, sdk/go/rest_test.go, sdk/go/sse_test.go, sdk/go/client_test.go, sdk/go/handshake_test.go</files>
  <read_first>
    - V13.3-RESEARCH.md §Architecture Patterns Pattern 5 (drift gate as go test + `os.Stat` skip when `contracts/openapi.json` absent) + §Validation Architecture (the per-requirement test name map — `TestDecodeAllMembers`, `TestDecodeUnknownType`, `TestIntegrationRestRoundTrip`, `TestIntegration401`, `TestIntegration409`, `TestIntegrationSSEOrdering`, `TestIntegrationSSECancel`, `TestSpawnNoOrphan`, `TestSpawnBadInterpreter`, `TestAttachRoundTrip`, `TestHandshakeParse`, `TestBearerHeader`, `TestPermissionAllow`, `TestPermissionDeny`).
    - V13.3-VALIDATION.md §Per-Task Verification Map + §Wave 0 Requirements (the full RED-scaffold list).
    - V13.3-PATTERNS.md §`sdk/go/internal/drift/drift_test.go` (drift gate + parity check; the `protocol_parity.rs` analog) + §Graceful Skip When Upstream Not Ready.
    - The `<interfaces>` block in this plan's context — the exact exported names the scaffolds must reference.
  </read_first>
  <action>
    Create `sdk/go/internal/drift/drift_test.go` (`package drift_test`) with `TestTypesAreUpToDate`: `os.Stat("../../contracts/openapi.json")` → `t.Skip` with "contracts/openapi.json not yet generated (V13.1 unexecuted)" when absent; otherwise run `go generate ../../...` then `git diff --exit-code types.gen.go` and `t.Fatalf` on non-empty diff. Add `TestDecodeCoversAllServerEventTypes` mirroring `crates/voss-tui/tests/protocol_parity.rs`: drive `.venv/bin/python` (VOSS_PYTHON → `.venv/bin/python` → `python3` chain) to enumerate the 21 `type` strings from `voss.harness.server.events.AgentEvent`, skip when no interpreter, and assert against the set Plan 02's `Decode()` will cover.
    Create the RED scaffold test files referencing the planned exported API from `<interfaces>`: `events_test.go` (`TestDecodeAllMembers` covering all 21 `type` strings incl. `"principles_overflow"`; `TestDecodeUnknownType` asserting `ErrUnknownEventType` via `errors.As`), `handshake_test.go` (`TestHandshakeParse`), `rest_test.go` (`TestIntegrationRestRoundTrip`, `TestIntegration401`, `TestIntegration409`, `TestBearerHeader`, `TestPermissionAllow`, `TestPermissionDeny`), `sse_test.go` (`TestIntegrationSSEOrdering`, `TestIntegrationSSECancel`), `client_test.go` (`TestSpawnNoOrphan`, `TestSpawnBadInterpreter`, `TestAttachRoundTrip`). Each scaffold must compile-fail or `t.Skip`/`t.Fatal` cleanly against the not-yet-written API — use `t.Skip("RED: implemented in Plan NN")` bodies that name the implementing plan so `go vet`/`go build ./...` of test files stays green where the referenced symbols are declared in Wave 0, and use build-time references only to symbols this plan creates. For symbols defined in later plans, keep the scaffold body a `t.Skip` and add a `// TODO(V13.3-0N)` comment rather than referencing undeclared identifiers (so the package still builds in Wave 0).
    Do NOT inline implementations. The scaffolds encode expected behavior as comments + skips; they go RED→GREEN as later plans land.
  </action>
  <verify>
    <automated>cd sdk/go && go build ./... && go vet ./... && grep -q "t.Skip" internal/drift/drift_test.go && grep -q "principles_overflow" events_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdk/go && go build ./...` and `go vet ./...` exit 0 (scaffolds compile; later-plan symbols are referenced only via comments/skips).
    - `internal/drift/drift_test.go` `TestTypesAreUpToDate` `t.Skip`s when `contracts/openapi.json` is absent (grep `t.Skip`) and runs `git diff --exit-code types.gen.go` otherwise (grep `exit-code`).
    - `events_test.go` enumerates a 21-entry decode table including `"principles_overflow"` (grep confirms the literal).
    - Every test name in the VALIDATION per-requirement map has a corresponding scaffold function across the test files.
    - `cd sdk/go && go test ./... -run TestTypesAreUpToDate` exits 0 (skips cleanly today).
  </acceptance_criteria>
  <done>Drift gate skip-aware and committed; full RED scaffold set present and compiling; package builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegen input → generated code | The OpenAPI fixture is captured from the live server; a tampered fixture would inject wrong types. Mitigated by checking it in + the drift gate. |
| Go module ↔ Python interpreter (fixture capture + parity test) | Sub-process spawn of `.venv/bin/python`; path comes from a fixed env chain, not untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.3-01-01 | Tampering | `types.gen.go` drifting from the snapshot | mitigate | Drift gate `go test` regenerates + `git diff --exit-code`; checked-in types make tampering reviewable. |
| T-V13.3-01-02 | Tampering | Interpreter resolution in parity/fixture sub-process | mitigate | Interpreter path comes from the fixed `VOSS_PYTHON → .venv/bin/python → python3` chain (no caller-supplied/path-injected arg); args are fixed literals. |
| T-V13.3-01-03 | Elevation/Tampering | No-FFI / no-orchestration-reimpl boundary (SPEC req 7) | mitigate | Wave 0 introduces no `import "C"`; the grep guard is enforced in Plan 06. Asserted here only by build hygiene. |
| T-V13.3-01-SC | Tampering | `go get` of oapi-codegen/runtime packages | mitigate | Both packages verified via Go module proxy in RESEARCH §Package Legitimacy Audit (Approved); versions pinned (v2.7.1 / v1.4.1) in `go.mod`; `go.sum` checksums committed. |
| T-V13.3-01-04 | Information Disclosure | Bearer token in fixture | accept | The fixture is an OpenAPI schema (`create_app('test-token')`); `test-token` is a placeholder build arg, never a live server token — no real secret is committed. |
</threat_model>

<verification>
- `cd sdk/go && go build ./... && go vet ./...` exits 0.
- `cd sdk/go && go test ./... -run 'TestDiscriminatorProbe|TestTypesAreUpToDate'` exits 0 (probe passes; drift gate skips on absent contracts/).
- `sdk/go/types.gen.go` and `sdk/go/testdata/openapi.fixture.json` both committed and contain `EventEnvelope`.
- No `import "C"` introduced anywhere under `sdk/go/`.
</verification>

<success_criteria>
- Module `github.com/vosslang/voss/sdk/go` (`go 1.22`) builds.
- Generated `types.gen.go` checked in, derived from the committed fixture via oapi-codegen v2.7.1 (types-only).
- Discriminator accessor shape empirically verified and recorded for Plan 02.
- Drift gate present and skip-aware (V13.1 not blocking).
- RED scaffolds present for every downstream behavior; package compiles clean.
</success_criteria>

<output>
Create `.planning/phases/V13.3-go-local-headless-client-sdk/V13.3-01-SUMMARY.md` when done. Record the verified `Discriminator()` signature (or fallback decision) explicitly.
</output>
