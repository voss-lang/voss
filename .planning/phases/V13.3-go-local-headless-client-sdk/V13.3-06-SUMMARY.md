# V13.3-06 Summary — PermissionReply + no-FFI guard + TestMain real-server suite (phase close)

**Plan:** V13.3-06 (Wave 4) · **Requirements:** VSDK-GO-07, VSDK-GO-08, VSDK-GO-01 · **Status:** COMPLETE (full suite green)

## What shipped

- `sdk/go/rest.go` — `PermissionReply(ctx, sessionID, id, choice string) (bool, error)` (10th `*Client` method): POST `/session/:id/permission {id,choice}` via the bearer chokepoint, decodes `{status}`, returns `stale = status=="stale"` (not an error); 401/404 → `*VossError`. Choice set `a|A|d|y|n` documented.
- `sdk/go/permission_test.go` — `TestPermissionAllow`/`TestPermissionDeny`: route-contract via direct HTTP (httptest) per D-09 — allow→ok (stale=false), deny→ok recorded (stale=false), unknown/resolved id→stale (stale=true, no error), unauthorized→`*VossError{401}`.
- `sdk/go/noffi_test.go` — `TestNoFFI`: AST-walks every `sdk/go/*.go`, fails on `import "C"` or any `voss/harness`/`voss_runtime` import. Confirmed: package runtime deps are stdlib + `github.com/oapi-codegen/runtime` only.
- `sdk/go/main_test.go` — `TestMain` spawns ONE `VOSS_SERVE_FAKE_TURN` server (when an interpreter is available), shares it via `sharedClient`/`requireShared`, tears it down (no orphan); `pythonAvailable()` gate.
- `sdk/go/rest_test.go` + `sdk/go/sse_test.go` — flipped the gated scaffolds to the real shared server: REST round-trip (201/202/200/204 via create→stream→post→drain→cost→delete), 401, bearer (good vs wrong), 409 (concurrent `sync.WaitGroup` posts, bounded retry + clean skip), SSE ordering (real fake-turn stream: server.connected first / session.idle last / typed plan·delta·final between), SSE cancel (real idle stream → cancel → channel closes, goroutine baseline restored).
- `sdk/go/client_test.go` — `requirePython` now delegates to `pythonAvailable`; duplicate `TestNoFFI` stub removed.
- `V13.3-VALIDATION.md` — `wave_0_complete: true`; per-task map all ✅ green.

## Verification (full suite green)

`VOSS_PYTHON=.../.venv/bin/python go test ./... -timeout 180s`:

- PASS: TestSpawnNoOrphan, TestSpawnBadInterpreter, TestAttachRoundTrip, TestDiscriminatorProbe, TestVossError, TestDecodeAllMembers, TestDecodeUnknownType, TestHandshakeParse, TestAttachClient, TestNoFFI, TestPermissionAllow, TestPermissionDeny, TestIntegrationRestRoundTrip, TestIntegration401, TestBearerHeader, TestIntegrationSSEOrdering, TestIntegrationSSECancel
- SKIP: TestIntegration409 (see below)
- drift pkg: TestTypesAreUpToDate PASS (live: `go generate` + `git diff --exit-code`), TestDecodeCoversAllServerEventTypes PASS (21 types incl. principles_overflow)
- `go build ./...`, `go vet ./...` → exit 0
- No orphan `voss.cli serve` after the run; no cgo; no orchestration import
- `grep -c 'func (c *Client)' rest.go` = 10 (9 from Plan 03 + PermissionReply)

## D-09 permission limitation (recorded)

`VOSS_SERVE_FAKE_TURN` emits NO `permission.updated` event and there is no server seam for a blocking gate without provider creds. So the **route contract** (allow→ok / deny→recorded / stale-id→stale / 401) is verified automatically via direct HTTP; the **SSE-observed loop** (allow→tool-state-ok / deny→denied) is **manual/deferred** per CONTEXT D-09 (recorded in VALIDATION.md §Manual-Only). VSDK-GO-07's helper + route AC is met automatically; the live-gate observation is the documented manual half.

## TestIntegration409 = clean skip (not a failure)

The fake turn runs to completion before the second concurrent `PostMessage` arrives, so the single-threaded-per-session 409 is not reliably triggerable on a fast machine. Per plan (Pitfall 7), the test fires concurrent posts, retries 3×, and `t.Skip`s with a clear message if no 409 is observed (asserting exactly one 202 + one `VossError{409}` when it IS observed). Not a hard fail by design.

## Deviations / notes

- **Integration tests flipped from httptest stubs to the real shared server** (Plan intent). Trade-off: when no interpreter is available they `t.Skip` rather than running hermetically; the permission and unit tests (Decode/VossError/handshake/attach) remain fully hermetic.
- **V13.1 contracts/ switchover:** the `//go:generate` directive + drift gate already target `../../contracts/openapi.json`, which now exists and is live (the D-08 "V13.1 unshipped" premise was already stale as of Plan 01). The fixture (`testdata/openapi.fixture.json`) remains the documented fallback and is byte-identical in generated output. No switchover action remains.

## Phase status

V13.3 (Go Local/Headless Client SDK) is functionally complete across all 6 plans:
VSDK-GO-01 (generated+drift-gated types), -02 (full 21-member event model), -03 (typed REST), -04 (typed SSE), -05 (spawn+attach supervisor), -06 (handshake), -07 (permission + thin-client guard), -08 (build + real-server integration harness). Carry-over deviations: go floor 1.24 (forced by audited deps), in-SDK 3.1→3.0 codegen normalizer (`internal/specgen`), 60s spawn handshake + `LITELLM_LOCAL_MODEL_COST_MAP=true`. Not committed (operator's call).
