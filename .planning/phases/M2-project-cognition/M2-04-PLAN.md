---
phase: M2
plan: 04
type: execute
wave: 3
depends_on: [M1, M2-00, M2-01, M2-02, M2-03]
files_modified:
  - voss/harness/skills/__init__.py
  - voss/harness/skills/analyze.py
  - voss/harness/cli.py
  - voss/harness/cognition.py
  - voss/harness/agent.py
  - tests/harness/test_cli.py
  - tests/harness/test_cognition.py
autonomous: true
requirements:
  - COG-01
  - COG-02
  - COG-04
  - COG-07
tags:
  - harness
  - skills
  - analyze
  - bootstrap

must_haves:
  truths:
    - "/analyze slash command exists in _run_repl and routes to voss.harness.skills.analyze.run."
    - "Natural-language phrases 'analyze repo' / 'analyze this project' / 'update project memory' route via a small intent classifier in _run_repl to the same handler."
    - "/analyze's bootstrap turn produces .voss/project.json, .voss/architecture.md (with full frontmatter: git_head, analyzed_at, file_count, analyzer_version=1), and the three .voss/*.yml configs."
    - "After the bootstrap turn, the harness rebuilds .voss-cache/repo.idx via build_repo_idx and writes .voss/.gitignore with `sessions/` and appends `.voss-cache/` to the project-root .gitignore idempotently."
    - "cognition.write_plan_md helper exists and is callable from cli /save-plan."
    - "/save-plan slash command persists the most-recent Plan from the REPL to .voss/plans/YYYY-MM-DD-<slug>.md with frontmatter keys {id, status: open, related_session, confidence, created_at} — COG-04 observably satisfied (resolves OQ-2)."
  artifacts:
    - path: "voss/harness/skills/__init__.py"
      provides: "package marker"
      contains: ""
    - path: "voss/harness/skills/analyze.py"
      provides: "run(cwd, provider, history, record, renderer, tools, gate) — orchestrates bootstrap prompt + run_turn + post-step indexing"
      contains: "def run"
    - path: "voss/harness/cli.py"
      provides: "/analyze slash branch in _run_repl + natural-language intent classifier + /save-plan slash branch + last_plan REPL-scope tracking + /help text update"
      contains: "/analyze"
    - path: "voss/harness/cognition.py"
      provides: "write_plan_md(cwd, plan, session_id, title) helper + bootstrap_prompt() builder + write_voss_gitignore() helper"
      contains: "def write_plan_md"
    - path: "tests/harness/test_cli.py"
      provides: "test_slash_analyze_routes, test_natural_analyze_routes, test_doctor_cognition_rows placeholder (Wave 4 owner)"
      contains: "def test_slash_analyze_routes"
    - path: "tests/harness/test_cognition.py"
      provides: "test_analyze_writes_project_json, test_architecture_md_frontmatter_well_formed, test_voss_gitignore_autogenerated, test_analyze_invokes_natural_language_route, test_analyze_emits_project_root_gitignore_append, test_save_plan_writes_plan_md, test_save_plan_no_op_when_no_prior_plan, test_save_plan_collision_appends_suffix"
      contains: "def test_analyze_writes_project_json"
  key_links:
    - from: "voss/harness/cli.py::_run_repl"
      to: "voss/harness/skills/analyze.py::run"
      via: "if line == '/analyze': from .skills.analyze import run as analyze_run; analyze_run(...)"
      pattern: "skills\\.analyze"
    - from: "voss/harness/skills/analyze.py::run"
      to: "voss/harness/cognition.py::build_repo_idx, write_voss_gitignore, append_gitignore_line_idempotent"
      via: "post-bootstrap-turn helper calls (machine-side, not via agent)"
      pattern: "build_repo_idx\\|append_gitignore_line_idempotent"
    - from: "voss/harness/cli.py::_run_repl"
      to: "voss/harness/cognition.py::write_plan_md"
      via: "/save-plan slash branch reads last_plan from REPL state and calls write_plan_md(cwd, last_plan, session_id=record.id, title=remainder)"
      pattern: "/save-plan\\|write_plan_md"
    - from: "voss/harness/agent.py::TurnResult.plan"
      to: "voss/harness/cli.py::last_plan"
      via: "_run_repl captures result.plan after each run_turn so /save-plan can serialize the most recent one"
      pattern: "TurnResult.*plan\\|last_plan"
---

<objective>
Ship `/analyze` as a built-in REPL slash command + natural-language route that (1) dispatches a bootstrap LLM turn instructing the agent to write the 5 cognition files via existing `fs_write`, (2) post-step rebuilds `.voss-cache/repo.idx` from `git ls-files`, (3) writes `.voss/.gitignore` and idempotently appends `.voss-cache/` to project-root `.gitignore`, (4) ships the `cognition.write_plan_md` helper, and (5) wires `/save-plan` so the most-recent Plan from the REPL is persisted to `.voss/plans/` on user demand (resolves RESEARCH OQ-2).

Purpose: This is the user-facing entrypoint to M2's cognition lifecycle (D-01, D-02). It turns the inert M2-01 helpers and M2-02 storage into a working "type `/analyze`, get `.voss/`" experience. After this plan, COG-01, COG-02, COG-04, and COG-07 are observably satisfied via `voss chat` → `/analyze` → ls `.voss/`.

Output:
- `voss/harness/skills/__init__.py` + `voss/harness/skills/analyze.py` — new package.
- `voss/harness/cli.py` — `/analyze` slash branch, natural-language route via prefix matching, /help text update.
- `voss/harness/cognition.py` — `bootstrap_prompt()` builder, `write_voss_gitignore(cwd) -> bool`, `write_plan_md(cwd, plan, session_id, title) -> Path`.
- 7 new/unskipped tests across test_cli.py and test_cognition.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/M2-project-cognition/M2-CONTEXT.md
@.planning/phases/M2-project-cognition/M2-RESEARCH.md
@.planning/phases/M2-project-cognition/M2-PATTERNS.md
@voss/harness/cli.py
@voss/harness/cognition.py
@voss/harness/agent.py
@tests/harness/test_cli.py

<interfaces>
From voss/harness/cli.py:262-303 (slash-command chain — insertion point for /analyze, after /save before catch-all):
    if line.startswith("/save"): ...
    if line.startswith("/"): ...  # catch-all unknown

From voss/harness/cli.py:372-386 (_print_slash_help — must append /analyze line).

From voss/harness/cognition.py (M2-01):
    voss_dir(cwd) -> Path
    cache_dir(cwd) -> Path
    build_repo_idx(cwd) -> dict
    append_gitignore_line_idempotent(path, line) -> bool
    slug(title), reserve_filename(dir_, base, ext)

From voss/harness/agent.py (M2-03):
    async def run_turn(task, *, tools, cwd, renderer, ..., cognition=None, session_id=None) -> TurnResult
        — accepts cognition=None (M2-05 will populate it). For /analyze we pass cognition=None explicitly (bootstrap turn skips auto-inject per RESEARCH §"/analyze slash command path" diagram).

Bootstrap prompt requirements (D-02, D-03):
    - Must instruct the agent to read repo via fs_glob + fs_read sampling.
    - Must produce 5 fs_write steps, one per cognition file, writing to the exact paths listed in D-03.
    - architecture.md MUST be ≤ 1-2 pages with sections in order: project name/type, primary language(s), entry points, module map (5-10 dirs with one-line purpose), key dependencies, testing approach.
    - architecture.md MUST start with YAML frontmatter containing: git_head (output of `git rev-parse HEAD` — agent obtains via git_status or shell_run), analyzed_at (ISO timestamp), file_count (from fs_glob), analyzer_version: 1.
    - constraints.yml / permissions.yml / validation.yml: write minimal sane defaults that pass the M2-01 strict pydantic schemas (empty rules / empty tool_policy / empty commands lists are all valid).

Natural-language route (D-01) — keep simple:
    `_classify_intent(line)` returns "analyze" if line.lower() in {
        "analyze repo", "analyze this repo", "analyze this project",
        "update project memory", "refresh cognition", "rebuild cognition"
    } else None. No LLM call for the classifier in M2; literal-match is sufficient.

repo.idx write path:
    cache_dir(cwd) / "repo.idx"
    cache_dir(cwd).mkdir(parents=True, exist_ok=True) before write.

.voss/.gitignore content (D-09):
    "# voss session state and rebuildable cache\nsessions/\n"

Project-root .gitignore handling:
    Already contains ".voss-cache/" per file inspection. append_gitignore_line_idempotent is no-op (returns False) in this repo, but must still be called to support new projects.

write_plan_md signature (D-08 + COG-04):
    write_plan_md(cwd: Path, plan: Plan, session_id: str, title: str | None = None) -> Path
    Filename: reserve_filename(cwd/.voss/plans, slug(title or plan.rationale[:40]))
    Frontmatter: id, status="open", related_session, confidence (from plan.confidence), created_at
    Body: plan.rationale + serialized step list
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement skills/analyze.py + cognition helpers + cli /analyze branch</name>
  <files>voss/harness/skills/__init__.py, voss/harness/skills/analyze.py, voss/harness/cognition.py, voss/harness/cli.py</files>
  <read_first>
    - voss/harness/cli.py (lines 261-303 slash branch chain; lines 372-386 /help text)
    - voss/harness/cognition.py (M2-01 — extend with new helpers)
    - voss/harness/agent.py (M2-03 — run_turn signature now accepts cognition + session_id)
    - .planning/phases/M2-project-cognition/M2-CONTEXT.md (§D-01..D-05, D-08, D-09)
    - .planning/phases/M2-project-cognition/M2-PATTERNS.md (§skills/analyze.py — slash dispatch + bootstrap pattern)
    - .planning/phases/M2-project-cognition/M2-RESEARCH.md (§"/analyze slash command path" diagram; §Code Examples for bootstrap)
  </read_first>
  <behavior>
    Coverage tests live in Task 2; this task ships the implementation.

    Functional contract:
    - `/analyze` typed at the REPL prompt invokes `skills.analyze.run(cwd=cwd, provider=provider, history=history, record=record, renderer=renderer, tools=tools, gate=gate)`.
    - Natural-language phrases listed in the interfaces section are caught by `_classify_intent` BEFORE the normal user-turn dispatch and call the same handler.
    - `run(...)` constructs `bootstrap_prompt(cwd)` (from cognition.py), then `asyncio.run(run_turn(bootstrap_prompt, ..., cognition=None, session_id=record.id))`.
    - After run_turn returns, regardless of whether every fs_write succeeded:
        - Call `build_repo_idx(cwd)` and write its JSON to `cache_dir(cwd) / "repo.idx"`.
        - Call `write_voss_gitignore(cwd)`.
        - Call `append_gitignore_line_idempotent(cwd / ".gitignore", ".voss-cache/")`.
    - Print a summary via renderer.show_final or click.echo: "cognition initialized: project.json, architecture.md, *.yml + repo.idx (N files indexed)".
  </behavior>
  <action>
    1. Create `voss/harness/skills/__init__.py` (empty file).
    2. Create `voss/harness/skills/analyze.py`. Imports: from __future__ import annotations; import asyncio, json; from pathlib import Path; import click; from .. import cognition; from ..agent import run_turn.
    3. Define `async def _run_bootstrap(cwd, provider, history, record, renderer, tools, gate)`:
        - prompt = cognition.bootstrap_prompt(cwd)
        - result = await run_turn(prompt, tools=tools, cwd=cwd, renderer=renderer, model=record.model, provider=provider, history=history, permissions=gate, cognition=None, session_id=record.id)
        - return result
    4. Define `def run(*, cwd, provider, history, record, renderer, tools, gate) -> None`:
        - asyncio.run(_run_bootstrap(...))
        - idx = cognition.build_repo_idx(cwd)
        - idx_path = cognition.cache_dir(cwd) / "repo.idx"
        - idx_path.parent.mkdir(parents=True, exist_ok=True)
        - idx_path.write_text(json.dumps(idx, indent=2))
        - cognition.write_voss_gitignore(cwd)
        - cognition.append_gitignore_line_idempotent(cwd / ".gitignore", ".voss-cache/")
        - click.echo(f"cognition initialized: .voss/ + .voss-cache/repo.idx ({len(idx['files'])} files indexed)")
    5. Edit voss/harness/cognition.py:
        - Add `def write_voss_gitignore(cwd) -> bool`: target = voss_dir(cwd) / ".gitignore"; voss_dir(cwd).mkdir(parents=True, exist_ok=True); if target.exists() return False; target.write_text("# voss session state and rebuildable cache\nsessions/\n"); return True.
        - Add `def bootstrap_prompt(cwd) -> str`: returns a multi-line string instructing the agent (per D-02/D-03/D-04) to:
            1. Use `git_status` and `fs_glob` to inventory the repo.
            2. Run `shell_run` with `git rev-parse HEAD` to obtain the current sha.
            3. Sample a few key files via `fs_read` to characterize the project.
            4. Produce 5 fs_write steps with these exact paths and contracts:
                - `.voss/project.json` — JSON matching ProjectMeta: {name, type, primary_language, entry_points}.
                - `.voss/architecture.md` — must start with YAML frontmatter exactly containing git_head/analyzed_at/file_count/analyzer_version=1, followed by sections in this order: # Project, ## Primary language, ## Entry points, ## Module map, ## Key dependencies, ## Testing approach. Total length ≤ 1-2 printed pages.
                - `.voss/constraints.yml` — `rules: []` (start empty; user populates).
                - `.voss/permissions.yml` — `tool_policy: {allow: [], deny: []}\npath_scopes: []`.
                - `.voss/validation.yml` — `commands: []`.
            5. Set Plan.final_when_done to a one-line summary.
            6. Do NOT write `.voss/.gitignore` or modify root `.gitignore` — the harness handles those.
            7. Do NOT touch `.voss-cache/` — the harness rebuilds the index.
        - Add `def write_plan_md(cwd, plan, session_id, title=None) -> Path`:
            - plans_dir = voss_dir(cwd) / "plans"; plans_dir.mkdir(parents=True, exist_ok=True)
            - base = slug(title or plan.rationale[:40] or "plan")
            - path = reserve_filename(plans_dir, base)
            - id_str = path.stem
            - created_at = datetime.now(timezone.utc).isoformat(timespec="seconds")
            - body lines: "## Rationale", plan.rationale, "## Steps", then per-step "- {step.name}({step.args}) — {step.why}".
            - content = f"---\nid: {id_str}\nstatus: open\nrelated_session: {session_id}\nconfidence: {plan.confidence:.2f}\ncreated_at: {created_at}\n---\n\n# {title or 'Plan'}\n\n" + "\n".join(body_lines) + "\n"
            - path.write_text(content); return path.
    6. Edit voss/harness/cli.py:
        - Add `from .skills.analyze import run as analyze_run` at the top imports (so the slash branch reference is symbol-checked at import time, not runtime). Actually defer to inline import inside the branch to avoid circular imports (since skills.analyze imports from .agent and .cognition; cli already imports from .agent — inline import is safer per PATTERNS.md).
        - Add `_classify_intent(line: str) -> str | None` helper just above `_run_repl`. Returns "analyze" if line.lower().strip() in the analyze-trigger set; else None.
        - Inside the REPL loop, after `if line.startswith("/save"): ... continue`, add:
            ```
            if line == "/analyze":
                from .skills.analyze import run as analyze_run
                analyze_run(cwd=cwd, provider=provider, history=history,
                            record=record, renderer=renderer, tools=tools, gate=gate)
                continue
            ```
        - Before `renderer.show_user(line)` (line ~305), insert intent classification:
            ```
            if _classify_intent(line) == "analyze":
                from .skills.analyze import run as analyze_run
                analyze_run(cwd=cwd, provider=provider, history=history,
                            record=record, renderer=renderer, tools=tools, gate=gate)
                continue
            ```
        - Update `_print_slash_help` to append `"/analyze       refresh project cognition (.voss/, .voss-cache/)"` line.
  </action>
  <verify>
    <automated>cd /Users/benjaminmarks/Projects/Voss && python -c "from voss.harness.skills.analyze import run; from voss.harness.cognition import write_voss_gitignore, write_plan_md, bootstrap_prompt; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ls voss/harness/skills/__init__.py voss/harness/skills/analyze.py` lists both files.
    - `grep -c "^def run\\|^async def _run_bootstrap" voss/harness/skills/analyze.py` returns at least 2.
    - `grep -c "def write_voss_gitignore\\|def bootstrap_prompt\\|def write_plan_md" voss/harness/cognition.py` returns at least 3.
    - `grep -c '"/analyze"' voss/harness/cli.py` returns at least 2 (slash branch + help text).
    - `grep -c "_classify_intent" voss/harness/cli.py` returns at least 2 (definition + call site).
    - `python -c "from voss.harness.cognition import bootstrap_prompt; p=bootstrap_prompt(__import__('pathlib').Path('.')); assert '.voss/architecture.md' in p and 'git_head' in p"` exits 0.
    - `python -m voss.harness --help` succeeds and lists `chat` / `do` / `sessions` (sanity: cli still imports cleanly).
  </acceptance_criteria>
  <done>/analyze slash + natural-language route exist; analyze.run orchestrates bootstrap + post-step indexing; cognition helpers complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unskip 5 cognition tests + add 2 CLI tests for /analyze routing</name>
  <files>tests/harness/test_cognition.py, tests/harness/test_cli.py</files>
  <read_first>
    - tests/harness/test_cognition.py (Wave-2 stubs from M2-00: test_analyze_writes_project_json, test_architecture_md_frontmatter_well_formed, test_voss_gitignore_autogenerated, test_analyze_invokes_natural_language_route, test_analyze_emits_project_root_gitignore_append)
    - tests/harness/test_cli.py (existing structure + CliRunner usage)
    - tests/harness/test_agent_integration.py (FakeProvider patterns from M2-03 — to assemble a stub provider for the analyze bootstrap turn)
    - voss/harness/skills/analyze.py + voss/harness/cognition.py (just-shipped helpers)
  </read_first>
  <behavior>
    - test_voss_gitignore_autogenerated: cognition.write_voss_gitignore(tmp_path) returns True; tmp_path/.voss/.gitignore exists and contains "sessions/". Second call returns False.
    - test_analyze_emits_project_root_gitignore_append: cognition.append_gitignore_line_idempotent(tmp_path/".gitignore", ".voss-cache/") returns True; second call returns False; file contains exactly one .voss-cache/ line.
    - test_analyze_writes_project_json: build a stub provider whose Plan contains 5 fs_write steps with the exact 5 cognition file paths and valid contents; invoke skills.analyze.run(...); assert tmp_path/.voss/project.json exists; assert json.loads(it) passes ProjectMeta.model_validate.
    - test_architecture_md_frontmatter_well_formed: under the same stub-driven bootstrap, assert tmp_path/.voss/architecture.md exists; split via FRONTMATTER_RE; yaml.safe_load the frontmatter; assert keys = {git_head, analyzed_at, file_count, analyzer_version}; analyzer_version == 1.
    - test_analyze_invokes_natural_language_route: use CliRunner to feed "analyze this repo\n/exit\n" into the chat command with a stub provider; assert that the analyze handler ran (proxy: tmp_path/.voss/ now exists, or the click output contains "cognition initialized").
    - test_slash_analyze_routes (in test_cli.py): same stub setup; feed "/analyze\n/exit\n"; assert .voss/ created.
    - test_natural_analyze_routes (in test_cli.py): same as test_analyze_invokes_natural_language_route but lives in test_cli.py for grouping.
    - test_doctor_cognition_rows: leave SKIPPED for M2-06 ownership.
  </behavior>
  <action>
    1. In tests/harness/test_cognition.py: unskip the 5 named tests and implement per <behavior>.
       - For the two that need a bootstrap turn (test_analyze_writes_project_json, test_architecture_md_frontmatter_well_formed), build a `_make_stub_analyze_provider(cwd)` helper that returns a FakeProvider whose Plan contains 5 fs_write steps producing valid file contents. Since FakeProvider returns the Plan verbatim on every complete() call regardless of response_format, and M2-03 adds a second response_format=RunSemantics call, also subclass to return a no-op RunSemantics(goal="analyze") on the closing call.
       - These tests are necessarily integration-flavored: they invoke `skills.analyze.run` with a stub provider rather than a real LLM.
    2. In tests/harness/test_cli.py: add test_slash_analyze_routes and test_natural_analyze_routes using CliRunner. Each test builds a stub auth+provider environment (monkeypatch the auth resolution to return a fake Resolution + the stub provider), invokes `main` with `chat` subcommand + simulated stdin, asserts post-conditions.
       - If CliRunner-driven full REPL is too heavy, factor a minimal `_dispatch_repl_line(line, cwd, provider, ...)` helper into cli.py and unit-test that directly. Choose whichever leads to a 2-task plan that doesn't blow the context budget.
    3. The Wave-3/4 stubs in test_cognition.py (test_analyze_invokes_natural_language_route if not implemented here) and the doctor test in test_cli.py stay skipped.
  </action>
  <verify>
    <automated>cd /Users/benjaminmarks/Projects/Voss && pytest tests/harness/test_cognition.py tests/harness/test_cli.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/harness/test_cognition.py::test_voss_gitignore_autogenerated -v` exits 0.
    - `pytest tests/harness/test_cognition.py::test_architecture_md_frontmatter_well_formed -v` exits 0.
    - `pytest tests/harness/test_cognition.py::test_analyze_writes_project_json -v` exits 0.
    - `pytest tests/harness/test_cli.py -v` reports no regressions; new /analyze routing tests pass.
    - At least 12 tests in test_cognition.py now pass (Wave-1 7-9 from M2-01 + Wave-2 5 from this plan; remaining Wave-3+ stubs still skipped).
    - `grep -c "test_slash_analyze_routes\\|test_natural_analyze_routes" tests/harness/test_cli.py` returns 2.
  </acceptance_criteria>
  <done>/analyze produces the 5 cognition files + .voss-cache/repo.idx + the two .gitignore edits, exercised via stub-provider integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire /save-plan slash command + track last Plan in REPL + integration test asserting plans/*.md persistence</name>
  <files>voss/harness/cli.py, voss/harness/agent.py, tests/harness/test_cognition.py</files>
  <read_first>
    - voss/harness/cli.py (slash branch chain near /analyze just added in Task 1; _run_repl loop around line 280-320 where run_turn results land)
    - voss/harness/agent.py (TurnResult shape from M2-03 1a — `plan` attribute? confirm; if Plan is not on TurnResult yet, this task adds it as an optional carry)
    - voss/harness/cognition.py (write_plan_md helper just added in Task 1)
    - .planning/phases/M2-project-cognition/M2-RESEARCH.md (§Open Questions OQ-2 resolution — /save-plan is the explicit M2 trigger)
    - .planning/phases/M2-project-cognition/M2-CONTEXT.md (§D-08 — plan persistence shape; COG-04 requirement)
  </read_first>
  <behavior>
    - test_save_plan_writes_plan_md (test_cognition.py, stub-provider integration): build a tmp_path with a stub provider whose Plan has rationale="refactor X for clarity" and confidence=0.9; drive one turn through _run_repl (or the factored `_dispatch_repl_line` helper from Task 2 if present) that produces a TurnResult; then feed "/save-plan" as the next line; assert `tmp_path/.voss/plans/YYYY-MM-DD-refactor-x-for-clarity.md` exists and contains frontmatter with keys exactly = {id, status, related_session, confidence, created_at}. status == "open"; related_session == record.id; confidence == 0.90; created_at is ISO-parseable.
    - test_save_plan_no_op_when_no_prior_plan (test_cognition.py or test_cli.py): /save-plan typed before any turn has produced a plan emits a friendly stderr message ("no plan to save yet — run a turn first") and creates no file.
    - test_save_plan_collision_appends_suffix (test_cognition.py): run two turns with the same rationale; /save-plan after each; second file has `-2` suffix per reserve_filename.
  </behavior>
  <action>
    1. Edit voss/harness/agent.py: extend TurnResult dataclass with `plan: "Plan | None" = None` (carries the Plan object so /save-plan can serialize it). Quote the annotation if Plan is defined below TurnResult to avoid forward-ref import cycles. Populate this in run_turn's normal terminal return (TurnResult(..., plan=plan, ...)); on confidence-clarify early-return it can stay None.
    2. Edit voss/harness/cli.py:
       - Hoist a REPL-scope variable `last_plan: Plan | None = None` initialized before the REPL loop (near `bundle = cognition_mod.load(...)` site).
       - After every `result = asyncio.run(run_turn(...))`, set `if result.plan is not None: last_plan = result.plan`.
       - Add a `/save-plan` slash branch after `/analyze` (and before the catch-all). Body:
         ```
         if line.startswith("/save-plan"):
             if last_plan is None:
                 click.echo("no plan to save yet — run a turn first", err=True)
                 continue
             # Optional inline title after the command: "/save-plan refactor X"
             remainder = line[len("/save-plan"):].strip()
             title = remainder or None
             try:
                 path = cognition.write_plan_md(cwd, last_plan, session_id=record.id, title=title)
                 click.echo(f"plan saved: {path.relative_to(cwd)}")
             except OSError as exc:
                 click.echo(f"warning: failed to save plan: {exc}", err=True)
             continue
         ```
       - Update `_print_slash_help` to append: `"/save-plan [title]  persist the most recent plan to .voss/plans/"`.
    3. Update tests/harness/test_cognition.py with the three behaviors above. Prefer reusing the `_make_stub_analyze_provider`-style helper from Task 2 but with a plain (non-fs_write) Plan — one fs_read step is enough; the stub-provider's FakeProviderWithSemantics from M2-03 (Task 2 there) supplies the closing RunSemantics call. If those helpers prove awkward to import across test files, copy a minimal `FakeProviderForSavePlan` inline.
    4. If Task 2 of this plan factored `_dispatch_repl_line(...)` out of `_run_repl` (per its "Choose whichever leads to a 2-task plan" note), reuse that here. Otherwise drive the test via CliRunner with stdin `"some task\n/save-plan\n/exit\n"`.
    5. Document the OQ-2 resolution choice in M2-04-SUMMARY.md (Task 1's output section already lists the SUMMARY contents; extend that list to include "/save-plan slash command — chosen over auto-persist-on-confidence for testability per RESEARCH OQ-2 resolution").
  </action>
  <verify>
    <automated>pytest tests/harness/test_cognition.py tests/harness/test_cli.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' voss/harness/cli.py | grep -c "/save-plan"` returns at least 2 (slash branch + help text).
    - `grep -v '^#' voss/harness/cli.py | grep -c "last_plan"` returns at least 3 (init + update + read).
    - `grep -v '^#' voss/harness/agent.py | grep -c "plan: " | head -1` shows the new TurnResult.plan field is declared.
    - `grep -c "def test_save_plan_writes_plan_md" tests/harness/test_cognition.py` returns 1.
    - `pytest tests/harness/test_cognition.py::test_save_plan_writes_plan_md -v` exits 0; the test creates `.voss/plans/YYYY-MM-DD-<slug>.md` with the 5 required frontmatter keys.
    - `pytest tests/harness/test_cognition.py::test_save_plan_no_op_when_no_prior_plan -v` exits 0.
    - `pytest tests/harness/test_cognition.py::test_save_plan_collision_appends_suffix -v` exits 0.
    - COG-04 (ROADMAP success criterion 2 "Agent plans saved under .voss/plans/") is now observably satisfied by an automated test that asserts file existence + frontmatter shape.
  </acceptance_criteria>
  <done>/save-plan slash command persists the most-recent Plan to .voss/plans/ with COG-04-compliant frontmatter; three tests prove the contract; COG-04 is wired end-to-end.</done>
</task>

</tasks>

<verification>
- `pytest tests/harness/ -x` exits 0.
- Manual: in a scratch dir, `voss chat` → `/analyze` → confirm `.voss/architecture.md` + `.voss/project.json` + 3 yml files + `.voss-cache/repo.idx` exist, frontmatter is well-formed, and `.voss/.gitignore` ignores `sessions/`.
- `cat .gitignore` (in any project) still has exactly one `.voss-cache/` line after multiple `/analyze` runs (idempotence).
</verification>

<success_criteria>
- COG-01 (.voss/project.json), COG-02 (.voss/architecture.md), COG-07 (.voss-cache/repo.idx + .gitignore policy) have observable behavior triggered by `/analyze`. COG-04 (plans/*.md) is observably satisfied by `/save-plan` writing to `.voss/plans/` with required frontmatter.
- Natural-language route works without requiring an LLM classifier (literal-prefix match is enough for M2).
- Bootstrap turn runs with cognition=None (no auto-inject — that's M2-05's job); analyze still produces a coherent architecture.md.
- Two .gitignore edits are idempotent — `/analyze` can be re-run safely without duplicate lines or overwritten user customizations.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| natural-language route → handler | user input bypasses the slash-prefix gate via intent classification |
| agent fs_write → .voss/*.md | LLM-authored content lands in committed project files |
| project-root .gitignore append | harness mutates a file outside `.voss/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M2-13 | Tampering | malicious user prompt "analyze repo" triggers /analyze unintentionally | accept | Intent classifier uses literal lowercased prefix match against a fixed allowlist (no LLM). False-positive rate is bounded; user can /exit before /analyze begins fs_writes (each fs_write still goes through PermissionGate). |
| T-M2-14 | Tampering | agent writes outside `.voss/` during bootstrap (e.g. fs_write to /etc) | mitigate | All fs_write calls go through M1 sandbox.jail_path; harness post-step writes (.voss-cache/repo.idx, .voss/.gitignore, .gitignore append) all use jail_path via cognition helpers. |
| T-M2-15 | Tampering | project-root .gitignore loses user content if append helper rewrites it | mitigate | append_gitignore_line_idempotent only appends (open mode "a") and only when the line is absent; never rewrites existing content. Test: write a project gitignore with arbitrary content, run helper, diff shows only the new line added. |
| T-M2-16 | Input Validation | malformed Plan from analyze bootstrap (e.g. agent writes architecture.md without frontmatter) | mitigate | M2-01's `cognition.load` returns initialized=True with `architecture_frontmatter=None` and pushes a load_errors entry; user sees the error at next REPL boot and re-runs /analyze. Loud-fail, not silent. |
</threat_model>

<output>
After completion, create `.planning/phases/M2-project-cognition/M2-04-SUMMARY.md` documenting: (1) bootstrap_prompt structure (sections enforced + frontmatter contract), (2) post-step machine actions in order, (3) intent classifier phrase allowlist, (4) `write_plan_md` frontmatter shape, (5) idempotence guarantees for the two .gitignore writes.
</output>
