---
phase: V13.1-typescript-local-client-sdk
plan: 06
type: execute
wave: 4
depends_on: ["V13.1-05"]
files_modified:
  - sdk/typescript/tests/helpers/serve-fixture.ts
  - sdk/typescript/tests/rest.test.ts
  - sdk/typescript/tests/sse.test.ts
  - sdk/typescript/tests/permission.test.ts
  - sdk/typescript/tests/launcher.test.ts
autonomous: true
requirements: [VSDK-TS-07, VSDK-TS-02, VSDK-TS-03, VSDK-TS-04, VSDK-TS-05]
must_haves:
  truths:
    - "An integration suite spawns a real `voss serve` (VOSS_SERVE_FAKE_TURN) and exercises REST, SSE, permission, and launcher surfaces, green in CI"
    - "REST test: create→message(202)→cost→delete(204); 401 (bad token) and 409 (message during turn) surface as VossApiError with the right status"
    - "SSE test: server.connected first, session.idle last, typed events between; consumer abort closes cleanly with no unhandled rejection"
    - "Permission test: `a` unblocks an fs_write gate (next tool event ok); `d` blocks it"
    - "Launcher test: spawns serve, runs a turn end-to-end, leaves no orphan process on dispose (PID-verified)"
  artifacts:
    - path: "sdk/typescript/tests/helpers/serve-fixture.ts"
      provides: "spawnVossServe helper (real serve, VOSS_SERVE_FAKE_TURN, teardown)"
      contains: "VOSS_SERVE_FAKE_TURN"
    - path: "sdk/typescript/tests/rest.test.ts"
      provides: "REST CRUD + 401 + 409 integration tests"
      contains: "VossApiError"
    - path: "sdk/typescript/tests/sse.test.ts"
      provides: "SSE ordering + abort integration tests"
      contains: "session.idle"
    - path: "sdk/typescript/tests/permission.test.ts"
      provides: "Permission a-unblocks / d-blocks integration tests"
      contains: "permission"
    - path: "sdk/typescript/tests/launcher.test.ts"
      provides: "Launcher turn + orphan-PID-free dispose tests"
      contains: "pid"
  key_links:
    - from: "sdk/typescript/tests/helpers/serve-fixture.ts"
      to: "voss.cli serve"
      via: "spawn with VOSS_SERVE_FAKE_TURN=1, stdin piped"
      pattern: "VOSS_SERVE_FAKE_TURN"
    - from: "sdk/typescript/tests/*.test.ts"
      to: "sdk/typescript/src (rest/sse/permission/launcher)"
      via: "import the SDK surfaces under test"
      pattern: "from \"../src"
---

<objective>
Build the integration test harness (vitest) that spawns a real `voss serve` via `VOSS_SERVE_FAKE_TURN` and exercises all four SDK surfaces end-to-end: REST (create→message→cost→delete + 401/409 typed errors), SSE (ordering + clean abort), permission (a-unblocks / d-blocks), and the Node launcher (turn end-to-end + orphan-free dispose). Resolve RESEARCH open questions A2 (`voss[server]` extra present) and A4 (`VOSS_SERVE_FAKE_TURN` needs no `.voss/` scaffold in an empty tmp cwd).

Purpose: VSDK-TS-07 — the suite is the faithful test of handshake, Bearer auth, SSE framing, supervision, and the permission round-trip; it provides the behavioral acceptance for VSDK-TS-02/03/04/05 that the earlier compile-only plans deferred. This is the phase verification gate.
Output: a shared serve fixture + four green integration test files, runnable via `npm test`.
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-SPEC.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md
@.planning/PROTOCOL.md
@sdk/typescript/src/client/rest.ts
@sdk/typescript/src/client/sse.ts
@sdk/typescript/src/client/permission.ts
@sdk/typescript/src/launcher/launcher.ts

<interfaces>
<!-- fixture pattern, fake-turn seam, PID check, env questions A2/A4. -->

Fixture (RESEARCH Pattern 9, mirrors tests/harness/mcp/test_mcp_serve_e2e.py:38-95):
- spawn `process.env.PYTHON_BIN ?? "python3"` with `["-m","voss.cli","serve"]`, cwd = a tmp dir, `stdio:["pipe","pipe","inherit"]`, env `{...process.env, VOSS_SERVE_FAKE_TURN:"1"}`.
- Read one handshake line → {v,port,token}; 10s timeout; reject on early exit.
- teardown: `proc.stdin?.end()` + `proc.kill("SIGTERM")`.
- IMPORTANT (repo convention, memory voss-python-interpreter): the tested interpreter is `.venv/bin/python`. CI/local run MUST set `PYTHON_BIN=.venv/bin/python` (absolute path) so `voss` + the `[server]` extra resolve. The npm test invocation: `PYTHON_BIN="$PWD/../../.venv/bin/python" npm test` from `sdk/typescript/`, or document an absolute path.

VOSS_SERVE_FAKE_TURN seam (app.py:40-43 _resolve stub, :92/:166 fake turn — verified):
- Stubs provider resolution (no creds) AND emits a canned turn: server.connected → user → thinking → plan → stream.delta("hello ") → stream.delta("from fake turn") → stream.finalize → final("echo: <input>") → session.idle.
- A2 [BLOCKING]: `voss serve` imports require the `[server]` extra (uvicorn, sse-starlette). The fixture MUST fail with a clear message if the import is missing; verify the extra is installed in `.venv` (`pip install 'voss[server]'` if absent).
- A4 [BLOCKING]: confirm a fresh empty tmp cwd needs NO `.voss/` scaffolding for the fake turn (create session, post message, drive turn in a bare tmp dir). If it DOES, the fixture scaffolds the minimal `.voss/`.

PID check (VSDK-TS-05 orphan acceptance): after launcher.dispose(), assert the child pid is gone — e.g. `process.kill(pid, 0)` throws ESRCH (no such process). Allow a short poll for SIGTERM to take effect.

Endpoint status codes (verified): create 201, message 202, delete 204, 401 bad token, 409 message-during-turn, permission 200.
fs_write gate: a turn triggering an fs_write permission emits `permission.updated{id,tool_name,args,dimension}`; replying `a` → next `tool` event `state: ok`; `d` blocks. (Under FAKE_TURN the canned turn does not gate; the permission test may need a real-ish tool turn — if FAKE_TURN never gates, drive the gate via the documented seam or mark this single test as requiring the real turn path and gate it behind an env flag, keeping the rest of the suite green. Resolve during execution by inspecting whether FAKE_TURN can emit a gate; if not, the permission round-trip is verified against the permission ENDPOINT directly: POST a reply for a known pending id and assert {status:"ok"|"stale"}, plus a unit-level assertion that replyPermission posts the §7 body.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared serve fixture + REST integration tests (resolve A2/A4)</name>
  <read_first>
    - tests/harness/mcp/test_mcp_serve_e2e.py (the verified spawn/handshake/teardown analog)
    - voss/harness/server/app.py (VOSS_SERVE_FAKE_TURN seam :40-43, :92, :166; endpoint status codes)
    - sdk/typescript/src/client/rest.ts (createVossClient + VossApiError under test)
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md (Pattern 9 fixture; Open Q A2/A4; Pitfall 7 stdin piped; Environment Availability)
  </read_first>
  <files>sdk/typescript/tests/helpers/serve-fixture.ts, sdk/typescript/tests/rest.test.ts</files>
  <action>
    Create `tests/helpers/serve-fixture.ts` exporting `spawnVossServe(cwd: string): Promise<ServeFixture>` per RESEARCH Pattern 9: spawn `process.env.PYTHON_BIN ?? "python3"` with `["-m","voss.cli","serve"]`, `stdio:["pipe","pipe","inherit"]`, env merged with `VOSS_SERVE_FAKE_TURN:"1"`, cwd = a vitest `tmpdir` (resolve A4: try a bare empty tmp dir first; if the fake turn fails for lack of `.voss/`, scaffold the minimal dir and document it in the summary). Read one handshake line (10s timeout, reject on early exit). Return `{ baseUrl, token, pid, proc, teardown() }`. Make the import failure of `voss serve` (missing `[server]` extra — A2) surface as a clear fixture error so a missing extra is diagnosable; verify in the summary that `.venv` has the extra (run `.venv/bin/python -c "import uvicorn, sse_starlette"`).

    Create `tests/rest.test.ts` using the fixture: (1) create session (expect id), post a message (expect 202), read cost (expect a typed cost body), delete (expect 204) — the full VSDK-TS-02 happy path against the real server. (2) Build a client with a WRONG token and assert a request throws/returns `VossApiError` with `status === 401`. (3) Post a message, then immediately post a second message while the turn runs and assert `VossApiError` with `status === 409` (the concurrency rule, PROTOCOL §4). Use `afterEach`/`afterAll` to call `teardown()`. Run with `PYTHON_BIN` pointed at `.venv/bin/python`.
  </action>
  <verify>
    <automated>cd sdk/typescript && PYTHON_BIN="$(cd ../.. && pwd)/.venv/bin/python" npx vitest run tests/rest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdk/typescript && PYTHON_BIN=<abs .venv python> npx vitest run tests/rest.test.ts` exits 0 (all REST tests green).
    - The suite spawns a real `voss serve` (fixture references `VOSS_SERVE_FAKE_TURN`) — `grep -q VOSS_SERVE_FAKE_TURN tests/helpers/serve-fixture.ts`.
    - The create→message(202)→cost→delete(204) path and the 401 + 409 VossApiError assertions are present (`grep -c "VossApiError" tests/rest.test.ts` ≥ 2; `grep -c "409" tests/rest.test.ts` ≥ 1).
    - A2 resolved: `.venv/bin/python -c "import uvicorn, sse_starlette"` exits 0 (extra present; otherwise summary documents `pip install 'voss[server]'`).
    - A4 resolved: the summary states whether a bare tmp cwd sufficed or minimal `.voss/` scaffolding was needed.
  </acceptance_criteria>
  <done>Fixture spawns a real server; REST happy path + 401/409 typed errors pass; A2/A4 resolved and documented.</done>
</task>

<task type="auto">
  <name>Task 2: SSE, permission, and launcher integration tests</name>
  <read_first>
    - sdk/typescript/tests/helpers/serve-fixture.ts (the fixture from Task 1)
    - sdk/typescript/src/client/sse.ts (subscribeToEvents under test)
    - sdk/typescript/src/client/permission.ts (replyPermission under test)
    - sdk/typescript/src/launcher/launcher.ts (VossLauncher under test)
    - .planning/PROTOCOL.md (§6 SSE order, §7 permission, §8 abort)
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md (fake-turn sequence; permission shapes; orphan-PID check)
  </read_first>
  <files>sdk/typescript/tests/sse.test.ts, sdk/typescript/tests/permission.test.ts, sdk/typescript/tests/launcher.test.ts</files>
  <action>
    Create `tests/sse.test.ts`: with the fixture, open an SSE stream, post a message (FAKE_TURN), collect yielded events, and assert the FIRST is `server.connected` and the LAST is `session.idle`, with at least one `plan`/`stream.delta`/`final` typed event between (VSDK-TS-03 ordering). Add an abort test: start consuming, abort the AbortController mid-turn, and assert the async iteration ends WITHOUT an unhandled rejection (use a rejection listener or assert the loop exits cleanly).

    Create `tests/permission.test.ts`: verify the permission round-trip (VSDK-TS-04). Per the interface note, FAKE_TURN's canned turn may not emit a real gate — resolve at execution: if a gate can be driven, assert `a` unblocks (subsequent `tool` event `state: ok`) and `d` blocks; if FAKE_TURN cannot gate, verify `replyPermission` posts the §7 body and the `POST /session/:id/permission` endpoint returns `{status:"ok"|"stale"}` for a reply, AND keep the strict-choice-type assertion. Document the chosen path in the summary.

    Create `tests/launcher.test.ts`: use `VossLauncher` (from `../src/node` or `../src/launcher/launcher`) with `python` set to the `.venv` interpreter and `VOSS_SERVE_FAKE_TURN` in env; assert `start()` returns a working client (run a create→message→read turn end-to-end), capture the pid, call `dispose()`, then poll-assert the pid is gone via `process.kill(pid, 0)` throwing `ESRCH` (VSDK-TS-05 orphan-free). Tear down in `afterEach`. Finally confirm the WHOLE suite is green with `npm test` (all four files).
  </action>
  <verify>
    <automated>cd sdk/typescript && PYTHON_BIN="$(cd ../.. && pwd)/.venv/bin/python" npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdk/typescript && PYTHON_BIN=<abs .venv python> npx vitest run` exits 0 (full suite: rest + sse + permission + launcher green) — VSDK-TS-07.
    - `tests/sse.test.ts` asserts `server.connected` first and `session.idle` last (`grep -q "server.connected" tests/sse.test.ts && grep -q "session.idle" tests/sse.test.ts`) and includes an abort test with no unhandled rejection.
    - `tests/permission.test.ts` exercises the §7 reply (`a`/`d` or endpoint-level) — `grep -q "permission" tests/permission.test.ts`.
    - `tests/launcher.test.ts` runs a turn via the launcher and asserts the child pid is gone after dispose (`grep -q "ESRCH\\|kill(.*, 0)\\|process.kill" tests/launcher.test.ts`).
  </acceptance_criteria>
  <done>SSE ordering+abort, permission round-trip, and launcher turn+orphan-free dispose all pass; the full integration suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned servers | Each test spawns real `voss serve` children that must be torn down to avoid orphan accumulation in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.1-15 | Denial of Service | leaked `voss serve` children across tests | mitigate | Every test uses `afterEach`/`afterAll` teardown (stdin EOF + SIGTERM); launcher test explicitly PID-verifies no orphan remains |
| T-V13.1-16 | Information Disclosure | provider creds in CI | accept | `VOSS_SERVE_FAKE_TURN` stubs provider resolution so no real credentials are needed in CI (the locked CI seam) |
| T-V13.1-17 | Tampering | tests passing against a stale build | mitigate | Suite imports `../src` (source under test) and runs after Wave-3 build; phase gate runs `npm run build` + `check:core-imports` + Python drift gate together |
</threat_model>

<verification>
- `cd sdk/typescript && PYTHON_BIN=<abs .venv python> npx vitest run` exits 0 (all four files).
- A2: `.venv/bin/python -c "import uvicorn, sse_starlette"` exits 0.
- A4 resolution documented in the summary.
- Phase gate (run together): `npx vitest run` + `npm run check:core-imports` + `npm run build` + `.venv/bin/python -m pytest tests/harness/server/test_contract_drift.py`.
</verification>

<success_criteria>
- Integration suite green against a real launched `voss serve` in CI (VSDK-TS-07).
- Behavioral acceptance for VSDK-TS-02 (REST CRUD + 401/409), VSDK-TS-03 (SSE order + abort), VSDK-TS-04 (permission), VSDK-TS-05 (launcher orphan-free) all proven.
- A2 and A4 resolved and documented.
</success_criteria>

<output>
Create `.planning/phases/V13.1-typescript-local-client-sdk/V13.1-06-SUMMARY.md` when done.
</output>
