---
phase: V13.1
slug: typescript-local-client-sdk
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-07
---

# Phase V13.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

Two test surfaces (this phase spans Python exporter + TS package):

| Property | Value |
|----------|-------|
| **Framework (TS SDK)** | vitest |
| **Framework (contract exporter + drift gate)** | pytest (existing; mirror `tests/codegen/test_snapshots.py` + `tests/harness/mcp/test_mcp_serve_e2e.py`) |
| **Config file** | `sdk/typescript/vitest.config.ts` (Wave 0 creates) · existing `pyproject.toml`/pytest for the drift gate |
| **Quick run command** | `cd sdk/typescript && npx vitest run` |
| **Full suite command** | `cd sdk/typescript && npx vitest run` + `.venv/bin/python -m pytest tests/<contract-drift-test>` |
| **Estimated runtime** | ~30–60s (integration spawns a real `voss serve`) |

---

## Sampling Rate

- **After every task commit:** Run the quick TS run (`npx vitest run`) and, for exporter tasks, the drift-gate pytest.
- **After every plan wave:** Run the full suite (TS + Python drift gate).
- **Before `/gsd-verify-work`:** Full suite green, including the negative drift-gate test (must FAIL on an un-snapshotted schema change).
- **Max feedback latency:** ~60 seconds.

---

## Per-Task Verification Map

> Populated by the planner from PLAN.md tasks. Each task maps to an automated command or a Wave 0 dependency.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| V13.1-01-01 | 01 | 0 | VSDK-TS-08 | — | snapshot byte-stable | unit | `.venv/bin/python -m pytest tests/<drift>` | ❌ W0 | ⬜ pending |

---

## Wave 0 Requirements

- [ ] Contract exporter + committed `contracts/openapi.json` + event-union schema (VSDK-TS-08) — drift-gate pytest stub
- [ ] `sdk/typescript/` package scaffold (package.json exports split, tsup, vitest config)
- [ ] Open questions to resolve in Wave 0 (from RESEARCH): A1 — inspect generated `types.ts` confirms `oneOf`+discriminator → TS string-literal `type`; A2 — `voss[server]` extra installed in CI; A4 — `VOSS_SERVE_FAKE_TURN` needs no `.voss/` scaffold in an empty tmp cwd.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Browser-target build consumes core entry | VSDK-TS-06 | No headless browser in CI by default | Build a tiny Vite app importing `@vosslang/sdk` core; confirm it bundles without `node:*` resolution errors |

*All other phase behaviors have automated verification (REST/SSE/permission/launcher integration vs real `voss serve`; drift gate; no-`node:` lint).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (vitest run, not vitest watch)
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
