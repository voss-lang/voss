---
phase: V13.1-typescript-local-client-sdk
plan: 04
type: execute
wave: 2
depends_on: ["V13.1-02"]
files_modified:
  - sdk/typescript/src/client/sse.ts
autonomous: true
requirements: [VSDK-TS-03]
must_haves:
  truths:
    - "The SSE client opens GET /session/:id/events with Bearer auth and yields typed AgentEvent values as an async iterable"
    - "Each SSE data payload is parsed and typed against the AgentEvent discriminated union"
    - "Consumer abort (AbortSignal) closes the stream cleanly with no unhandled rejection and cancels the in-flight turn"
  artifacts:
    - path: "sdk/typescript/src/client/sse.ts"
      provides: "fetch-body + eventsource-parser async iterable of typed AgentEvent"
      contains: "EventSourceParserStream"
  key_links:
    - from: "sdk/typescript/src/client/sse.ts"
      to: "GET /session/:id/events"
      via: "fetch with Authorization: Bearer + AbortSignal"
      pattern: "Bearer"
    - from: "sdk/typescript/src/client/sse.ts"
      to: "eventsource-parser/stream"
      via: "pipeThrough(new EventSourceParserStream())"
      pattern: "EventSourceParserStream"
    - from: "sdk/typescript/src/client/sse.ts"
      to: "sdk/typescript/src/generated/types.ts"
      via: "yields typed AgentEvent"
      pattern: "AgentEvent"
---

<objective>
Implement the browser-safe SSE typed-event client (`src/client/sse.ts`): open `GET /session/:id/events` with `Authorization: Bearer <token>`, stream the response body through `eventsource-parser/stream`, parse each `data` payload, and yield typed `AgentEvent` values as an async iterable. Support clean teardown on consumer abort via `AbortController` (which also cancels the in-flight turn, PROTOCOL §8).

Purpose: The event-stream surface (VSDK-TS-03). Native `EventSource` is rejected (can't send Bearer, absent in Node) — fetch body + eventsource-parser is the isomorphic path (D-04). Runs parallel with the REST client (Plan 03); no shared files.
Output: `src/client/sse.ts` (replacing the Wave-1 `export {}` stub).
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-SPEC.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md
@.planning/PROTOCOL.md
@sdk/typescript/src/generated/types.ts
@sdk/typescript/src/errors.ts

<interfaces>
<!-- SSE framing (PROTOCOL §6), abort (§8), lib (D-04). -->

SSE wire framing (sse-starlette EventSourceResponse, PROTOCOL §6):
  event: <type>\n  data: {"v":1,...}\n  id: <seq>\n  \n
- First event ALWAYS `server.connected`. Turn end ALWAYS `session.idle`. The `event:` name matches the `data.type` literal.
- The SSE GET REQUIRES `Authorization: Bearer <token>` (Pitfall 6 — _BearerASGI intercepts before routing; missing → 401, empty stream).

Abort (PROTOCOL §8): passing an AbortController `signal` to `fetch` cancels the in-flight request; the server's SSE generator CancelledError cancels the turn (app.py). No separate POST /abort needed for consumer teardown.

Lib (D-04, RESEARCH Pattern 5 + Pitfall 4): import `{ EventSourceParserStream }` from `eventsource-parser/stream` (the `/stream` subpath — Pitfall 4: not the root import). Pipe: `response.body.pipeThrough(new TextDecoderStream()).pipeThrough(new EventSourceParserStream())`; iterate `for await (const ev of stream)`, `JSON.parse(ev.data)` → typed AgentEvent.

Types: `AgentEvent` from `src/generated/types.ts`. `VossApiError(status, detail)` from `src/errors.ts`.

Fake-turn sequence (RESEARCH Code Examples, VOSS_SERVE_FAKE_TURN): server.connected → user → thinking → plan → stream.delta×2 → stream.finalize → final → session.idle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async-iterable SSE client with Bearer auth + abort teardown</name>
  <read_first>
    - sdk/typescript/src/generated/types.ts (AgentEvent union)
    - sdk/typescript/src/errors.ts (VossApiError)
    - .planning/PROTOCOL.md (§6 SSE taxonomy/framing, §8 abort)
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md (Pattern 5 SSE client; Pitfall 4 /stream subpath; Pitfall 6 Authorization on SSE GET; Code Examples fake-turn sequence)
  </read_first>
  <files>sdk/typescript/src/client/sse.ts</files>
  <behavior>
    - subscribeToEvents(baseUrl, sessionId, token, signal?) opens GET /session/:id/events with `Authorization: Bearer <token>` and yields typed AgentEvent values.
    - The first yielded event is `server.connected`; the last (for a completed turn) is `session.idle`, with typed plan/stream.delta/final in between (verified against VOSS_SERVE_FAKE_TURN in Plan 06).
    - A non-ok response (e.g. 401) throws VossApiError(status, detail) rather than yielding an empty stream silently.
    - Aborting the passed AbortSignal mid-turn ends the async iteration cleanly with NO unhandled promise rejection.
  </behavior>
  <action>
    Replace the Wave-1 stub `src/client/sse.ts`. Export an async generator `subscribeToEvents(baseUrl: string, sessionId: string, token: string, signal?: AbortSignal): AsyncIterable<AgentEvent>` (RESEARCH Pattern 5). `fetch` the events URL with `headers: { Authorization: "Bearer " + token }` and `signal` (Pitfall 6 — Bearer is mandatory on the SSE GET). If `!response.ok || !response.body`, throw `VossApiError(response.status, <detail-from-body-or-statusText>)`. Pipe `response.body.pipeThrough(new TextDecoderStream()).pipeThrough(new EventSourceParserStream())` — import `EventSourceParserStream` from `eventsource-parser/stream` (Pitfall 4 — the `/stream` subpath, NOT the root). For each parsed message with non-empty `data`, `JSON.parse(ev.data)` and `yield` it typed as `AgentEvent`.

    Handle abort cleanly (PROTOCOL §8): wrap the iteration so that an `AbortError` thrown by the aborted fetch/stream is swallowed (return from the generator) rather than propagating as an unhandled rejection — i.e. catch the `DOMException`/`AbortError` from the aborted signal and end iteration. Do NOT call POST /abort here; consumer disconnect already cancels the turn server-side. Import zero `node:*` (TextDecoderStream / ReadableStream / fetch are web-platform, present in Node 18+).
  </action>
  <verify>
    <automated>cd sdk/typescript && npx tsc --noEmit && grep -q "eventsource-parser/stream" src/client/sse.ts && grep -q "Bearer" src/client/sse.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdk/typescript && npx tsc --noEmit` exits 0.
    - `src/client/sse.ts` imports `EventSourceParserStream` from `eventsource-parser/stream` (the `/stream` subpath) and sets an `Authorization: Bearer` header on the SSE GET.
    - The generator's return type is `AsyncIterable<AgentEvent>` (typed yield, not `any`).
    - `grep -rn "node:" sdk/typescript/src/client/sse.ts` returns empty (browser-safe).
    - The `server.connected`-first / `session.idle`-last ordering and clean-abort (no unhandled rejection) behaviors are verified by Plan 06 `sse.test.ts` against a real `voss serve` driven by VOSS_SERVE_FAKE_TURN.
  </acceptance_criteria>
  <done>SSE client compiles, opens the stream with Bearer auth, yields typed AgentEvent, and tears down cleanly on abort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → consumer | Each `data` payload is parsed JSON from the server; only the loopback authed server can write it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.1-09 | Tampering | prototype pollution / malformed JSON via JSON.parse(ev.data) | mitigate | Parsed value is consumed as the typed AgentEvent union (V5 input-validation); narrowing on `type` discards unknown shapes. Server is loopback + Bearer-authed, so injection requires a compromised server |
| T-V13.1-10 | Denial of Service | orphan SSE connection on consumer crash | accept | PROTOCOL §8: server cancels the in-flight turn on client disconnect; the SSE generator's CancelledError handles teardown server-side |
| T-V13.1-11 | Information Disclosure | bearer token in SSE error message | mitigate | VossApiError carries only status + detail; token never logged |
</threat_model>

<verification>
- `cd sdk/typescript && npx tsc --noEmit` exits 0.
- `grep -rn "node:" sdk/typescript/src/client/sse.ts` empty.
- Ordering + clean-abort behavior verified by Plan 06 integration suite.
</verification>

<success_criteria>
- SSE client yields a typed AgentEvent async iterable with Bearer auth (VSDK-TS-03).
- Clean abort teardown, no unhandled rejection.
- Browser-safe (zero node:*).
</success_criteria>

<output>
Create `.planning/phases/V13.1-typescript-local-client-sdk/V13.1-04-SUMMARY.md` when done.
</output>
