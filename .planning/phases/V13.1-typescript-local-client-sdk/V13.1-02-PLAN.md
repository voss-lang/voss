---
phase: V13.1-typescript-local-client-sdk
plan: 02
type: execute
wave: 1
depends_on: ["V13.1-01"]
files_modified:
  - sdk/typescript/package.json
  - sdk/typescript/tsconfig.json
  - sdk/typescript/tsup.config.ts
  - sdk/typescript/vitest.config.ts
  - sdk/typescript/.dependency-cruiser.cjs
  - sdk/typescript/src/index.ts
  - sdk/typescript/src/errors.ts
  - sdk/typescript/src/generated/types.ts
  - sdk/typescript/tests/types-exhaustive.test-d.ts
autonomous: false
requirements: [VSDK-TS-01]
must_haves:
  truths:
    - "openapi-typescript regenerates sdk/typescript/src/generated/types.ts byte-identically from contracts/openapi.json"
    - "The generated AgentEvent union renders each member's `type` as a string literal (not `string`), enabling exhaustive switch narrowing over all 21 members"
    - "The package scaffold defines an ESM-only exports map: `.` = browser-safe core, `./node` = Node launcher"
    - "A shared VossApiError class carries HTTP status + protocol detail"
  artifacts:
    - path: "sdk/typescript/package.json"
      provides: "@vosslang/sdk ESM package manifest with exports split + codegen/build/test scripts"
      contains: "@vosslang/sdk"
    - path: "sdk/typescript/src/generated/types.ts"
      provides: "Checked-in openapi-typescript output (paths + AgentEvent union)"
      contains: "AgentEvent"
    - path: "sdk/typescript/src/errors.ts"
      provides: "VossApiError typed error class (status + detail)"
      contains: "class VossApiError"
    - path: "sdk/typescript/src/index.ts"
      provides: "Browser-safe core barrel re-exporting types, errors, and client modules"
      contains: "export"
    - path: "sdk/typescript/.dependency-cruiser.cjs"
      provides: "Forbidden-rule config: zero node:* in core graph"
      contains: "forbidden"
  key_links:
    - from: "sdk/typescript/src/generated/types.ts"
      to: "contracts/openapi.json"
      via: "openapi-typescript codegen (committed output)"
      pattern: "AgentEvent"
    - from: "sdk/typescript/package.json"
      to: "contracts/openapi.json"
      via: "codegen script reads ../../contracts/openapi.json"
      pattern: "contracts/openapi\\.json"
---

<objective>
Stand up the `@vosslang/sdk` package skeleton at `sdk/typescript/` and generate the checked-in TS contract types from the Wave-0 snapshot. Establish the isomorphic exports split (`.` core / `./node` launcher), the build (tsup), test (vitest), and boundary-lint (dependency-cruiser) configs, a shared `VossApiError`, and a core barrel that downstream waves extend. Verify RESEARCH open question A1 (Pydantic discriminated union → TS string-literal `type`) by a compile-time exhaustiveness type test, and add the byte-identical TS drift gate.

Purpose: Everything in Waves 2–4 is written against `src/generated/types.ts` and the barrel established here. The barrel pre-declares re-exports of the client modules so Wave 2 plans add files with zero conflict on `index.ts`.
Output: package scaffold + configs + generated types + errors + barrel + exhaustiveness type-test.
</objective>

<execution_context>
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminmarks/Projects/Voss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-SPEC.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-CONTEXT.md
@.planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md
@contracts/openapi.json

<interfaces>
<!-- Locked toolchain (CONTEXT D-01..D-13) + RESEARCH-pinned versions. Use exactly these. -->

Locked decisions:
- D-01/D-12: types via `openapi-typescript` reading `contracts/openapi.json`.
- D-05: package name `@vosslang/sdk`. D-06: location `sdk/typescript/`. D-07: ESM-only (`"type":"module"`).
- D-08: exports map `"."` → core (`./dist/index.js`), `"./node"` → launcher (`./dist/node.js`).
- D-09: build `tsup` (dts:true), test `vitest`.
- D-11: generated types CHECKED IN. D-13(b): TS drift gate = regen + `git diff --exit-code`.

Pinned versions (RESEARCH Standard Stack, all [VERIFIED: npm registry]):
openapi-typescript 7.13.0 · openapi-fetch 0.17.0 · eventsource-parser 3.1.0 · tsup 8.5.1 · vitest 4.1.8 · dependency-cruiser 17.4.3 · typescript (workspace).

Codegen command (RESEARCH Pattern 3): `openapi-typescript ../../contracts/openapi.json -o src/generated/types.ts` (run from `sdk/typescript/`).
tsup config (RESEARCH Pattern 7): entry `{ index: "src/index.ts", node: "src/node.ts" }`, format `["esm"]`, dts true, clean true, splitting false.
dependency-cruiser config (RESEARCH Pattern 8): forbidden rule `core-no-node-builtins`, from `^dist/index\.js$`, to `{ dependencyTypes: ["core"] }`.

A1 (RESEARCH Open Q1 / Pitfall 3): Pydantic `Literal[...]` → JSON Schema `const` → openapi-typescript renders `type: "server.connected"` (string literal). MUST be verified by inspecting the generated file + a compile-time exhaustiveness test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the @vosslang/sdk package + configs + shared error + barrel</name>
  <read_first>
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-CONTEXT.md (D-05..D-13 — locked toolchain)
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md (Recommended Project Structure; Pattern 4 error surfacing; Pattern 7 tsup; Pattern 8 dependency-cruiser)
    - npm/package.json (sibling @vosslang package conventions — org, not a base to extend)
  </read_first>
  <files>sdk/typescript/package.json, sdk/typescript/tsconfig.json, sdk/typescript/tsup.config.ts, sdk/typescript/vitest.config.ts, sdk/typescript/.dependency-cruiser.cjs, sdk/typescript/src/errors.ts, sdk/typescript/src/index.ts</files>
  <action>
    Create `sdk/typescript/package.json`: name `@vosslang/sdk`, `"type": "module"`, `"private": true` (publish out of scope per SPEC), version `0.0.0`. `exports` map per D-08: `"."` → `{ "import": "./dist/index.js", "types": "./dist/index.d.ts" }`, `"./node"` → `{ "import": "./dist/node.js", "types": "./dist/node.d.ts" }`. Scripts: `build` = `tsup`; `codegen` = `openapi-typescript ../../contracts/openapi.json -o src/generated/types.ts`; `check:core-imports` = `depcruise --config .dependency-cruiser.cjs dist/index.js`; `test` = `vitest run`; `typecheck` = `tsc --noEmit`. devDependencies pinned to the exact versions in the interfaces block. Install with `cd sdk/typescript && npm install`.

    Create `tsconfig.json` (ESM, `module`/`moduleResolution` = `NodeNext` or `Bundler`, `strict: true`, `noEmit: true` for typecheck, include `src` + `tests`, lib including `DOM` + `ES2022` so the browser-safe core type-checks against `fetch`/`ReadableStream`/`TextDecoderStream`). Create `tsup.config.ts` per RESEARCH Pattern 7 (two named entries, esm, dts, clean, splitting false, sourcemap). Create `vitest.config.ts` (test environment `node`, no watch — `vitest run` is the command). Create `.dependency-cruiser.cjs` per RESEARCH Pattern 8 (forbidden `core-no-node-builtins`, from `^dist/index\.js$`, to `dependencyTypes: ["core"]`, `options.moduleSystems: ["es6"]`).

    Create `src/errors.ts`: export `class VossApiError extends Error` with `status: number` and `detail: string` fields (per RESEARCH Pattern 4 error surfacing); constructor `(status, detail)` sets a readable message. This is the shared error for REST (Plan 03) and SSE (Plan 04).

    Create `src/index.ts` as the browser-safe core barrel. Re-export `./errors`, `./generated/types` (type re-export), and pre-declare re-exports of the Wave-2 client modules so those plans add files without touching this barrel: `export * from "./client/rest"`, `export * from "./client/sse"`, `export * from "./client/permission"`. NOTE for executor: these three sub-modules do not exist yet (Wave 2 creates them) — leave the re-export lines in place; the typecheck/build verification for THIS plan must tolerate that (run typecheck only on files that exist, or stub the three client modules with empty `export {}` files that Wave 2 overwrites). Prefer stubbing: create empty `src/client/rest.ts`, `src/client/sse.ts`, `src/client/permission.ts` each containing `export {}` so the barrel resolves now and Wave 2 replaces them. Do NOT import `node:*` anywhere in `src/index.ts` or its graph.
  </action>
  <verify>
    <automated>cd sdk/typescript && npm install --silent && node -e "const p=require('./package.json'); if(!p.exports['.']||!p.exports['./node']||p.name!=='@vosslang/sdk') process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/typescript/package.json` has `name: "@vosslang/sdk"`, `"type": "module"`, and an `exports` map with both `"."` and `"./node"` keys.
    - `cd sdk/typescript && npm install` exits 0 and installs the six pinned devDependencies.
    - `sdk/typescript/src/errors.ts` contains `class VossApiError` with `status` and `detail`.
    - `.dependency-cruiser.cjs` contains a `forbidden` rule named `core-no-node-builtins` with `dependencyTypes: ["core"]`.
    - `grep -rn "node:" sdk/typescript/src/index.ts sdk/typescript/src/errors.ts` returns empty.
  </acceptance_criteria>
  <done>Package installs; configs and shared error/barrel exist; no node:* in the core barrel.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + commit TS types and prove exhaustive discriminated-union narrowing (A1)</name>
  <read_first>
    - contracts/openapi.json (the committed Wave-0 snapshot — source for codegen)
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-RESEARCH.md (Pattern 3 codegen; Pitfall 3 / Open Q1 A1; Pitfall 8 wave ordering)
    - sdk/typescript/package.json (the `codegen` script from Task 1)
  </read_first>
  <files>sdk/typescript/src/generated/types.ts, sdk/typescript/tests/types-exhaustive.test-d.ts</files>
  <action>
    Run `cd sdk/typescript && npm run codegen` to generate `src/generated/types.ts` from `../../contracts/openapi.json` (Wave-0 output of Plan 01 — MUST exist first, Pitfall 8). Commit the generated file as-is (D-11 checked-in).

    Resolve A1 ([BLOCKING] — RESEARCH Open Q1): inspect the generated file and confirm each `AgentEvent` member renders its `type` field as a string literal (e.g. `type: "server.connected"`), NOT `type: string`. If openapi-typescript did NOT emit literals (Pitfall 3), STOP and escalate via the decision checkpoint (Task 3) — do not hand-edit generated types.

    Create `sdk/typescript/tests/types-exhaustive.test-d.ts` (a vitest type-test / `tsc --noEmit` compile-time check). Import the generated `AgentEvent` union. Write a function that `switch`es over `event.type` with a `case` for each of the 21 members and a `default` branch asserting `const _exhaustive: never = event` — so the file FAILS to type-check if any member is missing a literal `type` or if a member is unhandled. This is the concrete A1 verification: it only compiles if the union is a true discriminated union over all 21 string literals. List all 21 type strings from PROTOCOL §6 + events.py: `server.connected, session.idle, permission.updated, banner, user, thinking, plan, tool, clarify, final, stream.delta, stream.finalize, status, cognition_loaded, cognition_overflow, principles_overflow, warning, probable, budget.updated, confidence.updated, gate.updated`.
  </action>
  <verify>
    <automated>cd sdk/typescript && npm run codegen && git diff --exit-code src/generated/types.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdk/typescript && npm run codegen` followed by `git diff --exit-code src/generated/types.ts` exits 0 (byte-identical regen — VSDK-TS-01 / D-13b drift gate).
    - `grep -c '"server.connected"' sdk/typescript/src/generated/types.ts` ≥ 1 and `grep -c 'type: string' sdk/typescript/src/generated/types.ts` does not match the AgentEvent members (A1 confirmed: discriminator is a literal).
    - `cd sdk/typescript && npx tsc --noEmit` exits 0 — the exhaustive `switch` with `never` default compiles, proving all 21 members narrow.
    - `grep -c 'case "' sdk/typescript/tests/types-exhaustive.test-d.ts` returns 21.
  </acceptance_criteria>
  <done>Generated types are committed, byte-stable on regen, and proven to be a discriminated union exhaustive over all 21 events.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>A1 fallback — only if openapi-typescript did NOT render `type` as string literals</decision>
  <context>RESEARCH Open Q1 / Pitfall 3: if Pydantic's discriminated `oneOf` did not produce per-member `const`, the generated `AgentEvent.type` may be `string`, breaking exhaustive narrowing. Task 2's `tsc --noEmit` will fail at the `never` default. Hand-editing generated types is forbidden (it reintroduces the drift the gate prevents).</context>
  <options>
    <option id="opt-transform">
      <name>Apply an openapi-typescript transform/flag</name>
      <pros>Keeps types generated; drift gate intact</pros>
      <cons>Requires confirming the right openapi-typescript option for const→literal</cons>
    </option>
    <option id="opt-server-fix">
      <name>Add json_schema_extra const to event models in events.py + re-snapshot (re-run Plan 01)</name>
      <pros>Fixes the contract at the source so all SDKs (V13.2–.4) benefit</pros>
      <cons>Touches the server contract surface; requires re-running the Wave-0 exporter + drift gate</cons>
    </option>
  </options>
  <resume-signal>This checkpoint is a NO-OP if Task 2's `tsc --noEmit` passed (literals rendered correctly — the expected case). Only engage if it failed. Select: opt-transform or opt-server-fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed generated types ↔ contracts snapshot | Generated types must stay in lockstep with `contracts/openapi.json`; a hand-edit silently desyncs |
| package install → supply chain | Six new devDependencies enter the build (npm registry) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V13.1-04 | Tampering | hand-edited src/generated/types.ts | mitigate | TS drift gate (`npm run codegen && git diff --exit-code`) fails on any manual edit |
| T-V13.1-SC | Tampering | npm devDependency installs | mitigate | All six packages [VERIFIED: npm registry] in RESEARCH Package Legitimacy Audit (3–9 yr age, authoritative repos, zero postinstall); see checkpoint below if any flips to [SUS] |
| T-V13.1-05 | Spoofing | wrong/incomplete event union → consumer mis-narrows | mitigate | Exhaustive `never`-default switch type-test compiles only if all 21 literals present |
</threat_model>

<verification>
- `cd sdk/typescript && npm install` succeeds; six pinned devDeps present.
- `npm run codegen && git diff --exit-code src/generated/types.ts` exits 0.
- `npx tsc --noEmit` exits 0 (exhaustive switch compiles).
</verification>

<success_criteria>
- `@vosslang/sdk` scaffold with ESM exports split, build/test/lint configs, shared error, and barrel.
- Checked-in generated types regenerate byte-identically (VSDK-TS-01).
- Discriminated-union exhaustiveness proven over all 21 events (A1 resolved).
</success_criteria>

<output>
Create `.planning/phases/V13.1-typescript-local-client-sdk/V13.1-02-SUMMARY.md` when done.
</output>
