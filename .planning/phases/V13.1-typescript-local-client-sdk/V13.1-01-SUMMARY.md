---
phase: V13.1-typescript-local-client-sdk
plan: "01"
subsystem: sdk-contracts
tags: [typescript-sdk, openapi, json-schema, contract-drift, fastapi, pydantic]

# Dependency graph
requires: []
provides:
  - Deterministic static contract exporter for SDK code generation
  - Git-tracked OpenAPI snapshot containing EventEnvelope and AgentEvent variants
  - Git-tracked standalone EventEnvelope JSON Schema snapshot
  - Python drift gate comparing regenerated contracts byte-for-byte
affects: [V13.1, V13.2, V13.3, V13.4, sdk-codegen, contract-drift]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Static FastAPI contract export through create_app(FIXED_TOKEN).openapi()
    - Stable JSON serialization with indent=2, sort_keys=True, and trailing newline
    - Snapshot drift tests that regenerate in-process and byte-compare committed files

key-files:
  created:
    - scripts/export_contract.py
    - contracts/openapi.json
    - contracts/events.schema.json
    - tests/harness/server/test_contract_drift.py
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-01-SUMMARY.md
  modified: []

key-decisions:
  - "Used create_app(FIXED_TOKEN).openapi() as the only OpenAPI source so _force_event_schema injects EventEnvelope and all AgentEvent member schemas."
  - "Kept the exporter static: no live server, socket bind, subprocess, uvicorn, or direct FastAPI construction."
  - "Used the same dump format in exporter and drift gate to make byte equality the contract."

patterns-established:
  - "Contract exporter writes snapshots from repo-root-relative contracts/ and can be rerun safely in CI."
  - "Drift gate positive tests compare regenerated JSON strings against committed snapshots byte-for-byte."
  - "Synthetic drift test mutates a stable regenerated field name and asserts the equality gate would fail."

requirements-completed: [VSDK-TS-08]

# Metrics
duration: 5min
completed: 2026-06-07
---

# Phase V13.1-01: TypeScript SDK Contract Snapshot Summary

**Deterministic OpenAPI and EventEnvelope snapshots with a Python byte-for-byte drift gate for downstream SDK codegen**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-07T22:25:00Z
- **Completed:** 2026-06-07T22:30:27Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `scripts/export_contract.py`, a static exporter that calls `create_app(FIXED_TOKEN).openapi()` and `EventEnvelope.model_json_schema(...)`.
- Produced `contracts/openapi.json` and `contracts/events.schema.json` with stable formatting and confirmed `EventEnvelope` plus `"server.connected"` are present.
- Added `tests/harness/server/test_contract_drift.py` with two positive snapshot equality tests and one negative synthetic drift test.
- Confirmed the contract artifacts are git-tracked and currently byte-identical to `HEAD` after export.

## Task Commits

No commits were created during this Cursor execution. The requested plan artifacts are present and tracked; this run only left the new summary/state bookkeeping for review.

## Files Created/Modified

- `scripts/export_contract.py` - Static contract exporter for OpenAPI and standalone event JSON Schema snapshots.
- `contracts/openapi.json` - REST + forced EventEnvelope OpenAPI snapshot for SDK generation.
- `contracts/events.schema.json` - Standalone EventEnvelope JSON Schema snapshot for event codegen.
- `tests/harness/server/test_contract_drift.py` - Drift gate comparing regenerated snapshots to committed files and proving synthetic drift is caught.
- `.planning/phases/V13.1-typescript-local-client-sdk/V13.1-01-SUMMARY.md` - Execution summary and future-phase context.

## Decisions Made

- Followed the plan's fixed token and stable JSON dump format exactly so the exporter and drift gate share one byte-level contract.
- Imported `FIXED_TOKEN` from `scripts.export_contract` in the drift test while regenerating the schemas in-process from the source FastAPI app and Pydantic model.

## Deviations from Plan

None - plan executed as written. The only operational deviation was using `.venv/bin/python` for all parent verification after the system `python` command was unavailable.

## Issues Encountered

- Parent verification initially failed because `python` was not on PATH. Re-ran the same checks with `.venv/bin/python`; all passed.

## User Setup Required

None - no external service configuration required.

## Verification

- `.venv/bin/python scripts/export_contract.py`
- Contract content check: `EventEnvelope`, `"server.connected"`, and standalone `EventEnvelope` schema present.
- Determinism check: repeated exports of both `contracts/openapi.json` and `contracts/events.schema.json` diff clean.
- `.venv/bin/python -m pytest tests/harness/server/test_contract_drift.py -q` -> `3 passed`
- Test count check: exactly 3 `def test_` functions.
- Forbidden exporter path check: no `uvicorn`, `socket`, `subprocess`, or bare `FastAPI(` construction.

## Next Phase Readiness

V13.1 Wave-0 contract substrate is ready for TypeScript SDK codegen and for downstream V13.2 Rust SDK planning/execution gates that depend on `contracts/`.

---
*Phase: V13.1-typescript-local-client-sdk*
*Completed: 2026-06-07*
