---
phase: V13.1-typescript-local-client-sdk
plan: "06"
subsystem: sdk-typescript
tags: [typescript-sdk, vitest, integration-tests, voss-serve, sse, launcher]

# Dependency graph
requires:
  - phase: V13.1-05
    provides: Node-only launcher and dual-entrypoint build boundary
provides:
  - Vitest fixture that spawns real `voss serve` with `VOSS_SERVE_FAKE_TURN`
  - REST integration coverage for create, message, cost, delete, 401, and 409
  - SSE integration coverage for event ordering and abort-safe consumer teardown
  - Permission endpoint/body coverage for protocol replies under the fake-turn seam
  - Launcher integration coverage for turn execution and orphan-free dispose
affects: [V13.1, sdk-typescript, integration-tests, ci, local-client-sdk]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - `spawnVossServe(cwd)` fixture using piped stdin/stdout and `VOSS_SERVE_FAKE_TURN=1`
    - Source-level Vitest imports from `../src` for SDK behavior
    - PID polling with `process.kill(pid, 0)` after launcher dispose

key-files:
  created:
    - sdk/typescript/tests/helpers/serve-fixture.ts
    - sdk/typescript/tests/rest.test.ts
    - sdk/typescript/tests/sse.test.ts
    - sdk/typescript/tests/permission.test.ts
    - sdk/typescript/tests/launcher.test.ts
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-06-SUMMARY.md
  modified:
    - .planning/STATE.md

key-decisions:
  - "Used one real `voss serve` child per test file/test path through a shared fixture, with stdin EOF plus SIGTERM teardown."
  - "Verified REST 409 with a concurrent postMessage burst because the fake turn completes quickly."
  - "Verified permission at endpoint/body level because `VOSS_SERVE_FAKE_TURN` does not emit real permission gates."
  - "Kept all runtime tests importing SDK source modules rather than built `dist` artifacts."

patterns-established:
  - "Integration tests run with `PYTHON_BIN=<repo>/.venv/bin/python` so `voss[server]` imports resolve."
  - "Bare temporary cwds work for fake-turn sessions; no `.voss/` scaffolding is required."
  - "SSE tests wait for `server.connected` before posting, then collect through `session.idle`."

requirements-completed: [VSDK-TS-07, VSDK-TS-02, VSDK-TS-03, VSDK-TS-04, VSDK-TS-05]

# Metrics
duration: 5min
completed: 2026-06-07
---

# Phase V13.1-06: TypeScript SDK Integration Gate Summary

**Real `voss serve` Vitest suite covering REST, SSE, permission replies, and Node launcher supervision**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-07T23:07:00Z
- **Completed:** 2026-06-07T23:12:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Added `spawnVossServe(cwd)`, a shared fixture that launches real `voss serve` with `VOSS_SERVE_FAKE_TURN=1`, parses the handshake, and tears down via stdin EOF plus `SIGTERM`.
- Added REST integration tests for create -> message -> cost -> delete, bad-token 401, and concurrent-turn 409 as `VossApiError`.
- Added SSE integration tests asserting `server.connected` first, `session.idle` last, fake-turn typed events between, and clean abort behavior with no unhandled rejection.
- Added permission tests for the real `/permission` endpoint and `replyPermission` protocol body.
- Added launcher integration coverage for `VossLauncher.start()`, a real fake-turn message, `dispose()`, and PID disappearance.

## Task Commits

No commit command was run during this Cursor execution.

## Files Created/Modified

- `sdk/typescript/tests/helpers/serve-fixture.ts` - Real `voss serve` fixture with fake-turn env, handshake parsing, teardown, and PID polling.
- `sdk/typescript/tests/rest.test.ts` - REST happy path, 401, and 409 integration tests.
- `sdk/typescript/tests/sse.test.ts` - SSE ordering and abort cleanup integration tests.
- `sdk/typescript/tests/permission.test.ts` - Permission endpoint stale responses and `replyPermission` body-shape coverage.
- `sdk/typescript/tests/launcher.test.ts` - Launcher start/message/dispose/PID integration test.
- `.planning/phases/V13.1-typescript-local-client-sdk/V13.1-06-SUMMARY.md` - Execution summary and future context.
- `.planning/STATE.md` - Recent activity breadcrumb for Plan 06.

## Decisions Made

- Used a bare `mkdtemp` cwd for each server; A4 resolved green with no `.voss/` scaffolding needed.
- Verified A2 by running `.venv/bin/python -c "import uvicorn, sse_starlette"` successfully before the suite.
- Kept the permission gate test at endpoint/body level because the hermetic fake-turn seam intentionally bypasses `PermissionGate` and emits no `permission.updated` events.
- Used a concurrent burst for the 409 test to catch the short fake-turn busy window reliably.

## Deviations from Plan

### Auto-resolved Execution Choice

**1. Permission gate behavior under `VOSS_SERVE_FAKE_TURN`**
- **Found during:** Task 2 context mapping
- **Issue:** The fake turn emits canned user/thinking/plan/stream/final/idle events and never drives a real tool permission gate.
- **Fix:** Tested `POST /session/:id/permission` directly for stale replies across all allowed choices and added a `replyPermission` body-shape test.
- **Files modified:** `sdk/typescript/tests/permission.test.ts`
- **Verification:** Full Vitest suite passed.

---

**Total deviations:** 1 planned fallback path
**Impact on plan:** Keeps CI hermetic without provider credentials while still covering the §7 permission reply protocol.

## Issues Encountered

- The first combined phase-gate attempt ran the Python drift test from `sdk/typescript`, where `scripts.export_contract` was not importable. Rerunning the same gate from the repo root resolved it.

## User Setup Required

No external services. Local/CI integration runs should set `PYTHON_BIN` to the repo virtualenv interpreter, for example `PYTHON_BIN="$PWD/.venv/bin/python" npm --prefix sdk/typescript test -- --run`.

## Verification

- `.venv/bin/python -c "import uvicorn, sse_starlette"`
- `cd sdk/typescript && PYTHON_BIN="$(cd ../.. && pwd)/.venv/bin/python" npx vitest run tests/rest.test.ts` -> 3 passed
- `cd sdk/typescript && PYTHON_BIN="$(cd ../.. && pwd)/.venv/bin/python" npx vitest run tests/sse.test.ts tests/permission.test.ts tests/launcher.test.ts` -> 5 passed
- Full gate from repo root: `PYTHON_BIN="$PWD/.venv/bin/python" npm --prefix sdk/typescript test -- --run && npm --prefix sdk/typescript run build && npm --prefix sdk/typescript run check:core-imports && "$PWD/.venv/bin/python" -m pytest tests/harness/server/test_contract_drift.py -q`
- Full gate result: 4 TypeScript test files passed, 8 tests passed; build passed; dependency-cruiser found no violations; contract drift passed.
- Required grep checks for `VOSS_SERVE_FAKE_TURN`, `VossApiError`, `409`, `server.connected`, `session.idle`, `permission`, and PID polling all passed.
- Lints: no diagnostics.

## Next Phase Readiness

V13.1 now has the behavioral integration gate for the local TypeScript SDK. Remaining milestone work can treat REST, SSE, permission reply, and launcher supervision as verified against real `voss serve`.

---
*Phase: V13.1-typescript-local-client-sdk*
*Completed: 2026-06-07*
