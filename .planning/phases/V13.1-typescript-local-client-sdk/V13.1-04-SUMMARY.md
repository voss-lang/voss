---
phase: V13.1-typescript-local-client-sdk
plan: "04"
subsystem: sdk-typescript
tags: [typescript-sdk, sse, eventsource-parser, async-iterable, bearer-auth]

# Dependency graph
requires:
  - phase: V13.1-02
    provides: TypeScript SDK scaffold, generated OpenAPI types, and browser-safe barrel
provides:
  - Browser-safe SSE subscription helper for `GET /session/:id/events`
  - Typed `AgentEvent` alias from `components["schemas"]["EventEnvelope"]["event"]`
  - Async iterable yielding parsed AgentEvent values
  - Abort-safe stream teardown for consumer cancellation
affects: [V13.1, sdk-typescript, sse-client, event-stream, integration-tests]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - `fetch` SSE request with explicit `Authorization: Bearer <token>` header
    - `TextDecoderStream` piped through `EventSourceParserStream`
    - Abort-aware async generator that returns cleanly on consumer cancellation

key-files:
  created: []
  modified:
    - sdk/typescript/src/client/sse.ts
    - .planning/phases/V13.1-typescript-local-client-sdk/V13.1-04-SUMMARY.md

key-decisions:
  - "Used `eventsource-parser/stream` exactly, not the root package import."
  - "Aliased `AgentEvent` from `EventEnvelope.event` because the generated contract does not emit a standalone AgentEvent component."
  - "Handled consumer abort by swallowing AbortError/signal-aborted failures and ending iteration without POST /abort."

patterns-established:
  - "SSE consumers use `subscribeToEvents(baseUrl, sessionId, token, signal?)` and iterate `AgentEvent` values."
  - "SSE errors surface as `VossApiError(status, detail)` without including bearer tokens."
  - "Browser-safe SDK modules continue to avoid `node:*` imports."

requirements-completed: [VSDK-TS-03]

# Metrics
duration: 4min
completed: 2026-06-07
---

# Phase V13.1-04: TypeScript SSE Client Summary

**Browser-safe async iterable for authenticated Voss SSE AgentEvent streams**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-07T22:52:00Z
- **Completed:** 2026-06-07T22:56:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Replaced the `sse.ts` stub with `subscribeToEvents(baseUrl, sessionId, token, signal?)`.
- Opened `GET /session/:id/events` with `Authorization: Bearer <token>`.
- Piped the fetch body through `TextDecoderStream` and `EventSourceParserStream`.
- Parsed each SSE `data` payload and yielded it as the generated `AgentEvent` union.
- Added non-OK response handling via `VossApiError` and abort-safe generator teardown.

## Task Commits

No commit command was run during this Cursor execution.

## Files Created/Modified

- `sdk/typescript/src/client/sse.ts` - Fetch-based SSE async iterable with bearer auth, parsing, typed yields, and abort handling.
- `.planning/phases/V13.1-typescript-local-client-sdk/V13.1-04-SUMMARY.md` - Execution summary and future-phase context.

## Decisions Made

- Used `ReadableStream.getReader()` instead of `for await` so the implementation typechecks consistently against the current DOM lib configuration.
- Kept abort handling local to the generator: aborted fetch/stream reads return cleanly and do not call `POST /abort`, matching PROTOCOL §8.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

None blocking.

## User Setup Required

None.

## Verification

- `cd sdk/typescript && npx tsc --noEmit`
- `npm run build`
- `npm run check:core-imports` -> no dependency violations
- Required symbol check: `eventsource-parser/stream`, `EventSourceParserStream`, `Bearer`, `AgentEvent`, `subscribeToEvents`
- Browser-safety check: no `node:` imports in `sse.ts`
- Lints: no diagnostics

## Next Phase Readiness

The SDK has browser-safe REST, permission, and SSE surfaces ready for the later integration suite to verify fake-turn ordering, 401 handling, and clean abort behavior against `voss serve`.

---
*Phase: V13.1-typescript-local-client-sdk*
*Completed: 2026-06-07*
