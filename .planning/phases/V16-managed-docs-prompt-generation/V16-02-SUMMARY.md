---
phase: V16-managed-docs-prompt-generation
plan: 02
subsystem: cli
tags: [jinja2, templates, markdown, strictundefined]

requires:
  - phase: V16-01
    provides: SyncContext frozen dataclass (field names + absent-marker convention the templates render against)
provides:
  - "voss/templates/docs/cheatsheet.md.jinja — agent operating guide (D-06, imperative D-09)"
  - "voss/templates/docs/commands.md.jinja — layout-adjusted command reference (D-07, worktree-vs-repo-root divergence)"
  - "voss/templates/docs/review.md.jinja — review workflow, content gated on review.enabled (D-08)"
  - "voss/templates/docs/voss_md_fence.md.jinja — marker-free VOSS.md fence body (R4) with conditional review-doc link"
affects: [V16-03, V16-04]

tech-stack:
  added: []
  patterns:
    - "templates reference only SyncContext-declared fields; {% if %} guards every optional fact (D-04, StrictUndefined-safe)"
    - "fence-body template emits NO voss:begin/hash/end markers — write_fence_body owns structure (D-17)"
    - "render via dataclasses.asdict(SyncContext) through render_package_template only"

key-files:
  created:
    - voss/templates/docs/cheatsheet.md.jinja
    - voss/templates/docs/commands.md.jinja
    - voss/templates/docs/review.md.jinja
    - voss/templates/docs/voss_md_fence.md.jinja
    - tests/harness/test_doc_templates.py
  modified:
    - pyproject.toml

key-decisions:
  - "Layout divergence keyed on is_worktree: worktree render gets worktree-root invocation note + local-.voss caveat; repo-root gets plain repo-root note"
  - "Command subset = core (sync/chat/do) + capability-gated sections (memory, review→board+review, eval) — real CLI verbs only, no fabricated commands"
  - "Fence doc list derived inside the template from review.enabled — no extra SyncContext field needed"
  - "Test contexts built via dataclasses.asdict(SyncContext) so tests pin templates to declared fields"

patterns-established:
  - "Generated-doc header: first line '<!-- generated by `voss sync` — do not edit. -->'"

requirements-completed: [R3, R4]

duration: 5min
completed: 2026-06-10
---

# Phase V16 Plan 02: Doc & Fence Templates Summary

**Four Jinja templates rendering from SyncContext: cheatsheet/commands/review docs with do-not-edit headers and worktree-aware invocations, plus a marker-free VOSS.md fence body with review-conditional doc list**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-10T17:35:44Z
- **Completed:** 2026-06-10T17:40:15Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Three machine-owned doc templates (D-06/D-07/D-08, imperative D-09 voice) each opening with the generated do-not-edit header; commands.md renders different invocation text for repo-root vs worktree contexts (test-pinned)
- Fence-body template carries the full R4 struct (name/type, install/check, tools, capabilities, review config, generated doc list) and emits zero fence markers — write_fence_body owns VOSS.md structure (D-17)
- 11 tests prove header presence, layout divergence, review gating (docs + fence link), and StrictUndefined safety on a fully-bare context (all optional facts absent)
- `templates/docs/*` registered in setuptools package-data so wheels ship the new templates

## Task Commits

1. **Task 1: Three workflow doc templates** - `7d6c619` (test) + `0bcdf72` (feat)
2. **Task 2: Fence-body template** - `8149477` (feat; tests shared in 7d6c619)

## Files Created/Modified
- `voss/templates/docs/cheatsheet.md.jinja` - layout facts, where things live, capabilities, do/don't conventions
- `voss/templates/docs/commands.md.jinja` - invocation section (worktree-aware) + core/memory/review/eval command sections
- `voss/templates/docs/review.md.jinja` - reviewer list + board/review workflow when enabled; enable-hint when disabled
- `voss/templates/docs/voss_md_fence.md.jinja` - R4 fence body, marker-free, review.md link conditional
- `tests/harness/test_doc_templates.py` - 11 tests over full/bare/worktree/review-off contexts
- `pyproject.toml` - package-data + `templates/docs/*`

## Decisions Made
- Capability-gated command sections use `{% if "memory" in capabilities %}` style membership tests — keeps command list project-relevant per D-07 without new context fields
- review.md renders an enable-hint when disabled (template stays renderable) even though Plan 03's orchestrator skips writing it entirely (D-08)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] templates/docs/* absent from package-data**
- **Found during:** Task 1 (template creation)
- **Issue:** `pyproject.toml` package-data lists template dirs explicitly (`templates/init/*` etc.); without `templates/docs/*`, built wheels would ship without the new templates → PackageLoader TemplateNotFound at runtime
- **Fix:** Added `templates/docs/*` to `[tool.setuptools.package-data]`
- **Files modified:** pyproject.toml
- **Verification:** entry present; tests render via PackageLoader
- **Committed in:** 0bcdf72

---

**Total deviations:** 1 auto-fixed (missing critical)
**Impact on plan:** Packaging completeness only; no scope creep. (Note: `templates/agent/*`, `templates/prompts/*`, `templates/cognition/*` are also absent from package-data — pre-existing gap, not touched; relevant to V16-04 prompt sync.)

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 03 can render all four templates from `dataclasses.asdict(build_sync_context(cwd))` and hand the fence body straight to `write_fence_body`
- review.md write-skip (D-08) is orchestrator logic in Plan 03; template handles both states
- Pre-existing package-data gap for `templates/prompts/*` will matter for V16-04 (prompt sync) — flag for that executor

---
*Phase: V16-managed-docs-prompt-generation*
*Completed: 2026-06-10*
