# Phase T6 — PRD §2.4 Slash Debt — Plan Outline

**Mode:** chunked (outline-only). v0.1.1 patch — harden + test + discoverability. No new persistence (D-08).

| Plan ID | Objective | Wave | Depends On | Requirements |
|---|---|---|---|---|
| T6-01-cost-by-tool-approximation | Replace the `/cost --by-tool` "lands with T4" stub (cli.py:585-591) with the D-01 derived even-split aggregation (`cost_usd / len(tool_results)` per iteration, aggregated per tool name, loud `~approx (turn cost ÷ N tool calls)` legend, skip zero-cost/empty-tool iterations). Rewrite the now-obsolete `test_cost_by_tool_is_honest_stub` (test_repl_slash.py:225-231, deletes the `"T4" in out` tripwire) to assert the approximation. Carries the D-06 note (T6 owns both `--by-model` and `--by-tool`; T4-CONTEXT D-09 placeholder is obsolete; no T4 file edited). | 1 | — | SLASH-07 |
| T6-02-grouped-help-and-cli-signpost | Replace flat `_print_slash_help` (cli.py:1587-1589) with a GROUPED renderer (Editing / Session / Insight / Control + an Other fallback bucket so no registered slash is dropped), preserving the `help_lines` width-align style and existing one-line `help` strings (D-04). Add exactly ONE `voss --help` signpost line to the `main` group docstring (cli.py:2197-2201) — no `epilog=`, no slash-list duplication. Extend `TestSlashHelp` with a grouped-help assertion. Closes SC#3. | 2 | T6-01-cost-by-tool-approximation | SLASH-01, SLASH-02, SLASH-03, SLASH-04, SLASH-05, SLASH-06, SLASH-07 |
| T6-03-per-slash-happy-path-tests | Close the SC#1 per-slash integration-test gap and resolve D-07: add happy-path tests for `/diff` and `/resume` (id-prefix arm, exact-name arm, cross-cwd warning — monkeypatch `voss.harness.cli.session_store.load`, do NOT change session.py:222 resolution order, D-03); add the SC#2 `/why` audit test documenting the D-07 resolution (PRD Ticket 7 satisfied by the existing single `confidence:.2f` float; `ProbableValue` is a runtime-layer type, NOT a `/why` output-format mandate → NO `/why` code delta); roll-up audit confirming all SLASH-01..07 have ≥1 happy-path test and extend `test_t6_prd_slash_commands_registered` to cover `/cost`. Full-suite green gate. | 3 | T6-02-grouped-help-and-cli-signpost | SLASH-01, SLASH-03, SLASH-05, SLASH-06 |

**Wave rationale:** Plans 01/02/03 all touch `voss/harness/cli.py` and/or `tests/harness/test_repl_slash.py` (overlapping `files_modified`), so the file-ownership rule forces strict serialization (W1 → W2 → W3) rather than parallel waves. Each plan is small (~15-25% context). Plan 01 doubles as the test-scaffold-first step (it rewrites the `--by-tool` tripwire test alongside its code change, mirroring T4/T5 Wave-0 posture).

**Requirement coverage check:** SLASH-01 ✓(02,03) · SLASH-02 ✓(02) · SLASH-03 ✓(02,03) · SLASH-04 ✓(02) · SLASH-05 ✓(02,03) · SLASH-06 ✓(02,03) · SLASH-07 ✓(01,02). All seven covered.

**Source audit:** GOAL (ship PRD §2.4 slashes registered+tested+discoverable) → 01/02/03. REQ SLASH-01..07 → covered above. RESEARCH → none (intentionally skipped; SC#1 is the validation contract). CONTEXT: D-01→01, D-02→03(test, no code change — git-tree semantics kept), D-03→03, D-04→02, D-05→all, D-06→01 note, D-07→03, D-08→all (constraint). No unplanned items; no split needed.
