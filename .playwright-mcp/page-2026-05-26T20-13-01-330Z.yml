- generic [ref=e3]:
  - generic [ref=e4]: $ voss tools
  - generic [ref=e5]: "name mutating description --------------------- -------- ---------------------------------------- code_refresh no Rebuild the project code index (cache only, read-only to so… code_search no Structural code search using ast-grep (with regex fallback). find_definition no Find definition of a symbol using LSP + index. find_references no Find references to a symbol using LSP + index. fs_edit yes Replace exact `old` text with `new` in a file. `old` must a… fs_edit_many yes Atomically apply N edits to one file. Each `edits` entry is… fs_glob no List files matching a glob pattern, relative to cwd. fs_grep no Recursively search for a regex pattern. Returns matching li… fs_read no Read a UTF-8 text file from the project. Path must be insid… fs_read_many no Read N files as one bundle. Returns sections separated by `… fs_watch no Register a file-system watcher for glob patterns. Patterns … fs_watch_poll no Read incremental file-watch events by handle. since_ms is a… fs_write yes Write text to a file inside cwd. Creates parent dirs. Overw… git_diff no Run `git diff` (unstaged) or `git diff --cached` (staged) o… git_status no Run `git status --porcelain`. record_run yes (privileged) Close the current turn with semantic fields. D… shell_monitor no Read incremental output from a background job by handle. si… shell_run yes Run an allowlisted command (no shell). Output truncated to … shell_run_background yes Run an allowlisted command in the background; returns a bg-… shell_signal yes Send INT or TERM to a background job by handle. KILL is not… voss_budget_trace no Inspect recorded per-iteration budget usage for a persisted… voss_check no Run `voss check` on a .voss file or directory. Returns anal…"